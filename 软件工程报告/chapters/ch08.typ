#set heading(numbering: "1.1")
#show heading: it => {
  let level = it.level
  if level == 1 {
    v(20pt)
    align(center)[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 18pt, weight: "bold"); #it]
    v(15pt)
  } else if level == 2 {
    v(12pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 14pt, weight: "bold"); #it]
    v(6pt)
  } else {
    v(8pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 12pt, weight: "bold"); #it]
    v(4pt)
  }
}

= 第 8 章 结构化设计方法

// 第8章 结构化软件设计方法
// 从OCR提取并修正

面向数据流的结构化软件设计方法是基于模块化、自顶向下细化及结构化程序设计等技术基础上发展起来的，依据需求分析的结果"数据流图"推导出软件的系统功能结构图。该方法实施的要点是：

- 建立数据流的类型。
- 指明数据流的边界。
- 将数据流图映射到程序结构。
- 用"因子化"方法定义控制的层次结构。
- 用设计测量和一些启发式规则对结构进行细化。

== 系统功能结构图结构

=== 系统结构图中的模块

在系统结构图中，不能再分解的底层模块为原子模块。如果一个软件系统的全部实际"加工"（数据计算或处理）都由底层的原子模块来完成，而其他所有非原子模块仅仅执行控制或协调功能，那么这样的系统就是完全因子分解的系统，是最好的系统。一般而言，系统结构图有4种类型的模块，如图8-1所示。

#figure(
  image("img/fig_8_1.png", width: 80%),
  caption: [系统结构图的4种模块类型],
) <fig-8-1>

#par(first-line-indent: 0em)[（1）传入模块：从下属模块取得数据，经过某些处理过程，再将其传送给上级模块。]

#par(first-line-indent: 0em)[（2）传出模块：从上级模块获得数据，进行某些处理过程，再将其传送给下属模块。]

#par(first-line-indent: 0em)[（3）变换模块：即加工模块。它从上级模块取得数据，进行特定的处理过程，转换成其他形式，再传送回上级模块。大多数计算模块（原子模块）属于这一类。]

#par(first-line-indent: 0em)[（4）协调模块：对所有下属模块进行协调和管理的模块。在系统的输入/输出部分或数据加工部分可以找到这样的模块。在一个好的系统结构图中，协调模块应在较高层出现。]

在实际系统中，有些模块属于上述某一类型，还有一些模块是上述各种类型的组合。

=== 变换型数据流与变换型系统结构

变换型数据处理问题的工作过程大致分为三步，即取得数据、变换数据和给出数据，如图8-2所示。这三步反映变换型问题数据流的基本思想。其中，"变换数据"是数据处理过程的核心工作，而"取得数据"只不过是为它准备，"给出数据"则是对变换后的数据进行后处理工作。

#figure(
  image("img/fig_8_2.png", width: 80%),
  caption: [变换型数据流],
) <fig-8-2>

变换型系统结构图如图8-3所示，相应于取得数据、变换数据、给出数据，系统的结构图由输入、中心变换和输出等三部分组成。

#figure(
  image("img/fig_8_3.png", width: 50%),
  caption: [变换型的系统结构图],
) <fig-8-3>

=== 事务型数据流与事务型系统结构图

事务型数据处理问题的工作机理是接受一项事务，根据事务处理的特点和性质，选择分派一个适当的处理单元，然后给出结果。完成选择分派任务的部分称为事务处理中心，或分派部件。这种事务型数据处理问题的数据流图如图8-4所示。其中，输入数据流在事务中心处进行选择，激活某一种事务处理加工；D1—D4是并列的供选择的事务处理加工。

#figure(
  image("img/fig_8_4.png", width: 50%),
  caption: [事务型数据处理问题],
) <fig-8-4>

事务是一个最小的工作单元，不论成功与否都作为一个整体进行工作，并且不会有部分完成的事务。由于事务是由几个任务组成的，因此如果一个事务作为一个整体是成功的，则事务中的每个任务都必须成功。如果事务中有一部分失败，则整个事务失败。当事务失败时，系统返回事务开始前的状态。这个取消所有变化的过程称为"回滚"（rollback）。

事务还具有原子性（Atomicity）、一致性（Consistency）、隔离性（Isolation）和持久性（Durability）的特点，简称ACID。

#par(first-line-indent: 0em)[（1）原子性：用于标识事务是否完全地完成，一个事务的任何更新须在系统上完全完成。如果由于某种原因出错，事务不能完成它的全部任务，系统返回到事务开始前的状态。]

#par(first-line-indent: 0em)[（2）一致性：事务在系统完整性中实施一致性，这通过保证系统的任何事务最后都处于有效状态来实现。如果事务成功地完成，那么系统中所有的变化正确地应用，系统处于有效状态。如果在事务中出现错误，那么系统中所有的变化自动回滚，系统返回到原始状态。]

#par(first-line-indent: 0em)[（3）隔离性：在隔离状态执行事务，使它们好像是系统在给定的时间内执行的唯一操作。如果两个事务运行在相同的时间内，执行相同的功能，那么事务的隔离性确保每一事务在系统中认为只有该事务在使用系统。这种属性有时称为串行化，为了防止事务操作间混杂，必须串行化或序列化请求，使得在同一时间仅有一个请求用于同一数据。]

#par(first-line-indent: 0em)[（4）持久性："持久性"说明事务一旦执行成功，在系统中产生的所有变化是永久的。应该存在一些检查点，防止在系统失败时丢失信息。甚至硬件本身失败，系统的状态仍能通过在日志中记录事务完成的任务进行重建。"持久性"的概念允许开发者认为不管系统以后发生什么变化，完成的事务是系统永久的部分。]

事务型数据流图所对应的系统结构图是事务型系统结构图，如图8-5所示。

#figure(
  image("img/fig_8_5.png", width: 80%),
  caption: [事务型系统结构图],
) <fig-8-5>

在事务型系统结构图中，事务中心模块按所接受事务的类型选择某一个事务处理模块执行。各个事务处理模块是并列的，依赖一定的选择条件，分别完成不同的事务处理工作。每个事务处理模块可能调用若干个操作模块，而操作模块又可能调用若干个细节模块。不同的事务处理模块可以共享一些操作模块。同样，不同的操作模块又可以共享一些细节模块。

事务型系统结构图在数据处理中经常遇到，但是更多的是变换型与事务型系统结构图的结合。例如，变换型系统结构中的某个变换模块本身又具有事务型的特点。

== 变换映射

变换映射是一组设计步骤，将具有变换流特征的数据流图映射为一个预定义的程序结构模板。变换映射是体系结构设计的一种策略，运用变换映射方法建立初始的变换型系统结构图，然后对它进一步改进，最后得到系统的最终结构图。设计的步骤如下所述。

步骤1：复审基本系统模型（顶层数据流图和相应的软件需求规格说明书）。评估系统规格说明和软件需求规格说明。

步骤2：复审和细化软件的数据流图。重画数据流图时，可以从物理输入到物理输出，或者相反；还可以从顶层加工框开始，逐层向下检查各变换型加工是否具有高内聚的特性（即"加工"具有执行单一、独立的功能），以及每个"加工"是否具有足够多的细节信息，而无须再进一步细化。

重画数据流图的出发点是描述系统中的数据是如何流动的，在需求分析阶段的数据流图则侧重于描述系统如何加工数据。因此，重画数据流图应注意以下6个要点：

#par(first-line-indent: 0em)[（1）以需求分析阶段的数据流图为基础，可以从物理输入到物理输出，或者相反；可以从顶层加工开始，逐层向下。]

#par(first-line-indent: 0em)[（2）在图上不出现控制逻辑（例如判定和循环等），箭头只表示数据流而非控制流。]

#par(first-line-indent: 0em)[（3）不用考虑系统的开始和结束（假定系统在不停地运行）。]

#par(first-line-indent: 0em)[（4）省略每一个加工的简单例外处理，只考虑主要加工处理逻辑。]

#par(first-line-indent: 0em)[（5）当数据流进入和离开一个"加工"时，仔细地标记"加工"，不重名。]

#par(first-line-indent: 0em)[（6）如有必要，可以使用逻辑运算符"与"和"或"。]

步骤3：确定数据流图中含有变换流特征，还是含有事务流特征。通常，系统的信息流总能表示为变换型，但其中也可能遇到明显的事务流特征，这时可采用以变换型为主，在局部范围采用事务型的设计方法。

步骤4：区分输入流、输出流和中心变换部分，即标明数据流的边界。不同的设计人员可能选择不同的数据流边界，这导致不同的系统结构图。输入流被描述为信息从外部形式变换为内部形式的路径，输出流是信息从内部形式变换为外部形式的路径。但是，输入流和输出流的边界并未加以说明，这导致不同的设计人员在选择数据流边界时会有所不同。

#par(first-line-indent: 0em)[（1）中心变换：多股数据流汇集的地方往往是系统的中心变换部分。]

#par(first-line-indent: 0em)[（2）逻辑输入：可以从数据流图上的物理输入开始，逐步向系统中间移动，一直到数据流不再被看作是系统的输入为止，则其前一个数据流就是系统的逻辑输入。可以认为逻辑输入就是离物理输入端最远的，且仍被看作是系统输入的数据流。]

#par(first-line-indent: 0em)[（3）逻辑输出：从物理输出端开始，逐步地向系统中间移动，就可以找到离物理输出端最远，且仍被看作是系统输出的数据流。]

步骤5：进行一级"因子化"分解，设计顶层和第一层模块。程序结构表示控制自顶向下的分布，因子化的作用是得到一个顶层的模块（主模块）完成决策。低层模块完成大多数输入、计算和输出工作的程序结构；中层的模块既完成一部分控制，又完成适当的变换工作。

首先设计主模块，用程序名字为它命名，将它画在与中心变换相对应的位置上。作为系统的顶层，它调用下层模块，完成系统所需完成的各项工作。系统结构第一层的设计方针：为每一个逻辑输入设计一个输入模块，它为主模块提供数据；为每一个逻辑输出设计一个输出模块，它将主模块提供的数据输出；为中心变换设计一个变换模块，它将逻辑输入转换成逻辑输出。第一层模块与主模块之间传送的数据应与数据流图相对应。

步骤6：进行二级"因子化"分解，设计中、下层模块。第二级因子将数据流图中的每一个变换型加工映射为程序结构中的模块（一个输入模块、输出模块、变换模块）。从变换中心的边界开始，沿输入路径和输出路径向外，将"变换"依次映射到低层的软件结构中。这一步工作是自顶向下，逐层细化数据流图，为每一个输入模块、输出模块、变换模块设计它们的从属模块。

第二级因子化的结果往往是现实考虑和设计质量要求的折中结果，后期的软件设计复审和细化还可能导致这个功能模块结构变化，但这个结构仍然可以作为系统的初始功能结构图。

输入模块向调用它的上级模块提供数据，因而它必须有两个下属模块：一个是接收数据；另一个是把这些数据变换成它的上级模块所需的数据。输出模块从调用它的上级模块接收数据，用以输出，因而也应当有两个下属模块：一个是将上级模块提供的数据变换成输出的形式；另一个是将它们输出。中心变换模块的下层模块没有通用的设计方法，一般应参照数据流图的中心变换部分和功能分解的原则来考虑如何对中心变换模块进行分解。

根据以上方法推导出来的最初程序结构设计结果还须为每个功能模块进行命名，以体现其功能，并为每个模块提供简要的处理说明，如图8-6所示。处理说明应该包含以下内容：

#par(first-line-indent: 0em)[（1）模块输入和输出的信息，即接口描述。]

#par(first-line-indent: 0em)[（2）模块需处理的信息，局部数据结构中存储的数据。]

#par(first-line-indent: 0em)[（3）过程描述，指明该功能模块主要的逻辑规则和任务。]

#par(first-line-indent: 0em)[（4）与该功能模块的有关限制条件和特殊的要求（I/O、与硬件相关的特征、时间要求等）。]

#figure(
  image("img/fig_8_6.png", width: 80%),
  caption: [由变换型数据流图推导出的系统初始结构图],
) <fig-8-6>

步骤7：利用一些启发式原则（应用模块的独立概念）来改进系统的初始结构图，直到得到符合要求的结构图为止。主要考虑这些功能模块具有高内聚、低耦合的程序结构，而且最重要的是易于后期功能实现、测试和维护的程序结构。

== 事务映射

很多应用中存在某一种作业数据流，它可以引发一个或多个处理。这种数据流称为事务。与变换映射类似的是，事务映射也是从分析数据流图开始，自顶向下，逐步分解，建立有别于变换型的事务型系统结构图。这里以如图8-7(a)所示的数据流图为例，说明如何推导事务型的系统结构图。

#figure(
  image("img/fig_8_7.png", width: 80%),
  caption: [由数据流图推导事务型系统结构图示意],
) <fig-8-7>

步骤1：复审基本系统模型。

步骤2：复审和细化软件的数据流图。

步骤3：确定数据流图中含有变换流特征，还是含有事务流特征。以上三步与变换映射中的相应工作相同。

步骤4：识别事务中心和每一条操作路径上的流特征。事务中心通常位于几条操作路径的起始点上，可以从数据流图上直接找出。输入路径必须与其他所有操作路径区分开来。

在如图8-7(a)所示的数据流图中，首先确定它是具有事务型特征的数据流图，即数据流A是一个带有"请求"性质的信息，即为事务源，加工T则具有"事务中心"的功能，后继的三个加工L、M、N是并列的，在加工T的选择控制下完成不同功能的处理。最后，经过加工O将某一加工处理的结果整理输出。

步骤5：将数据流图映射到事务型系统结构图上。事务流应映射到包含一个输入分支和一个分类事务处理分支的程序结构上。输入分支结构的开发与变换流的方法类似。分类事务处理分支结构包含一个调度模块，调度和控制下属的事务处理模块。

为此，首先建立一个主模块用以代表整个"加工"，它位于P层（主层）；然后考虑被称为T层（事务层）的第二层模块。第二层模块只能是三类：取得事务、处理事务和给出结果。在图8-7(b)中，依据并列的三个"加工"，在主模块之下建立三个事务模块，分别完成L、M和N的工作，并在主模块的下沿以菱形引出对这三个事务模块的选择，而在这些事务模块的左右两边则是对应于加工I和O的"取得A"模块和"给出H"模块。

各个事务模块下层的操作模块，即A层（活动层）和细节模块，即D层（细节层），在图中未画出，可以继续分解扩展，直至完成整个结构图。

步骤6："因子化"分解和细化该事务结构和每一条操作路径的结构。每一条操作路径的数据流图由它的信息流特征，可以是变换流也可以是事务流。与每一条操作路径相关的子结构可以依照前面介绍的设计步骤进行开发。

步骤7：利用一些启发式原则来改进系统的初始结构图。

== 变换-事务混合型的系统结构图

变换分析是软件系统结构设计的主要方法。因为大部分软件系统都可以应用变换分析进行设计。但是，在很多情况下，仅使用变换分析是不够的，需要其他补充方法。事务分析就是最重要的一种方法。虽然不能说全部数据处理系统是事务型，但是很多数据处理系统属于事务型系统。

一般来讲，大型的软件系统不可能是单一的数据变换型，也不可能是单一的事务型，通常是变换型结构和事务型结构的混合体。在具体的应用中，一般以变换型为主、事务型为辅的方式进行软件结构设计。图8-8是典型的变换-事务混合型的系统结构图。

#figure(
  image("img/fig_8_8.png", width: 80%),
  caption: [一个典型的变换-事务混合型的系统结构],
) <fig-8-8>

== 改进系统功能结构图的启发式原则

一旦经过上述方法的分析和设计得到初步的系统功能结构图，还须利用启发式原则对系统功能结构图进行改进和优化。

=== 模块功能的完善

一个完整的功能模块不仅应能完成指定的功能，而且还应当能够告诉使用者完成任务的状态，以及不能完成的原因。完整的模块应具有以下两个部分，且这两个部分应当看作是一个模块的有机组成部分，不应分离到其他模块中，否则将增大模块间的耦合程度：

- 规定的功能部分；
- 出错处理部分。当模块不能完成规定的功能时，必须返回出错信息和标志，向它的调用者报告出现这种例外情况的原因。
- 如果须返回一系列数据给它的调用者，完成数据加工时应给它的调用者返回一个该模块执行是否正确结束的"标志"。

=== 消除重复功能，改善软件结构

在系统的初始结构图得出之后，应当审查分析这个结构图。如果发现几个模块的功能有相似之处，可以加以改进：

#par(first-line-indent: 0em)[（1）完全相似：在结构上完全相似，可能只是在数据类型上不一致。此时可采取完全合并的方法，只需在数据类型的描述上和变量定义上加以修改即可。]

#par(first-line-indent: 0em)[（2）局部相似：如图8-9(a)所示，虚线框部分是相似的部分。这时不能简单地把两者合并为一个部分，如图8-9(b)所示，因为这样合并后的模块内部必须设置许多的查询开关，如图8-9(f)所示，很大程度上把模块降低到逻辑内聚一级。处理的办法是分析R1和R2，找出两者之间的相同部分，并从R1和R2中分离，重新定义一个独立的下一层模块。R1和R2剩余的部分根据情况还可以与它的上级模块合并，以减少控制的传递、全局数据的引用和接口的复杂度，形成如图8-9(c)、图8-9(d)、图8-9(e)所示的三种方案，这些方案在减少模块间的耦合度、提高模块的内聚度方面，收到较好效果。]

#figure(
  image("img/fig_8_9.png", width: 80%),
  caption: [模块相似的拆分及合并方案],
) <fig-8-9>

=== 模块的作用范围

模块的控制范围包括它本身及其所有的从属模块，如图8-10(a)所示。模块A的控制范围为模块A、B、C、D、E、F、G。模块C的控制范围为模块C、F、G。模块的作用范围是指模块内一个判定的作用范围，凡是受这个判定影响的所有模块都属于这个判定的作用范围。如果一个判定的作用范围包含在这个判定所在模块的控制范围之内，则这种结构是简单的，否则是不简单的。

#figure(
  image("img/fig_8_10.png", width: 80%),
  caption: [模块的控制范围与作用范围的关系],
) <fig-8-10>

图8-10(b)表明，作用范围不在控制范围之内。模块G判定之后，若需要模块C工作，则必须把信息回送给模块D，再由D把信息回送给模块B，从而就增加数据的传送量和模块间的耦合，使模块之间出现控制耦合，这显然不是一个好的设计。图中黑框表示判定的作用范围。图8-10(c)虽然表明模块的作用范围是在控制范围之内，可是判定所在模块TOP所处层次太高，这样也须经过不必要的信息传送，从而增加数据的传送量。虽然可以用，但不是较好的结构。图8-10(d)表明作用范围在控制范围之内，只有一个判定分支有一个不必要的穿越，是一个较好的结构。图8-10(e)是个比较理想的结构。从以上的比较中可知，在一个设计得很好的系统模块结构图中，所有受一个判定影响的模块应该都从属于该判定所在的模块，最好局限于给出判定的那个模块及其直接下属模块。

在设计过程中，如果作用范围不在控制范围之内，可应用如下办法把作用范围移到控制范围之内：

- 将判定所在模块合并到父模块中，使判定处于较高的层次；
- 将受到判定影响的模块下移到控制范围内；
- 将判定上移到层次中较高的位置。

=== 减少高扇出结构

经验证明，对于一个设计得很好的软件模块结构，通常上层扇出比较高，中层扇出较少，底层扇入到有高扇入的公用模块中。模块的扇出过大，使得系统的模块结构图的宽度变大，宽度越大，结构图越复杂。比较适当的模块扇出数目为2～5，最多不超过9。模块的扇出过小也不好，比如每个模块的扇出总是1，这样使得系统的功能结构图的深度大大增加，不但增加模块接口的复杂度，而且增加调用和返回的时间开销，从而降低系统的工作效率。经验表明，对于一个设计得较好的软件模块结构，平均扇出数目为3～4。

模块的扇出指模块调用子模块的个数。如果一个模块的扇出过大，表明模块过于复杂，它需要协调和控制过多的下属模块。如图8-11(a)所示，模块J的扇出为10，属于高扇出的结构，为此须增加一个中间层次的两个模块P1和P2，如图8-11(b)所示，以降低扇出的数目。

#figure(
  image("img/fig_8_11.png", width: 80%),
  caption: [高扇入和高扇出的分解示意图],
) <fig-8-11>

一个模块的扇入数目很大，说明共享该模块的上级模块数目增多。但如果一个模块的扇入太大，比如超过7或8，而且它又不是公用模块，说明该模块可能具有多个功能。为此应当对其进一步分析并将其功能分解。如图8-11(c)所示，模块Q的扇入数目为9，它又不是公用模块，通过分析得知它是具有三个功能的模块，可以对其进行分解。为此在Q模块的上面增加一个中间层次的三个控制模块Q1、Q2、Q3，而把真正公用部分提取出来留在Q中，使其成为这三个控制模块的公用模块，使各模块的功能单一化，从而改善模块结构，如图8-11(d)所示。

=== 避免或减少使用病态联接

应限制使用如下三种病态联接：直接病态联接（内容耦合）、公共数据域病态联接（公共耦合）和通信模块病态联接。

- 直接病态联接：即模块A直接从模块B内部取出某些数据，或者把某些数据直接送到模块B内部，如图8-12(a)。
- 公共数据域病态联接：模块A和模块B通过公共数据域直接传送或接收数据，而不是通过它们的上级模块。这种方式使得模块间的耦合度剧增。它不仅影响模块A和模块B，而且影响与公共数据域有关联的所有模块，如图8-12(b)。
- 通信模块病态联接：即模块A和模块B通过通信模块TABLEIT传送数据。从表面看，这不是病态联接，因为模块A和模块B都未涉及通信模块TABLEIT的内容。然而，它们之间的通信（即数据传送）没有通过它们的上级模块。从这个意义上讲，这种联接是病态的，如图8-12(c)所示。

#figure(
  image("img/fig_8_12.png", width: 80%),
  caption: [三种病态联接模块示意图],
) <fig-8-12>

=== 模块的大小适中

限制模块的大小是减少复杂度的手段之一，因而要求把模块的大小限制在一定的范围之内，通常规定其语句行数为50～100，最多不超过500行。

实际上，容量过大的模块往往由于分解不充分，且具有多个功能，因此须对功能进一步分解，生成一些下级模块或同层模块。反之，模块容量较小时也可以考虑是否可能与调用它的上级模块合并。

=== 设计功能可预测的模块，避免过度受限制的模块

一个功能可预测的模块不论内部处理细节如何，对于相同的输入数据，总能产生同样的结果。但是，如果模块内部蕴藏一些特殊的鲜为人知的功能，则这个模块可能是不可预测的。对于这种模块，如果调用者不小心使用，其结果将不可预测。调用者无法控制这个模块如何执行，或者不能预知将引起什么后果，最终造成混乱。

图8-13(a)是一个功能不可预测的例子。模块内部保留一个内部标记M，模块在运行过程中由这个内部标记确定什么处理。由于这个内部标记对于调用者来说是隐藏起来的，因而调用者无法控制这个模块如何执行，或者不能预知将引起什么后果，最终造成混乱。

#figure(
  image("img/fig_8_13.png", width: 80%),
  caption: [不可预测模块和受限模块],
) <fig-8-13>

对于一个仅处理单一功能的模块，由于具有高度的内聚度，而受到设计人员的重视。但是，如果限制一个模块局部数据结构的大小、控制流的选择或者与外界（人、软硬件）的接口模式，如图8-13(b)所示，则很难适应用户新的要求或环境的变化，从而给将来的软件维护造成很大的困难，使得人们不得不花费更大的代价来消除这些限制条件。

为了能够适应将来的变化，软件模块中局部数据结构的大小应当是可控制的，调用者可以通过模块接口上的参数表或一些预定义外部参数来规定或改变局部数据结构的大小。另外，控制流的选择对于调用者来说，应当是可预测的。与外界的接口应当是灵活的，可以用改变某些参数的值来调整接口的信息，以适应未来的变化。

=== 软件包应满足设计约束和可移植性

运用变换分析方法建立系统的结构图时应当注意以下四点。

#par(first-line-indent: 0em)[（1）在选择模块设计的次序时，必须对一个模块的全部直接下属模块都设计完成之后才转向另一个模块的下层模块的设计。]

#par(first-line-indent: 0em)[（2）在设计下层模块时，应考虑模块的低耦合和高内聚问题，提高初始结构图的质量。]

#par(first-line-indent: 0em)[（3）注意黑盒技术的使用。在设计当前模块时，先把这个模块的所有下属模块定义成只知道功能和调用方式的"黑盒"，并在系统设计中利用它们，暂时不考虑它们的内部结构和实现方法。在这一步定义好的"黑盒"，由于已确定了它的功能和输入、输出，在下一步就可以对它们进行设计和加工。这样，又会导致更多的"黑盒"。最后，全部"黑盒"的内容和结构应完全被确定。]

#par(first-line-indent: 0em)[（4）如果出现了以下情况，就停止模块的功能分解：]

- 当模块不能再细分为明显的子任务时；
- 当分解成用户提供的模块或程序库的子程序时；
- 当模块的界面是输入/输出设备传送的信息时；
- 当模块不宜再分解得过小时。

== 数据设计和文件设计的原则

=== 数据设计的原则

R. S. Pressman把数据设计的过程概括成以下两步：

#par(first-line-indent: 0em)[（1）为在需求分析阶段所确定的数据对象选择逻辑表示，必须对不同结构进行算法分析，以便选择一个最有效的设计方案。]

#par(first-line-indent: 0em)[（2）确定对逻辑数据结构所必需的那些操作的程序模块（软件包），以便限制或确定各个数据设计决策的影响范围。]

无论采取什么样的设计方法，如果数据设计得好，往往能产生很好的软件系统结构，具有很强的模块独立性和较低的程序复杂性。

Pressman提出了一组原则，用来定义和设计数据：

#par(first-line-indent: 0em)[（1）用于软件的系统化方法也适用于数据。应当考虑几种不同的数据组织方案，还应当分析数据设计给软件设计带来的影响。]

#par(first-line-indent: 0em)[（2）要确定所有的数据结构和在每种数据结构上施加的操作。对于涉及软件中若干个功能的实现处理的复杂数据结构，可以为它定义一个抽象数据类型。]

#par(first-line-indent: 0em)[（3）应当建立一个数据词典并用它来定义数据和软件的设计。]

#par(first-line-indent: 0em)[（4）低层数据设计的决策应推迟到设计过程的后期进行。可以将逐步细化的方法用于数据设计。在需求分析时确定总体数据组织，在概要设计阶段加以细化，而在详细设计阶段才规定具体的细节。]

#par(first-line-indent: 0em)[（5）数据结构的表示只限于那些必须直接使用该数据结构内数据的模块才能知道。此原则就是信息隐藏和与此相关的耦合原则，把数据对象的逻辑形式与物理形式分开。]

#par(first-line-indent: 0em)[（6）数据结构应当设计成为可复用的。建立一个存有各种可复用的数据结构模型的构件库，以减少数据定义和设计的工作量。]

#par(first-line-indent: 0em)[（7）软件设计和程序设计语言应当支持抽象数据类型的定义和实现。如果没有直接定义某种复杂数据结构的手段，这种结构的设计和实现往往是很困难的。]

以上原则可适用于软件工程的定义阶段和开发阶段。"清晰的信息定义是软件开发成功的关键。"

=== 文件设计的过程

文件设计指数据存储文件设计，其主要工作是根据使用要求、处理方式、存储的信息量、数据的活动性，以及所能提供的设备条件等确定文件类别，选择文件媒体，决定文件组织方法，设计文件记录格式，并估算文件的容量。

文件设计的过程主要分为两个阶段。第一个阶段是文件的逻辑设计，主要在概要设计阶段实施。它包括以下各个步骤。

#par(first-line-indent: 0em)[（1）整理必需的数据元素。在软件设计中所使用的数据有长期的，有短期的，还有临时的。它们都可以存放在文件中，在需要时对它们进行访问。因此首先必须整理应存储的数据元素，给它们一个易于理解的名称，指明其类型和位数，以及其内容含义。]

#par(first-line-indent: 0em)[（2）分析数据间的关系。分析在业务处理中哪些数据元素是同时使用的，把同时使用次数多的数据元素归纳成一个文件进行管理。分析数据元素的内容，研究数据元素与数据元素之间的逻辑关系，根据分析结果，明确数据元素的含义及其属性。]

#par(first-line-indent: 0em)[（3）确定文件的逻辑设计。根据数据关联分析，明确哪些数据元素应当归于一组进行管理，把应当归于一组的数据元素进行统一布局，产生文件的逻辑设计。]

第二个阶段是文件的物理设计，主要在软件的详细设计阶段实施。主要工作有以下5项。

#par(first-line-indent: 0em)[（1）理解文件的特性。针对文件的逻辑规格说明，进一步研究从业务处理的观点来看所要求的一些特性，包括文件的使用率、追加率和删除率，以及保护和保密等。]

#par(first-line-indent: 0em)[（2）确定文件的存储媒体。选择文件的存储媒体时，应当考虑以下一些因素。]

- 数据量：根据处理数据量，估算需要媒体的数量。数据量大的文件可选用磁带、磁盘或光盘作为存储媒体，数据量小的文件可采用软盘作为存储媒体。
- 处理方式：处理方式有联机处理和批处理。对于联机处理，多选用直接存取设备，如磁盘等；对于批处理，选用任何一种存储媒体都可以。
- 存取时间和处理时间：批处理对于时间没有严格的要求，因此对存储媒体也没有特殊的要求。实时处理最好选用直接存取媒体，如磁盘等，以满足响应时间的要求。
- 数据结构：根据文件的数据结构，选用能实现其结构的合适媒体及相应的存取方法。例如，顺序文件可选用磁带或光盘，而索引文件和散列文件则必须选用磁盘。
- 操作要求：对于数据量大、执行时较少，且要求用户干预的文件，应当选用磁带媒体；对于频繁交互的文件，应当选用磁盘媒体。
- 费用要求：在满足上述要求的基础上，应当尽量选用价格低的媒体。

#par(first-line-indent: 0em)[（3）确定文件的组织方式。根据文件的特性确定文件的组织方式。常用的文件组织方式有：顺序文件（按记录的加入先后次序排列、按记录关键码的升序或降序排列、按记录的使用频率排列）、直接存取文件（无关键码直接存取文件、带关键码直接存取文件、桶式直接存取文件）、索引顺序文件（B+树）、分区文件、虚拟存储文件、倒排文件等。]

#par(first-line-indent: 0em)[（4）确定文件的记录格式。确定文件的组织方式之后，须进一步确定文件记录中各数据项及它们在记录中的物理安排。考虑设计记录的布局时，应当注意以下5点。]

- 记录的长度：设计记录的长度应确保能满足需要，还应考虑使用设备的制约和效率，尽可能与读写单位匹配，并尽可能减少处理过程中内外存的交换次数。
- 数据项的顺序：对于可变长记录应在记录的开头记入长度信息；对于关键码，应尽量按级别高低顺序配置；联系较密切的数据项应归纳在一起进行配置。
- 数据项的属性：属性相同的数据项应尽量归纳在一起配置；数据项应按双字长、全字长、半字长和字节的属性顺序配置。
- 预留空间：考虑到将来可能变更或扩充，应当预先留一些空闲空间。
- 子数据项：可把一个数据项分成几个子数据项，每一个子数据项也可以作为单独的项来使用。

#par(first-line-indent: 0em)[（5）估算存取时间和存储容量（不要求）。]

== 设计的后处理

为了有效地进行变换映射和事务映射，须补充一些附加的文档。这部分内容应当是软件概要设计文档的一个组成部分。在程序结构被设计和细化以后，必须为每一个模块写一份处理说明，为每一个模块提供一份接口说明，确定全局数据结构和局部数据结构，指出所有的设计约束和限制条件，然后进行设计评审并进行设计优化（如果需要和可能）。

=== 处理说明

处理说明是一个关于模块内部处理的清晰且无歧义的正确描述。这种说明描述模块的主要处理任务、条件抉择和输入/输出。例如，图8-6给出一个变换型问题的系统结构图。其中，"给出结果2"模块的处理说明可以如下：

"给出结果2"模块调用"格式化2"模块，将内部编码形式的"计算值2"转换成以ASCII码表示的文本形式的预格式化数据，再调用"给出结果"模块，进一步转换成按预定的图表安排的形式输出。

这种处理说明可作为初始的模块说明，以后在详细设计时还将进一步具体化。

=== 接口说明

接口说明给出一张表格，列出所有进入模块和从模块输出的数据。接口说明应包括通过参数表传递的信息、对外界的输入/输出信息、访问全局数据区的信息等。此外，还指出其下属的模块和上级模块。例如，对于图8-6给出的程序结构图中的"格式化2"模块，其接口说明如下：

```text
PROCEDURE format-2;                          //过程format-2（格式化2）
INTERFACE ACCEPTS;                           //入口
  TYPE calc-value-2 IS BINARY CODE;          //类型calc-value-2是二进制码
INTERFACE RETURNS;                           //出口
  TYPE preformatted-data IS NUMERIC          //类型preformatted-data是数值型
  * no external I/O or global data Used      //无外部I/O或全局数据
  * called by put-result-2                   //所调用：模块put-result-2
  * calls no subordinate modules             //调用：无下属模块
```

=== 数据结构说明

数据结构的设计对每个模块的程序结构和过程细节都有深刻的影响，在软件结构确定之后，必须确定全局的和局部的数据结构。数据结构的描述可以用伪码（如PDL类Pascal语言）或Warnier图等形式表达。

=== 概要设计评审

一旦所有模块的设计文档完成以后，就可进行设计评审。在评审中，应着重评审软件需求是否满足，软件结构的质量、接口说明、数据结构说明、实现和测试的可行性和可维护性等。

软件生命周期每个阶段的工作不可能不引入人为的错误。某一阶段出现的错误如果不及时纠正，即传播到开发的后续阶段，并在后续阶段中引出更多的错误。图8-14是错误的扩大效应模型。由这个模型可知，开发的每一阶段都可能由于疏忽而产生错误，如果未经评审进入下一阶段，在下一阶段中，有些错误可能扩大X倍，再加上新产生的错误，从而导致更多的错误，并传播到后续阶段。

#figure(
  image("img/fig_8_14.png", width: 80%),
  caption: [错误的扩大模型],
) <fig-8-14>

图8-15(a)表示在软件设计阶段与编码阶段不进行评审的情况，最后可能把12个潜在的错误带到运行环境中。在同样条件下，如果进行评审，如图8-15(b)所示，最后只有3个潜在的错误传播到运行环境。

#figure(
  image("img/fig_8_15.png", width: 80%),
  caption: [有评审和无评审情况下错误的传播比较],
) <fig-8-15>

=== 设计的优化

在软件的程序结构和数据结构已经按照功能和性能需求，以及按照设计标准和直觉设计出之后，再进行设计的优化工作。过早地考虑优化设计是没有意义的。

应当了解的是，简明的结构往往是精巧和高效的。优化设计需力争使模块的个数最少，这与有效的模块化不矛盾。同时，还应当寻求尽量简单，且满足信息需求的数据结构。

对于有时间运行要求的应用问题，在详细设计阶段和编码阶段必须进行优化。应注意，一个程序段的代码行数可能只占整个软件系统一个相当小的百分比（一般为10%～20%），却常常占整个系统运行时间很大的百分比（如50%～80%）。因此，对于有时间效率要求的软件，可以参考以下原则：

#par(first-line-indent: 0em)[（1）在不考虑时间和运行要求进行优化的条件下构造并改进软件的结构。]

#par(first-line-indent: 0em)[（2）在细节设计的过程中，挑出那些有可能占用过多时间的模块，并为这些模块精心设计出时间效率更高的过程（算法）。]

#par(first-line-indent: 0em)[（3）用高级程序设计语言编写代码程序。]

#par(first-line-indent: 0em)[（4）检测软件，分离出占用大量处理机资源的模块。]

#par(first-line-indent: 0em)[（5）如果有必要，用依赖机器的语言（机器指令、汇编语言）重新设计或重新编码，以提高软件的开发效率。]

== 详细设计

在概要设计阶段，软件设计人员完成软件系统的总体结构设计，确定与需求规格说明书相对应的各个功能模块及这些模块之间的关系，此时还须进一步明确各个模块内部是如何实现这些功能的，这个阶段就是软件的详细设计。

从软件开发的工程化观点来看，在使用程序设计语言编制程序以前，须对所采用算法的逻辑关系进行分析，设计出全部必要的过程细节，并给予清晰的表达，使之成为编码的依据，这就是详细设计的任务。

表达详细设计规格说明的工具称为详细设计工具，可以分为三类：图形工具、表格工具和语言工具。常用的用于描述结构化程序逻辑结构的图形工具有程序流程图、N-S图及PAD。

=== 程序流程图

程序流程图比较直观清晰，易于学习掌握，流程图也存在一些严重的缺点，例如流程图所使用的符号不够规范，常使用一些习惯用法。特别是表示程序控制流程的箭头可以不受任何条件约束，可随意转移控制。这些现象显然是与软件工程化的要求相背离的。为了消除这些缺点，应对流程图所使用的符号给出严格的定义，不允许随意画出各种不规范的流程图。例如，为使用流程图描述结构化程序，必须限制流程图，只能使用图8-16所给出的五种基本控制结构。

#figure(
  image("img/fig_8_16.png", width: 80%),
  caption: [流程图的基本控制结构],
) <fig-8-16>

任何复杂的程序流程图都应由这五种基本控制结构组合或嵌套而成。作为上述五种控制结构相互组合和嵌套的实例，图8-17给出一个程序的流程图。图中增加一些虚线构成的框，目的是便于理解控制结构的嵌套关系。显然，这个流程图所描述的程序是结构化的。

#figure(
  image("img/fig_8_17.png", width: 70%),
  caption: [嵌套构成的流程图实例],
) <fig-8-17>

=== N-S图

Nassi和Shneiderman提出一种符合结构化程序设计原则的图形描述工具，称为盒图，也称为N-S图。为表示五种基本控制结构，N-S图规定五种图形构件，参看图8-18。

#figure(
  image("img/fig_8_18.png", width: 80%),
  caption: [N-S图的五种基本控制结构],
) <fig-8-18>

为说明N-S图的使用方法，仍用图8-17给出的实例，将它用如图8-19所示的N-S图表示。如前所述，任何一个N-S图都是前面介绍的五种基本控制结构相互组合与嵌套的结果。当问题很复杂时，N-S图可能很大。

#figure(
  image("img/fig_8_19.png", width: 85%),
  caption: [N-S图的实例],
) <fig-8-19>

N-S图有以下4个特点：

- 图中每个矩形框（除CASE构造中表示条件取值的矩形框外）都是明确定义的功能域（即一个特定控制结构的作用域），以图形表示，清晰可见。
- 它的控制转移不能任意规定，必须遵守结构化程序设计的要求。
- 很容易确定局部数据和（或）全局数据的作用域。
- 很容易表现嵌套关系，也可以表示模块的层次结构。

任何一个N-S图都是前面介绍的五种基本控制结构相互组合与嵌套的结果。当问题很复杂时，N-S图可能很大，在一张纸上画不下，这时可给这个图中一些部分取名称，在图中相应位置用名称（用椭圆形框住它）而不是用细节表现这些部分。然后，在另外的纸上再把这些命名的部分进一步展开。例如，图8-20(a)中判断X取值为T部分和取值为F部分用矩形框界定的功能域中画有椭圆形标记k和l，表明它们的功能进一步展开在另外的N-S图（即图8-20(b)与图8-20(c)）中。

#figure(
  image("img/fig_8_20.png", width: 75%),
  caption: [N-S图的扩展表示],
) <fig-8-20>

=== PAD

PAD是Problem Analysis Diagram的缩写，系日本日立公司提出由程序流程图演化而来，且用结构化程序设计思想表现程序逻辑结构的图形工具，现在已为ISO认可。

PAD也设置五种基本控制结构的图式，并允许递归使用，如图8-21所示。

#figure(
  image("img/fig_8_21.png", width: 75%),
  caption: [PAD的基本控制结构],
) <fig-8-21>

作为PAD应用的实例，图8-22给出图8-17程序的PAD表示。PAD所描述程序的层次关系表现在纵线上，每条纵线表示一个层次，把PAD从左到右展开。随着程序层次的增加，PAD逐渐向右展开。

#figure(
  image("img/fig_8_22.png", width: 85%),
  caption: [PAD实例],
) <fig-8-22>

PAD的执行顺序从最左主干线的上端的结点开始，自上而下依次执行。每遇到判断或循环，就自左而右进入下一层，从表示下一层的纵线上端开始执行，直到该纵线下端，再返回上一层的纵线的转入处。如此继续，直到执行到主干线的下端为止。

=== 判定表

当算法包含多重嵌套的条件选择时，用程序流程图、N-S图或PAD都不易清楚地描述。然而，判定表却能清晰地表达复杂的条件组合与应执行动作之间的对应关系。仍然使用图8-17进行说明。为了能适应判定表条件取值只能是T和F的情形，对原图稍加改动，把多分支判断改为两分支判断，但整个图逻辑没有改变，见图8-23。

#figure(
  image("img/fig_8_23.png", width: 60%),
  caption: [不包含多分支结构的流程图实例],
) <fig-8-23>

与图8-23表示的流程图对应的判定表如图8-24所示。在表的右上半部分列出所有条件，T表示该条件取值为"真"，F表示该条件取值为"假"，"空白"表示这个条件无论取何值对动作不产生影响。在判定表右下半部分中列出所有的处理结果，Y表示执行这个动作，"空白"表示不执行这个动作。判定表右半部的每一列实质上是一条规则，规定与特定条件取值组合相对应的动作。

#figure(
  image("img/fig_8_24.png", width: 90%),
  caption: [反映程序逻辑的判定表],
) <fig-8-24>

判定表的优点是能够简洁、无歧义地描述所有的处理规则。但判定表表示静态逻辑，是在某种条件取值组合情况下可能的结果，不能表达加工的顺序，也不能表达循环结构，因此判定表不能成为一种通用的设计工具。

=== PDL

PDL（Program Design Language）是一种用于描述功能模块的算法设计和加工细节的语言，称为程序设计用语言，是一种伪码。一般而言，伪码的语法规则分为外语法和内语法。外语法应当符合一般程序设计语言常用语句的语法规则；内语法可以用英语中一些简单的句子、短语和通用的数学符号来描述程序应执行的功能。

PDL具有严格的关键字外语法，用于定义控制结构和数据结构，同时表示实际操作和条件的内语法又是灵活自由的，可使用自然语言的词汇。下面举一个例子，它是一个具有查找拼写错误单词功能的算法：

```text
PROCEDURE spellcheck IS                          //查找错拼的单词
BEGIN
  split document into single words               //把整个文档分离成单词
  look up words in dictionary                     //在字典中查这些单词
  display words which are not in dictionary       //显示字典中查不到的单词
  create a new dictionary                         //造一新字典
END spellcheck
```

从上例可以看到，PDL具有正文格式，很像一个高级语言，可以很方便地使用计算机完成PDL的书写和编辑工作。

PDL作为一种用于描述程序逻辑设计的语言，具有以下特点：

- 有固定的关键字外语法，提供全部结构化控制结构、数据说明和模块特征。属于外语法的关键字是有限的词汇集，它们能对PDL正文进行结构分割，使之变得易于理解。为了区别关键字，规定关键字一律大写，其他单词一律小写。
- 内语法使用自然语言来描述处理特性。内语法比较灵活，写清楚即可，不必考虑语法错，以利于人们可把主要精力放在描述算法的逻辑上。
- 有数据说明机制，包括简单（如标量和数组）的与复杂（如链表和层次结构）的数据结构。
- 有子程序定义与调用机制，用以表达各种方式的接口说明。

使用PDL，可以逐步求精：从比较概括和抽象的PDL程序开始，逐步写出更详细、更精确的描述。下面给出上面例子中该功能实现的四个步骤如何实现的细化过程：

```text
PROCEDURE spell-check
BEGIN
  //split document into single words
  LOOP get next word
    add word to word list in sort-order
    EXIT WHEN all words processed
  END LOOP
  //look up words in dictionary
  LOOP get word from word list
    IF word not in dictionary THEN
      //display words not in dictionary
      display word, prompt on user terminal
      IF user response says word OK THEN
        add word to good word list
      ELSE
        add word to bad word list
      ENDIF
    ENDIF
    EXIT WHEN all words processed
  END LOOP
  //create a new words dictionary
  dictionary := merge dictionary and good word list
END spell-check
```

== 界面设计的原则

软件界面不仅是软件系统功能体现最直接的表现方式，也是验证用户需求与功能实现是否匹配的一种有效方式。尤其是在应用和迭代方式的开发模式中，软件开发人员可以利用自身的专业知识在很短的时间内将用户不完整、甚至片面的需求用界面的方式将系统功能体现出来，以达到补充、修改和统一对用户需求的理解和认识。

界面设计主要包括三个方面：（1）设计软件构件间的接口；（2）设计模块和其他非人的信息生产者和消费者（比如其他外部实体）的接口；（3）设计人（如用户）和计算机间的界面。在此，只着重于第三种界面设计范畴——用户界面设计。

Ben Shneiderman在其关于用户界面设计的经典著作的前言中指出：

#quote(block: true)[对于许多计算机化的信息系统的用户来说，挫折和焦虑是他们日常生活的一部分。他们努力地学习命令语言和菜单选择系统，这些应该可以帮助他们更好地完成工作。有些人甚至对计算机、终端和网络产生紧张和恐惧，因而刻意地回避使用计算机化的系统。]

为什么软件界面如此复杂，使得用户难以理解和掌握呢？原因很简单，软件界面不仅应能充分体现系统的功能，同时还应兼顾用户使用的习惯（键盘，还是鼠标）和知识水准（一般用户，还是具有高水平计算机能力的人员），甚至还应美观大方和灵巧实用。不仅如此，软件的界面还应紧跟潮流。

因此，在进行用户界面设计的同时不仅应在技术问题上研究，同时还应对使用系统的用户加以研究。用户是什么样的人？用户怎样学习与新的基于计算机的系统交互？用户怎样解释系统产生的信息？用户对系统有哪些期望？这些问题仅是在用户界面设计时必须询问和回答的问题的一部分。

Theo Mandel在其关于界面设计的著作中提出三条黄金规则：

#par(first-line-indent: 0em)[（1）置用户于控制之下；]
#par(first-line-indent: 0em)[（2）减少用户的记忆负担；]
#par(first-line-indent: 0em)[（3）保持界面一致。]

这些黄金规则实际上形成了用于指导该项重要软件设计活动的一组用户界面设计原则的基础。

=== 置用户于控制之下

在使用软件的过程中，使用者通常根据自身的使用习惯或者业务背景提出一些不切实际的要求，希望能够由用户来控制系统的操作和运行，而非由系统所规定的功能模式来完成用户所必须完成的工作。

与此同时，软件开发人员为了能够尽可能快地实现系统，在很多情况下给用户界面强加或者忽视一些必需的约束条件而简化系统的交互模式。其结果是系统在很短的时间内实现，但在使用过程中却给用户带来不少的麻烦。

Mandel定义一组设计原则，以允许用户操作控制：

#par(first-line-indent: 0em)[（1）以不强迫用户进入不必要或不希望的动作方式来定义交互模式。一种交互模式是界面的当前状态。例如，如果在文字处理菜单中选择"拼写检查"，则软件转移到拼写检查模式。如果用户希望在这种情形下进行一些文本编辑工作，则没有理由强迫用户停留在拼写检查模式，用户应该能够几乎不需任何动作就进入和退出该模式。]

#par(first-line-indent: 0em)[（2）提供灵活的交互。不同的用户有不同的交互偏好，应该提供选择。例如，软件可能允许用户交互通过键盘命令、鼠标移动、数字化笔或语音识别命令等方式进行。]

#par(first-line-indent: 0em)[（3）允许用户交互可以被中断和撤销。即使陷入一系列动作中时，用户也应该能够中断动作序列处理某些其他事情（而不会失去已经处理过的工作）。用户也应该能够撤销任何动作。]

#par(first-line-indent: 0em)[（4）技能级别增长时可以使交互流水化并允许定制交互。用户经常发现他们重复地完成相同的交互序列。因此，值得设计一种"宏"机制，使得高级用户能够定制界面以方便交互。]

#par(first-line-indent: 0em)[（5）使用户隔离内部技术细节。用户界面应该使用户进入应用的虚拟世界。用户不应该知道操作系统、文件管理功能或其他秘密的计算技术。用户界面本质上应该绝不需要用户在位于机器内部的层次上进行交互（如用户应该永远无须在应用软件中输入操作系统命令）。]

#par(first-line-indent: 0em)[（6）设计原则应允许用户与出现在屏幕上的对象直接交互。当用户能够操作完成某任务所必需的对象，而且操作的方式类似于如果该对象是一物理存在时所发生的情况，则用户有一种控制感。例如，某应用界面可允许用户"拉伸"某对象（增大其尺寸）即是直接操作的一种实现，所谓"所见即所得"的效果。]

=== 减少用户的记忆负担

用户必须记住的命令越多，与系统交互时出错的概率越大。为此，一个很好的用户界面设计不应加重用户的记忆负担。只要可能，系统应该"记住"有关的信息，并通过能够帮助回忆的交互场景来辅助用户，比如"前进"、"后退"以及"撤销"等功能按钮。

Mandel定义一组设计原则，使得界面能够减少用户的记忆负担：

#par(first-line-indent: 0em)[（1）减少对短期记忆的要求。当用户涉入复杂的任务时，短期记忆要求很高。界面的设计应该设法减少记住过去的动作和结果的需求。可行的解决办法是提供可视的提示信息，使得用户能够识别过去的动作，而不是必须记住它们。]

#par(first-line-indent: 0em)[（2）建立有意义的默认值。初始的默认集合应该对一般的用户有意义，但用户应该能够了解个人的偏好。然而，一个"reset重置"选项应该是可用的，使得用户可以很容易回到系统参数的初始默认值。]

#par(first-line-indent: 0em)[（3）定义直觉的捷径。使用快捷键来完成系统功能时（如用Ctrl+C激活"复制"命令），快捷键应该以容易记忆的方式被联系到相关动作（如使用被激活任务的第一个字母）。]

#par(first-line-indent: 0em)[（4）界面的视觉布局应该基于真实世界的背景。例如，一个账单支付系统应该使用支票本登记流程来指导用户的账单支付过程。这使得用户能够依赖已经很好理解的可视化流程提示信息，而不是记住复杂难懂的交互序列。]

#par(first-line-indent: 0em)[（5）以不断进展的方式提示信息。界面应该层次式地组织，即关于某任务对象或某行为的信息应该首先在高抽象层次展示。更多的细节应该在用户用鼠标单击表明兴趣后再展示。一个对很多文字处理应用常见的例子是加下划线的功能，该功能本身是文本风格菜单下的一系列功能之一。然而，某种加下划线的能力并未列出。用户必须选择"加下划线"，然后所有"加下划线"选项（例如加单下划线、加双下划线、加虚下划线）才被展示出来。]

=== 保持界面一致性

用户应该以一致的方式展示和获取信息，这表明：（1）所有可视信息的组织均按照贯穿所有屏幕显示所保持的设计标准；（2）输入机制被约束到有限的集合，在整个应用中一致地使用；（3）从任务到任务的导航机制一致地定义和实现。

Mandel定义一组帮助保持界面一致的设计原则：

#par(first-line-indent: 0em)[（1）允许用户将当前任务放入有意义的环境中。很多界面使用数十个屏幕图像来实现复杂的交互层次。提供指示器（如窗口题目、图形图符、一致的颜色编码）以使得用户能够知道目前工作的环境是非常重要的。此外，用户应该能够确定他来自何处，以及到某新任务的变迁存在什么选择。]

#par(first-line-indent: 0em)[（2）在应用系列内保持一致。一组应用（或产品）应该统一实现相同的设计规则，使得可以保持所有交互一致。如果过去的交互模型已经建立用户期望，则不改变，除非有合理的理由。一个特殊的交互序列一旦变成一个事实上的标准（如使用Alt+S来存储文件），则用户在后期的每个应用中都使用这个快捷键表示的功能；如果交互序列在不同的环境下具有其他含义（如使用Alt+S来激活缩放比例），则导致用户使用时应用系列不一致。]

=== 界面设计模型

设计用户界面时须考虑四种模型：软件工程师创建设计模型；系统分析人员（或软件工程师）建立用户模型；终端用户在脑海里对界面产生的映像，称为用户的模型或系统感觉；系统的实现者创建系统界面模型。但是，这四种模型可能相去甚远，界面设计人员的任务就是消除这些差距，导出一致的界面表示。

整个系统的设计模型包括对软件的数据、体系结构、界面和过程的表示，需求规约可以建立一定的约束条件来帮助定义系统的用户，界面的设计往往是设计模型的附带结果。

用户模型描述了系统终端用户的特点。为了建立有效的用户界面，开始界面设计之前，必须对用户加以了解，包括年龄、性别、身体状况、教育程度、工作性质、工作环境、文化和种族背景、动机、目的及性格。此外，用户可以分类为：

#par(first-line-indent: 0em)[（1）新手。对系统没有任何语法了解，对该应用程序或计算机的一般用法几乎没有任何基本概念。]

#par(first-line-indent: 0em)[（2）对系统有了解的中级用户。对该应用程序有一定合理的知识背景，但对使用界面所必需的语法信息了解得还比较少。]

#par(first-line-indent: 0em)[（3）对系统有了解的经验用户。对该应用程序的语义和语法了解得较好（这经常导致"用户强迫综合症"），这些用户经常寻找捷径和简短的交互模式。]

=== 用户界面设计过程

用户界面的设计过程是迭代的，可以应用螺旋模型表示。用户界面设计过程包含4个不同的框架活动：

#par(first-line-indent: 0em)[（1）用户、任务和环境分析及建模。]

#par(first-line-indent: 0em)[（2）界面设计。]

#par(first-line-indent: 0em)[（3）界面构造。]

#par(first-line-indent: 0em)[（4）界面确认。]

用户环境的分析着重于实际工作环境，须了解的问题有：

#par(first-line-indent: 0em)[（1）界面将物理地位于何处？]

#par(first-line-indent: 0em)[（2）用户是否将坐着、站着或完成其他和该界面无关的任务？]

#par(first-line-indent: 0em)[（3）显示设备是否适应空间、光线或噪音的约束条件？]

#par(first-line-indent: 0em)[（4）是否存在特殊的环境因素而需特殊考虑？]

作为分析活动的一部分而收集的信息用于创建界面的分析模型。使用该模型作为基础开始设计活动。界面设计的目标是定义一组界面对象和动作（以及它们的屏幕表示），它们使得用户能够使用这些既定的界面操作流程来完成用户所需要的工作。

"界面构造"主要根据已经确定的用户模型和设计模型开发界面的初始原型，并提供给用户使用，进而经过多次迭代，最终在用户满意的条件下完成界面的开发任务。

"界面确认"着重于：（1）界面功能具有正确地实现每个用户任务的能力，适应所有任务变更的能力及达到所有一般用户需求的能力；（2）界面具有良好的使用和学习的功能；（3）用户具有接受界面作为其工作中有用工具的功能。

=== 界面设计须注意的常见问题

在进行用户界面设计时，通常遇到以下四种问题：系统响应时间、帮助信息、错误信息处理和命令标记。但是，许多设计人员往往在用户界面设计后期才注意这些问题（有时在操作原型已经建立之后才考虑问题），这往往导致界面设计不断反复、项目延期和用户不满意，最好的办法是在设计的初期将这些作为设计问题加以考虑，因为此时修改比较容易，代价也低。

系统响应时间是交互式系统中用户经常有意见的地方。一般来说，系统响应时间是指从用户开始执行动作（比如按Enter键和单击鼠标）到软件给出预期的响应。系统响应时间包括两方面的属性：长度和变化。如果系统响应时间过长，用户显然不希望长时间等待；过快的系统响应时间有时也成为问题，因为这迫使用户加快操作节奏，从而导致错误。

系统响应时间的变化是指相对于平均响应时间的偏差，这往往更重要。即使响应时间比较长，低的响应时间变化也有助于用户建立稳定的节奏，例如，稳定在1秒的响应时间比从0.1秒到2.5秒不定的响应时间好。

在多数情况下，现代的软件系统都提供联机帮助，用户可以不离开系统而寻找到所需要的答案。除此之外，还应该提供一份详细的用户使用手册，注明每一个界面功能的操作细节和顺序。为此，在设计界面的帮助信息时应该考虑如下内容：

#par(first-line-indent: 0em)[（1）在进行系统交互时，是否总能得到各种系统功能的帮助？有两种选项：提供部分功能的帮助和提供全部功能的帮助。]

#par(first-line-indent: 0em)[（2）用户怎样请求帮助？有三种选项：帮助菜单、特殊功能提示和HELP命令。]

#par(first-line-indent: 0em)[（3）怎样表示帮助？有三种选项：在另一个窗口中，指出参考某个文档（非理想方式）和在屏幕特定位置的简单提示。]

#par(first-line-indent: 0em)[（4）用户怎样回到正常的交互方式？有两个选项：屏幕上显示"返回"按钮、功能键或控制序列。]

#par(first-line-indent: 0em)[（5）怎样构造帮助信息？有三种选项：平面式（所有信息均通过一个关键词来访问）、分层式（用户可以进一步查询得到更详细的信息）和超文本式。]

出错消息和警告是指出现问题时系统给出的"坏消息"。如果处理得不好，出错消息和警告给出无用或误导的信息，从而增加用户的反感程度。许多用户遇到下面这种形式的出错消息：

#quote(block: true)[SEVERE SYSTEM FAILURE — 14A]

应该在某处有对错误14A的解释，否则设计者为什么指出14A呢？但出错消息并没有指出是什么错误或从何处可以找到进一步的信息。类似上面的出错消息既不能使用户满意，也不能解决问题。

通常，交互式系统给出的出错消息和警告应具备以下特征：

#par(first-line-indent: 0em)[（1）消息以用户可以理解的术语描述；]

#par(first-line-indent: 0em)[（2）消息应提供如何从错误中恢复的意见；]

#par(first-line-indent: 0em)[（3）消息应指出错误可能导致哪些不良后果（比如破坏数据），以使用户检查是否出现这些情况或帮助用户进行改正；]

#par(first-line-indent: 0em)[（4）消息应伴随视觉或听觉上的提示信息，即显示消息时应伴随警告或者消息用闪烁方式，或明显的颜色进行提示；]

#par(first-line-indent: 0em)[（5）消息不能带有判定色彩，即不能指责用户操作不当，因为错误信息都是系统的问题而非用户造成的。]

命令行曾经是用户和系统交互的主要方式，广泛用于各种应用程序中。现在，面向窗口采用点击和拾取方式的界面使用户对命令行不再依赖，但许多高级用户仍然喜欢面向命令的交互方式。在提供命令交互方式时，必须考虑以下问题：

#par(first-line-indent: 0em)[（1）每一个菜单选项是否都有对应命令？]

#par(first-line-indent: 0em)[（2）以何种方式提供命令？有三种选项：控制序列（比如Alt+P）、功能键和输入命令。]

#par(first-line-indent: 0em)[（3）学习和记忆命令的难度有多大？命令忘了怎么办？]

#par(first-line-indent: 0em)[（4）用户是否可以定制和缩写命令？]

=== 界面设计评估和优化

一旦建立操作用户界面原型，必须对其进行评估，以确定是否满足用户的需求。评估方法可以从非正式的测试驱动（比如用户可以临时提供一些反馈）到正式的设计研究（比如按照统计学的方法向一定数量的用户发放评估问题表）。

用户界面评估的周期如图8-25所示。完成初步设计后开始建立第一级的原型；用户对该原型进行评估，直接向设计者提供有关界面功效的建议。如果采用正式的评估技术（比如问题表、排序单），设计者须从调查结果中得到需要的信息（比如80%的用户不喜欢其中保存数据文件的机制）；针对用户的意见对所设计的界面进行修改，完成下一级的原型。评估过程不断进行，直到无须再修改为止。

#figure(
  image("img/fig_8_25.png", width: 70%),
  caption: [界面设计评估周期],
) <fig-8-25>

原型完成以后，设计者可以通过对用户收集反馈信息得到一些定性和定量的数据帮助进行界面评估。如果需要定量的数据，就必须进行某种形式的定时研究分析，观察用户对界面交互的使用情况，记录以下信息：在标准时间内正确完成任务的数量、使用命令的频度、命令序列、用于看屏幕的时间、出错的数目、错误的类型和错误恢复时间、使用帮助的时间、标准时间段内查看帮助的次数，这些数据可以用于指导界面修改。

== 习题

1. 说明如何应用数据流图推导出初始的系统功能结构图。

2. 说明区分变换型和事务型功能结构图的要点，并举例说明。

3. 结合"医院就诊管理系统"中的"选择科室和医生"加工，试确定相应的中心变换模块、传入模块和传出模块，并进一步给出相应的功能结构图。

4. 说明概要设计后处理包含哪几个部分。

5. 试分别使用程序流程图和PDL给出"选择科室和医生"模块的详细设计方案。
