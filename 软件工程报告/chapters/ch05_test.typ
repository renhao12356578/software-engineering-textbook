#set heading(numbering: "1.1")
#show heading: it => {
  let level = it.level
  if level == 1 {
    v(20pt)
    align(center)[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 18pt, weight: "bold"); #it]
    v(15pt)
  } else if level == 2 {
    v(12pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 14pt, weight: "bold"); #it]
    v(6pt)
  } else {
    v(8pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 12pt, weight: "bold"); #it]
    v(4pt)
  }
}

#set heading(numbering: "1.1")

#show heading: it => {
  let level = it.level
  let body = it.body
  if level == 1 {
    v(20pt)
    align(center)[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 18pt, weight: "bold"); #it]
    v(15pt)
  } else if level == 2 {
    v(12pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 14pt, weight: "bold"); #it]
    v(6pt)
  } else {
    v(8pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 12pt, weight: "bold"); #it]
    v(4pt)
  }
}

// 第5章 结构化需求分析方法

== 结构化分析发展简史

分析建模的早期工作开始于20世纪60年代后期和20世纪70年代初期，但结构化分析方法的第一次出现是作为另一个重要课题——结构化设计的附属品。研究人员需要一种图形符号体系来表示数据和对数据进行变换，这些处理最终被映射到软件体系结构的设计中。

「结构化分析」最初由Douglas Ross提出，由De Marco进行推广。在关于这个主题的书中，De Marco引入并命名使得分析员可以创建信息流模型的关键图形符号，提出使用这些符号的模型。在之后的几年中，Page-Jones、Gane和Sarson等提出结构化分析方法的一些变种，在这些变种中，结构化方法关注于信息系统的应用，而没有提供足够的符号来表示实时工程问题中的控制和行为方面。

在20世纪80年代中期，Ward和Mellor，以及后来的Hatley和Pirbhai引入实时「扩展」，这些「扩展」有效地加强结构化的分析方法。人们还努力开发一致符号体系，以及适应CASE工具使用的现代处理方法。

== 面向数据流图的结构化分析模型

软件需求分析的分析模型必须达到3个主要目标：（1）描述客户的需求；（2）建立软件设计的基础；（3）定义在软件完成后可以被确认的一组需求。为了达到这些目标，在结构化需求分析中得到的分析模型可以描述成图5-1的结构。

// #figure(
//   image("img/fig_5_1.png", width: 50%),
//   caption: [基于结构化的需求分析模型],
// )

结构化需求分析模型的核心是数据字典。围绕数据字典有3种不同的图来描述需求的3种视图：

（1）*实体关系（Entity Relationship, ER）图*：描述数据对象间的关系，ER用来表示数据建模活动，在ER中出现的每个数据对象的属性可以使用数据对象描述来表达。

（2）*数据流图（Data Flow Diagram, DFD）*服务于两个目的：（1）指明数据在系统各功能间中移动时的变换过程；（2）描述对数据流进行变换的功能和子功能。DFD提供充分的数据流及其附加的信息，可以用于信息域的分析，并作为功能建模的基础。DFD出现的每个功能描述包含在加工规约（Process Specification, PS）中。

（3）*状态迁移图（State Transition Diagram, STD）*用于表示某个功能单位（系统级、子系统级、模块级、进程级或者线程级，甚至某个设备）接收外部事件后，该功能单位状态的变化及相应的后续动作。为此，STD可以用于表示系统的各种行为模式（称为状态）及在状态间进行变迁的方式。STD是行为建模的基础。关于软件控制方面的附加信息包含在控制规约（Control Specification, CS）中。

== 数据建模

软件系统本质上是信息处理系统，因此，软件系统的整个开发过程都必须考虑两方面的问题，即数据及对数据的处理。在需求分析阶段则既分析用户的数据要求（即有哪些数据、数据之间有什么联系、数据本身有什么性质、数据结构等），又分析用户的处理要求（即对数据进行哪些处理、每个处理的逻辑功能等）。

数据建模的目标是为了把用户对业务数据的要求清晰明确地表达出来。数据建模的基础条件是当前系统中物理和逻辑模型中总结出来的业务知识、业务流程及业务数据，软件开发人员须将其进行分析并抽象表达成「目标系统」的逻辑数据模型，或者概念数据模型（也称为信息模型）。概念模型是一种面向问题的数据模型，是按照用户的观点来对数据和信息建模。它描述从用户角度看到和使用的数据，反映用户的现实环境需要临时或者持久保存的数据。

最常用表示概念数据模型的方法是实体关系方法（Entity Relationship Approach）。这种方法用实体关系图（ER图）描述现实世界中的实体，而不涉及这些实体在软件系统中的表示和实现方法。用这种方法表示的概念数据模型又称为ER模型。数据建模反映与任何数据处理应用相关的一组特定问题：

（1）系统处理哪些主要的数据对象？

（2）每个数据对象的组成如何？

（3）哪些属性描述这些数据对象？

（4）这些数据对象当前位于何处？

（5）每个数据对象与其他数据对象有哪些关系？

（6）数据对象和变换它们的处理之间有哪些关系？

数据对象可能是一个外部实体（如生产或消费信息的任何事物）、一个事物（如报告或显示）、一个角色（如销售人员）、一个组织单位（如统计部门）、一个地点（如仓库）或一个结构（如文件）。例如，人或车可以被认为是数据对象，因为它们可以用一组属性来定义。数据对象描述包括数据对象及其所有属性。通常将数据对象简称为实体。

数据对象是相互关联的。例如，人可以拥有车，「拥有」关系表示人和车之间的一种特定的连接。关系是由被分析问题的语境定义的。数据对象只封装数据，数据对象没有指向作用于数据的操作引用。

因此，数据对象可以表示为如图5-2所示的一张表，表头反映对象的属性。在这个例子中，车是通过厂家、型号、车牌号、车型、颜色和拥有者定义的。该表的内容表示数据对象的特定实例。

// #figure(
//   image("img/fig_5_2.png", width: 75%),
//   caption: [数据对象的表格表示],
// )

（1）*属性*。属性定义数据对象的性质，可以具有3种不同的特性，可以用来：（1）为数据对象的实例命名；（2）描述这个实例；（3）引用另一个表中的另一个实例。另外，一个或多个属性应被定义为标识符，即当须找到数据对象的一个实例时，标识符属性成为一个关键字。在有些情况下，标识符的值是唯一的，尽管这不是必需的。在数据对象「车」的实例中，车牌号可以是一个合理的标识符。

（2）*关系*。客观世界中的事物彼此间通常是以某种方式进行关联的。例如，教师与课程之间存在「教课」这种联系，而学生与课程之间存在「上课」或者「听课」这种联系。客观世界中的事物在软件系统中通常以数据对象来表示，数据对象之间相互连接的方式称为关系。

数据建模的基本元素（数据对象、属性和关系）提供理解问题信息域的基础，然而，与这些基本元素相关的其他元素也必须理解。已经定义一组对象并表示将它们绑定的对象/关系，但是实体X与实体Y相关并没有为软件工程的目的提供足够的信息，还必须理解实体X的出现次数与实体Y的出现次数有何必然的联系，从而引出称为「基数」（Cardinality）的数据建模概念。

（3）*基数*。数据模型必须能够表示在一个给定的关系中实体出现的次数，即对象-关系对的基数。基数是关于一个（实体）可以与另一个（实体）出现次数相关联的规约。基数通常简单地表达为「一」或「多」。考虑到「一」和「多」的所有组合，两个（实体）可能的「关联」如下所述。

- *一对一（1:1）*：（实体）A的一次出现可以并且只能关联到（实体）B的一次出现，B的一次出现只能关联到A的一次出现。例如，一个人只能有一个身份证号码，同时一个身份证号码只能对应一个人。

- *一对多（1:N）*：（实体）A的一次出现可以关联到（实体）B的零次、一次或多次出现，但B的一次出现只能关联到A的一次出现。例如，一个母亲可以有多个孩子，但一个孩子只能有一个母亲。

- *多对多（M:N）*：（实体）A的一次出现可以关联到（实体）B的一次或多次出现，同时B的一次出现也可以关联到A的一次或多次出现。例如，一个叔叔可以有多个侄子，一个侄子可以有多个叔叔。

（4）*实体关系图（ER图）*。ER图的主要目的是以图形的形式表示实体及实体之间的关系。ER图最初是由Peter Chen为关系数据库系统的设计提出的，并进行扩展。ER图标识一组基本的构件：实体、属性、关系。带标记（或名称）的矩形表示实体，连接实体的线表示关系。实体和实体之间关系的符号表示如图5-3所示。

// #figure(
//   image("img/fig_5_3.png", width: 75%),
//   caption: [实体关系表示符号],
// )

下面的示例展示一个简单的教学管理系统中数据模型的分析和创建过程。

（1）*分析并确定实体*：需理解的概念，或者需长期保存的数据对象。

在教学管理中，学校针对每个学期设置若干门课程，一位教师可以承担一门或者多门课程的教学任务，也可以没有教学任务；同时，学校的学生须选择课程进行学习（课程可分为必修课及一些选修课程）。

通过业务理解和分析，可以发现这个教学管理主要涉及教师、学生和课程这三个实体。

（2）在确定3个业务实体后，就需要对其教学管理中的一些细节进行分析，并确立这些实体之间的关系：

- 一名教师本学期没有教学任务，不授课；
- 一名教师本学期有一门课程的教学任务；
- 一名教师本学期有多门课程的教学任务；
- 一门课程一旦设置，必须有一位教师负责授课；
- 一名学生本学期须选择至少一门课程学习；
- 一名学生本学期可以选择多门课程学习；
- 一门课程可以同时被多名学生选择学习。

（3）*确定实体属性*：属性用于描述实体的一些本质特征。

- 学生：学号、姓名、性别、年龄、专业等；
- 课程：课程号、课程名、学分、学时数、上课时间、上课教室等；
- 教师：职工号、姓名、年龄、职称、单位等。

（4）*建立实体关系图*，如图5-4所示。

// #figure(
//   image("img/fig_5_4.png", width: 75%),
//   caption: [教学管理系统数据模型],
// )

== 数据结构规范化

规范化的目的是消除数据模型中不必要的数据冗余，即消除实体表中每一条记录中可能重复的数据，进一步消除实体属性的多义性，使关系中的属性含义清楚、单一，使关系单纯，让每个属性即数据项只是简单的数或字符串，方便操作。数据的插入、删除与修改操作可行且方便；使关系模式更灵活，易于实现接近自然语言的查询方式。

关系规范化的程度通常按属性间的依赖程度来区分，并以范式（Normal Form, NF）来表达。构造数据库时必须遵循一定的规则，在关系数据库中，这种规则就是范式。范式是符合某一种级别关系模式的集合。关系数据库中的关系必须满足一定的要求，即满足不同的范式。目前，关系数据库有6种范式：第一范式（1NF）、第二范式（2NF）、第三范式（3NF）、第四范式（4NF）、第五范式（5NF）和第六范式（6NF）。一般说来，数据库只需满足第三范式（3NF）就可以达到设计的要求。

通常，第一范式的数据模型最简单，但是数据冗余程度最大；第六范式的数据冗余最小，但是数据库表太多，表之间的关系不仅多而且限制条件也很苛刻，不容易进行数据模型改动。

数据模型的范式级别越高，存储同样数据须分解成越多张表，因此存储自身的过程也越复杂。随着范式级别提高，数据的存储结构与基于问题域的结构间匹配程度也随之下降，为此在需求发生变化时数据的稳定性随之下降。除此之外，范式级别高，则须访问的表增多，因此性能或处理速度将下降。这就是大多数的情况下实用的系统通常选用第三范式的原因。

*第一范式（1NF）*关系的所有属性都是单纯域，即不出现表中有表的情况。

实体中的某个属性不能有多个值或者不能有重复的属性。如果出现重复的属性，就可能需要定义一个新的实体，新的实体由重复的属性构成，新实体与原实体之间为一对多的关系。在第一范式（1NF）中，表的每一行只需包含一个实例的信息。简而言之，第一范式就是无重复的列。

*第二范式（2NF）*非主属性完全函数依赖关键字。

第二范式是在第一范式的基础上建立起来的，即满足第二范式（2NF）必须先满足第一范式（1NF）。第二范式（2NF）要求数据库表中的每个实例或行必须可以唯一区分。为此须为表加上一个列，以存储各个实例的唯一标识。这个唯一属性列被称为主关键字或主键、主码。

第二范式（2NF）要求实体的属性完全依赖主关键字。所谓「完全依赖」是指不能存在仅依赖主关键字一部分的属性，如果存在，那么这个属性和主关键字的这一部分应该分离出来，形成一个新的实体，新实体与原实体之间是一对多的关系。简而言之，第二范式就是非主属性部分依赖主关键字。

*第三范式（3NF）*非主属性相互独立，即任何非主属性间不存在函数依赖。

满足第三范式必须先满足第二范式。简而言之，第三范式的属性不依赖其他非主属性。

== 数据流图

结构化分析是作为信息流建模技术而产生的。当数据或信息流过计算机系统时被系统的功能所处理、加工或变换后再将处理或变换后的数据从系统输出。基于结构化的计算机系统可以表示为如图5-5所示的数据流图形式，该结构称为目标系统的结构化功能模型。

// #figure(
//   image("img/fig_5_5.png", width: 75%),
//   caption: [数据流图的基本结构],
// )

=== 数据流图的结构

当信息或数据在系统中移动时，被一系列软件功能处理和变换。数据流图（DFD）或称为泡泡图（Bubble Chart），是描述信息流和数据从输入移动到输出时被系统的功能变换的图形化技术。

数据流图可以用来抽象地表示系统或软件，既能提供功能建模的机制，也可提供数据流建模的机制，并以自顶向下的机制表示层级的功能细节和数据变换细节。由数据流图可知，数据流图有4种基本元素，如图5-6所示。

// #figure(
//   image("img/fig_5_6.png", width: 60%),
//   caption: [数据流图的基本图形符号],
// )

*外部实体*：是产生并接收数据流的起点和终点，表示待处理数据的输入来源或处理结果送往何处。相对于待构建的软件系统，它是代表使用系统的角色或者设备等，故称外部实体。它是数据流产生的来源及是数据流结束的终点，也可以认为是功能需求的重要来源。

*加工*：「加工」的名称取自数据流图中该图形符号的英文名称：Process，说明是对数据流进行处理的功能单元。它接收外部实体或者其他加工传递过来的数据流，并对接收的数据进行加工处理。「加工」的名称通常以动词来命名，表示系统应该具有的功能。「加工」可以嵌套多级子加工，从最高级的系统级加工、子系统级加工、模块级加工，直到原子加工，即不可再分解的功能。

*数据流*：产生于外部实体，进入系统后在加工之间、加工和数据存储之间进行流动，并终止于外部实体的数据。数据流以具有方向的线条来表示，并在线条上加以命名以区分于其他的数据流，数据流的名称必须是具有明确含义的名词。特别提示，数据流不是控制流。举例说明，某人A对某人B说：「请帮我买一瓶矿泉水」和「请你开车去帮我买瓶矿泉水」的区别：第一句话中人A向B传达的是简单信息「矿泉水」，B接受信息后如何买，A没有明确提出要求，所以A和B之间传递的是数据流；第二句话除了要求矿泉水之外，还清楚地告知B必须「开车」，可以理解为A在向B传递信息的同时也传递一个控制命令，要求B以何种方式完成。

*数据存储*：在数据流图中起保存数据的作用，因而称为数据存储（Data Store）。它可以是数据库表、数据文件、临时的数据结构或任何形式的数据组织形式。指向文件的数据流可理解为写入或修改指令（控制流）中附带的数据信息，从文件中输出的数据流可理解为查询指令中从文件读取的数据信息。

=== 数据流与加工之间的关系

在数据流图中，如果两个以上数据流指向一个」加工」，或是从一个」加工」中输出两个以上的数据流，这些数据流之间往往存在一定的关系。图5-7给出所用符号及其含义。其中，星号（\*）表示相邻的一对数据流同时出现；（+）则表示相邻的两数据流只取其一。

// #figure(
//   image("img/fig_5_7.png", width: 70%),
//   caption: [数据流与加工之间关系的符号表示],
// )

=== 分层的数据流图

由于所分析问题的规模大，为了表达数据处理过程的完整情况，一个数据流图展示很多的复杂信息造成阅读不易。为此，可以采用层次化的数据流图反映实际问题的复杂结构关系。

按照自顶向下的分析思路，可以考虑将整个数据处理过程（即整个目标系统）暂且看成一个「加工」，它的输入数据和输出数据实际上反映目标系统与系统使用者之间的交互，这个「加工」就是顶层的数据流图。为了进一步分析系统内部的数据流转过程，以及和加工过程之间的关系，还须进一步将顶层加工进行分解和细化，这个分析过程使用的表示方法就是分层的数据流图。分层数据流图具有一个顶层流图、多个中间层流图和一个底层流图的结构。

- *顶层流图*仅包含一个「加工」，它代表被开发系统。它的输入流是该系统的输入数据，输出流是系统的输出数据。顶层流图的作用在于表明被开发系统的范围，以及它和周围环境的数据交换关系。

- *中间层数据流图*可以具有多层，根据具体情况来确定，对其上层父图细化。如果该层的某一个「加工」仍然具有多种功能，则该「加工」还可以继续细化，形成中间层子图。

- *底层流图*是指其「加工」无须再分解的数据流图，其「加工」称为「原子加工」。一般来讲，原子加工的应用有一个输入数据流和一个输出数据流的简单特征，但某些情况下也可能具有复杂特性，须视具体情况而定。

图5-8是一种抽象的层次化数据流图的例子。系统F具有一个输入数据A和一个输出数据B，表明一个系统顶层的数据流图。根据需求分析的结果，可以进而将系统下细化为具有f_1到f_5的加工子系统。此时必须保持进入系统的输入数据和输出数据一致，这个概念称为数据流图的「平衡」关系。

// #figure(
//   image("img/fig_5_8.png", width: 60%),
//   caption: [分层数据流图的一种表示],
// )

随后可以对加工f_1进一步细化，可以得到f_{11}到f_{15}的加工细节，同时保持f_1的输入数据X、Y，以及输出数据Z一致。

图5-9给出分层数据流图的另一种表示。L0表示顶层的数据流图，其中数据处理S包括3个子系统1、2、3。顶层下面的第一层数据流图为DFD/L1。第二层数据流图DFD/L2.1、DFD/L2.2及DFD/L2.3分别是子系统1、2和3细化的结果。对任何一层数据流图来说，它的上层图为父图，在它下一层的图则称为子图。

// #figure(
//   image("img/fig_5_9.png", width: 60%),
//   caption: [分层数据流图的另一种表示],
// )

=== 数据流图的绘制步骤

画数据流图的基本步骤是自外向内，自顶向下，逐层细化，完善求精。开始绘制数据流图的前提条件是，当前系统的物理模型和逻辑模型分析完成。再次强调，数据流图属于目标系统的功能模型。绘制数据流图的具体步骤可按如下顺序来完成：

（1）找出外部实体，它们是系统的数据源点与汇点，界定系统的分析范围。

（2）在图的边上画出系统的外部实体。

（3）找出外部实体的输入数据流与输出数据流。

（4）从外部实体的输出数据流（即系统的源点）出发，按照系统的逻辑需要，逐步画出一系列逻辑「加工」，直到找到外部实体所需的输入数据流（即系统的汇点），形成封闭的数据流。（特别提示，此时的加工完全是个人分析的结果，没有绝对意义上的对错）

（5）按照既定的原则进行检查和修改。

（6）按照上述步骤，再从各「加工」出发，画出所需的子图。

=== 数据流图的检查和修改原则

（1）数据流图上所有图形符号只限于前述4种基本图形元素。

（2）数据流图的主图必须包括前述4种基本元素，缺一不可。

（3）数据流图主图上的数据流必须封闭在外部实体之间，外部实体可以不止一个。

（4）每个「加工」至少有一个输入数据流和一个输出数据流。

（5）在数据流图中，按层给加工框编号。编号表明该加工处在哪一层，以及上下层父图与子图的对应关系。

（6）任何一个数据流子图必须与它上一层的一个「加工」对应，两者的输入数据流和输出数据流必须一致。此即父图与子图的平衡。它表明在细化过程中输入与输出不能丢失和强加。

（7）子图相对于父图，添加一些新的「加工」和新的数据流；如果发现有新的数据存储，更新上层的各级父图，直到顶层。

（8）图上每个元素都必须有名称。外部实体、数据流和数据存储都以名词命名，「加工」必须用动词命名。

（9）数据流图一般不可夹带控制流。因为数据流图是实际业务流程的客观表现，说明系统「做什么」而不是表明系统「如何做」，因此不是系统的执行顺序，不是程序流程图。

（10）初画时可以忽略琐碎的细节，以集中精力于主要数据流。

=== 具有控制流的数据流图

Ward和Mellor对实时系统的数据流图进行相应的扩展，除了原有的数据流，还引入控制流及连续的数据流等符号。这种「扩展」可以适应实时系统提出的以下要求：

（1）在时间连续的基础上接收或产生数据流；

（2）贯穿系统的控制信息和相关的控制处理；

（3）在多任务的情况下可能遇到同一个「加工」的多个实例；

（4）系统状态及导致系统状态迁移的机制。

图5-10给出的扩展图形符号可以让分析员在描述数据流和「加工」的同时，描述控制流和控制加工。这些符号可以与原来的数据流图图形符号混用。

// #figure(
//   image("img/fig_5_10.png", width: 60%),
//   caption: [Ward和Mellor开发的针对实时系统扩展的连续数据流符号],
// )

图5-11给出一个基于计算机水温控制系统的数据流处理过程。水温测量仪连续传送水温数据给温度监控加工模块，该「加工」将水温与允许波动范围进行比较，输出校正数据给温度调节装置。根据以下分析过程给出局部使用连续数据流表示的数据流图。

// #figure(
//   image("img/fig_5_11.png", width: 60%),
//   caption: [时间连续的数据流与普通数据流],
// )

（1）*外部实体*：首先，水温测量仪是终端设备，是该软件系统的数据输入源之一，所以它是系统的外部实体；其次，系统的温度测量范围需人工干预，系统管理员使用系统的功能也可以作为系统的一个外部实体；系统经过温度检测和调整后的温度输出到一个显示器上，该显示器也可以作为终端设备，为此也可以认为是该系统外部实体。

（2）*数据流*：水温测量仪定时产生连续的测量温度，并将该数据连续发送给系统，为此这个数据流适合使用连续的数据流来表示；人工干预的温度设置范围是一次或者是间断的数据，可以认为是普通的数据流；输出到显示器的数据由于取决于温度测量值的产生频率，为此也是连续的数据流。

图5-12给出一个制造车间数控设备的数据和控制流的数据流图示意，展示3个控制加工与普通数据流和控制流及控制存储之间的关系。

// #figure(
//   image("img/fig_5_12.png", width: 60%),
//   caption: [使用Ward和Mellor符号的数据流和控制流],
// )

这个例子的关键在于如何区分数据流和控制流。相对于「加工」而言，此处假定该「加工」是原子加工，一个数据流仅仅是该「加工」被激活所需的信息。换言之，没有这些信息该「加工」肯定不会被激活，但有这些信息该「加工」也不一定被激活，加工的激活取决于自身的状态。但是，如果是控制流，则该信息明确告知该「加工」是否激活，还是停止；如果该「加工」不是原子加工，则控制流告知该「加工」的哪个功能被激活。

关于多加工实例的应用，本书留给读者根据操作系统所学到的进程和线程深刻理解。除此之外，读者考虑如何使用多加工实例来表示一个服务能够处理多个客户端请求的表示。

=== 分层数据流图实例

下面通过一个简单的例子来具体展示如何从需求获取阶段得到一些文字信息，或者更加准确地说如何从《用户需求说明书》中描述的业务背景知识进行数据流图分析和构建。该示例以一所医院待构建一套电子化的医院就诊管理系统为基础，由于篇幅限制，仅以医院的4个部门：挂号、问诊、收费和取药进行简单的描述和讨论。这4个部门的基本业务流程描述如下。

（1）*挂号*：挂号处的挂号人员接受病人的就诊请求，根据门诊科室各医生病人的排队情况，分配合适的医生，记录并打印挂号凭据，收取挂号费，完成挂号请求。

（2）*问诊*：医生根据挂号的次序对病人进行病情诊断，根据挂号单据及病历号获取该病人的历史病历，然后将问诊结果记录在病历当中并开具相应的处方（可根据系统提供的药品进行选择），打印处方交给病人完成一次问诊。

（3）*收费*：收费员根据病人提交的处方所列出的药品种类和数量进行收费，之后打印收费清单。

（4）*取药*：药剂师根据盖章后的处方，进行核对并修改处方状态，将药品交付给病人。病人取药后离开医院，完成一次就医过程。

本例针对该系统进行顶层、第一层及只针对挂号部门进行第二层的数据流图分析和绘制。

（1）*顶层数据流图*。

① 分析要点：基于上述基本信息，确定外部实体及顶层流图中加工的名称。随后，根据上述基本信息及一些附加信息或者基本的业务知识，即当前系统的物理及逻辑模型的分析结果，进一步确认外部实体与加工之间的数据流；下面的分析过程内容是参考当前系统物理及逻辑模型的内容。最后确定数据存储与顶层加工之间的数据信息。须注意的是，顶层流图是系统的全貌，为了保持分层数据流图数据之间的平衡，要求顶层的数据存储是完整的。这给实际操作的人员带来不小的困难，如何在没有底层流图的情况下确定所有的数据存储信息？笔者认为，达到数据平衡的流图是最后的结果要求，而不是分析的开始和分析的过程，为此建议一开始分析时可以只有部分的数据存储，随着分析的深入，再进一步对顶层流图进行修改和添加，直到最终定稿。

② 外部实体的分析过程：*挂号处*。其主要功能是挂号人员完成对病人挂号的请求，并产生挂号单，完成一次挂号请求。此时，这里有两个参与挂号的主体：挂号人员和病人，哪一个才是外部实体？或者两个都是？

根据例子的基本信息，构建的是医院就诊管理系统，那么可以很清楚地得到一个结论，该系统是医院内部的系统，使用者只能是医务人员。结论已经很清楚，对于挂号处而言，外部实体只能是挂号人员。

假设本例子的题目改成如下内容「医院为了提高挂号效率，除了设置常备的挂号处之外，还会在医院大厅设置自助挂号机」。那么，自助挂号机是面向病人自助挂号的，为此，挂号处的外部实体应该有挂号人员和病人。

*问诊处*。其主要功能是方便医生对病人的排队叫号管理，并能快速调取病人病历，进而书写病历和处方，最后打印处方交给病人，完成一次问诊。根据以上信息，可以很清楚地得到一个结论，问诊处的外部实体是医生。

*交费处*。其主要功能是核对处方并收取费用，产生收费清单。收费员根据病人提交的处方号，查询并调取该处方后进行数量和金额核对，收取病人的费用并修改处方状态，打印收费清单并找零钱，完成一次收费。由此可以得到一个外部实体：收费人员。

在大医院实际的操作过程中，病人交费时须排长队，一旦系统处理速度慢或者操作人员不熟练，造成病人更长时间等待。从技术角度来讲，系统可以实现不排队叫号交费的功能，但是考虑到病人交费是一个不可控的因素，即病人可能拿处方不交钱取药直接离开，也有可能钱没带够或者其他原因不来交钱，造成很多无效处方。此处描述的目的是使读者可以额外考虑一些更加有效的方式。

*取药处*。其主要功能是快速根据处方配药，并将药品交给病人。药剂师根据病人提交的交费后盖章的处方，查询并调取处方信息，核对无误后修改处方状态，将药品交给病人。由此可以得到外部实体：药剂师。读者可以额外考虑，病人无须排队取药，药房可以利用大屏幕对病人进行叫号。

③ 加工的分析过程：顶层数据流图的加工数量只有一个，它代表待开发系统，根据题目的基本要求，该加工的名称可以定义为医院就诊管理系统。

④ 数据流的分析过程：数据流产生并终止于外部实体，并流动在加工和数据存储之间。为此，首先考虑外部实体和加工之间的信息和数据构成。按照例子的基本信息，还必须获取额外的信息或者分析人员必须分析的如下内容。

- 外部实体「挂号人员」与加工「医院就诊管理系统」之间的信息交互：两者之间产生挂号请求的信息，并最终产生挂号单。需分析的是，在接收「挂号请求」之后，挂号人员是否还需要其他的数据信息才可以产生挂号单？假定病人的挂号请求包含挂号人员所需要的所有必要信息，比如病历号、内科或者外科的科室信息、普通或者专家门诊等信息，那么挂号人员可以进行必要的后续操作，得到本次挂号的费用信息并告知病人交费，挂号人员收取费用后输入交费信息，确认后系统产生本次挂号单并交给病人，完成一次挂号过程。由此可以得到外部实体「挂号人员」与顶层数据流图加工「医院就诊管理系统」之间的数据流分析结果：挂号请求、挂号费用、挂号单。另外，挂号人员如何根据挂号请求的信息查询病人所需科室各医生当前的排队人数，读者自行分析。

- 外部实体「医生」与加工「医院就诊管理系统」之间的信息交互：医生从排队的病人中叫号开始，到书写新病历信息和处方信息，完成一次问诊过程中「医生」与「加工」之间产生多少数据流？医生叫号可以理解为一个功能，该功能向临时数据文件存储「病人队列」发送查询命令并获取一个查询结果信息，该结果信息中的每一条可以理解为某病人的挂号单。之后，医生对病人的问诊过程不是软件系统的考虑范围，医生完成问诊之后将问诊结果输入病历中，并根据病情开具相应的处方，最后打印处方交给病人，完成一次问诊过程。由此可以得到外部实体「医生」与顶层数据流图加工「医院就诊管理系统」之间的数据流分析结果：病历信息、处方信息。

- 外部实体「收费人员」与加工「医院就诊管理系统」之间的信息交互：收费员根据病人提交的处方，依照处方号进行查询并获取该处方的详细信息后，系统根据药品信息计算出本次处方所需交付的药费，即应付金额，病人交费后，收费员输入实收金额并修改处方状态盖章，如需找零钱则系统自动计算找零结果，打印收费清单，找钱并将处方与收费清单交还给病人，完成一次交费过程。由此可以得到外部实体「收费人员」与顶层数据流图加工「医院就诊管理系统」之间的数据流分析结果：实收金额、处方状态、收费清单。

- 外部实体「药剂师」与加工「医院就诊管理系统」之间的信息交互：药剂师根据病人提交的交费和盖章后的处方，查询并调取处方，核对无误后，修改处方状态（此处可以考虑药房库存管理的关系），配药并将药品交付给病人。病人完成一次医院的就医过程。由此可以得到外部实体「药剂师」与顶层数据流图加工「医院就诊管理系统」之间的数据流分析结果：处方状态。

⑤ 数据存储的分析过程：*挂号处*。经过分析，挂号处理后所须持久化或临时保存的信息有如下3个。

- *病历*：除了记录了病人病历号之外，还有病人的基本信息和历次问诊信息，是系统须持久化保存的数据。

- *挂号单*：记录每个挂号人员所产生的挂号记录，应该具有挂号单流水号、挂号时间、挂号人员工号等基本信息外，还须特别记录病人的病历号、姓名、住址等信息、挂号的科室及医生信息、挂号费及排队序号，是系统须持久化保存的数据。一般来说，挂号单也是统计挂号人员工作量的依据。

- *医生的排队信息*：系统级保存每日各科室每一位医生的病人排队信息，信息来源于挂号单，是系统临时保存的数据，每日须清空。

*问诊处*。

- *病历*：医生问诊之前须查询并调取病人的病历，查看历史病历，问诊之后须添加或者修改病历。

- *处方*：医生每次问诊之后都可能须根据结果开具相应的处方，它与病历的每次新记录具有关联关系。处方信息应具有病历号、问诊时间、医生工号和姓名、病人基本信息、疾病名称、药品名称及剂量、药品单价及总价（视情况而定）等，是系统须持久化保存的数据。

- *医生的排队信息*：医生登录系统后，须查看病人排队情况时，以工号为查询条件，从队列中获取相应的信息。

*收费处*。

- *处方*：收费员须根据病人处方的流水号查询并调取处方信息，然后填写收费数据，修改处方状态等信息。

- *收费清单*：一旦收取病人的药费，须根据处方信息生成一个以病人病历号为索引的收费清单。该清单一般是病人进行报销的依据，另外也可以考虑与社保报销对接。

*取药处*。

- *处方*：药剂师在发放药品之前须核对处方内容及交费信息，确认后修改处方状态，该状态表明处方已经在医院内部流转完毕。

分析结果：经过上述分析过程，得到绘制顶层数据流图4个基本元素的具体信息。

- 外部实体：挂号人员、医生、收费人员、药剂师。
- 加工：医院就诊管理系统。

数据流：

- 挂号人员与「加工」之间：挂号请求、挂号费、挂号单。
- 医生与「加工」之间：病历信息、处方信息。
- 收费人员与「加工」之间：实收金额、处方状态、收费清单。
- 药剂师与「加工」之间：处方状态。
- 数据存储与「加工」之间：在表示上只需加上双向箭头的线，用来表明「加工」与数据存储之间的数据交互，此时无须特别表示数据内容，可在数据词典中说明。

数据存储：病历、挂号单、处方、收费清单、排队信息。

根据上述的分析结果，可以得到一个初步的顶层数据流图，如图5-13所示。

// #figure(
//   image("img/fig_5_13.png", width: 75%),
//   caption: ["医院就诊管理系统"顶层数据流图],
// )

（2）*第一层数据流图*。

所谓「第一层」，也就是中间层数据流图中的「第一层」。其目标是确定该层具有多少个子「加工」，以及子「加工」之间新增加的数据流。须进一步解释顶层数据流图中每一条数据流从外部实体流入到系统级的「加工」之后，这些数据流是如何被接收和处理的。此时，涉及需要多少中间层能够解释清楚其结果和需求之间的关系。有的人习惯将分析结果直接表示出来，直接得到最底层各个「加工」的表示细节。从结果上看可以接受，但是忽略分析过程，一旦出现问题，或者被质疑时，很难分析清楚问题。为此，建议读者尽可能将分析过程逐层表示清楚。

① 子「加工」分析过程：根据已经得到的顶层数据流图，其目标是构建目标系统的子功能模型。可以参考业务模型分析的结果，也就是参考医院的组织结构如何分解医院的各项职能，比如挂号处、门诊科室、收费处和取药处，故系统级加工以部门设置分解成对应的子系统，或者根据具有相同类型的外部实体构成相应的子系统，即可以得到如下4个子「加工」：挂号处理子系统、问诊管理子系统、收费管理子系统、取药管理子系统。

下一步根据具体的业务信息分析并构建子系统之间的信息传递，也就是构建第一层数据流图加工之间的数据流。

② 子「加工」之间数据流的分析过程：首先须强调的是，子系统之间不一定存在数据交互，根据具体的当前系统物理和逻辑模型结果进行具体分析。

假设已知条件：

（1）医生的排队信息来自挂号处产生的挂号单；

（2）收费处的处方信息来自医生产生的处方；

（3）取药处的处方信息来自收费处修改状态后的处方；

（4）处方和排队信息都是数据文件存储。

基于此，分析人员有两个方案。

*方案一*：如果需求方没有特殊的要求，则4个「加工」之间可以不存在数据流，处方和排队信息的数据文件存储是这4个「加工」的共享数据文件，由此可以得到如图5-14(a)所示的第一层数据流图。

*方案二*：如果须考虑或者需求方有明确要求，一旦有新的挂号单产生，则医生可以第一时间被告知有新的患者已经挂号，比如医生的问诊界面有个明显的待诊人数提示，或者有新挂号时以某种方式提醒医生。同理，收费处和取药处也可具有同样的功能要求。在这种情况下，须在子「加工」之间存在一个数据流，并由此得到如图5-14(b)所示的第一层数据流图。

- 「挂号子系统」与「问诊子系统」之间的数据流：挂号通知；
- 「问诊子系统」与「收费子系统」之间的数据流：待收费通知；
- 「收费子系统」与「取药子系统」之间的数据流：待发药通知。

在绘制第一层数据流图时，须特别注意与顶层数据流图之间的元素完整和平衡原则。在顶层数据流图的基础上，第一层只增加一些子系统，以及子「加工」之间的一些数据流，并进一步明确子系统与外部实体之间的数据流。

// #figure(
//   image("img/fig_5_14.png", width: 75%),
//   caption: [两个方案的第一层数据流图],
// )

（3）*第二层数据流图*。

第二层数据流图的目的是进一步解释每一个输入数据流进入加工后，系统内是否还存在一些加工接收数据流、分解数据流、转换数据流、处理数据流直到存储必要的数据信息进入数据文件存储，并产生需求规定的数据流。

由于篇幅有限，只根据挂号处的挂号请求数据流开始，经过交纳挂号费，并最终产生一次挂号的挂号单来进行分析，确定第二层挂号子系统的子「加工」。

① 输入数据流：挂号请求的分析过程。

一个挂号请求等同于一个新的挂号操作，定义为「新建挂号」：

- 「新建挂号」可理解为创建一个新的初始挂号单。
- 所谓「初始」，可理解为初始化某些基础数据到挂号单里，比如挂号单的流水号、挂号时间、挂号人员的工号、挂号窗口号等信息。
- 挂号单的流水号根据系统某些生成规则产生，为此考虑一个子「加工」来处理，但是可以根据情况将该「加工」放置在后续层次的数据流图来表示；同样，其他诸如挂号时间、挂号人员的工号等信息也需要某些子「加工」从系统中获取。

初始化挂号单完成之后，根据基本信息的描述和要求，挂号人员输入病人的病历号，查询并调取该病人的一些基本信息，进行身份确认，定义为「病历查询」。

病人基本信息确认无误后，进一步输入病人须就诊的科室信息，以便查询到当前该科室和医生的排队情况，系统返回一个查询结果，挂号人员根据某些规则（默认规则是排队时间最短，即医生的排队人数最少）选择医生，定义为「选择医生」。这可以理解为一个子「加工」，同时可以进一步细化为两个子操作：

- 将病人信息与医生信息进行关联。
- 根据医生当前的病人排队情况产生一个挂号的排队号。

② 输入数据流：挂号费的分析过程。

一旦上述操作完成，系统根据医院的某些挂号费规则产生本次挂号的费用。

挂号人员收取病人交纳的费用，并输入相应的实收金额；如果实收金额大于应收挂号费，则系统还需要具有计算找赎零钱的子「加工」，定义为「收取挂号费」。

挂号人员确认所有挂号信息和收取的挂号费无误后，提交挂号单，定义为「生成挂号单」。提交挂号单等同于调用以下子操作：

- 本次挂号的数据写入挂号单数据文件存储。
- 调用挂号单打印功能。
- 将挂号单的某些信息插入排队信息的临时数据文件存储。
- 发送挂号通知。

完成一次挂号请求，即产生一个输出数据流挂号单，并返回就绪状态准备接收下一个挂号请求。

在得到上述子「加工」后，还须进一步明确子「加工」之间的数据流，其分析结果可参见图5-15。

// #figure(
//   image("img/fig_5_15.png", width: 75%),
//   caption: ["挂号子系统"的第二层数据流图],
// )

图中需要读者注意，由于第二层数据流图展开后图形元素较多，为此可以按照第一层数据流图的加工过程分别展开。在第二层数据流图中，由于图5-15仅仅描绘挂号子系统加工的展开，为此第一层数据流中与之有关的加工「问诊子系统」在图中由外部实体的图形元素表示，代表该「加工」是当前问题域的外部元素。

根据以上分析过程得到「挂号子系统」的第二层数据流图，其他3个子系统的第二层数据流图也可以参照上述方式分析得到。从图5-14和图5-15两层数据流图可知，为了提高可读程度，要求在分层的数据流图中「加工」须有统一的编号。

后续的问题：是否需要第三层数据流图？根据「挂号请求」和「挂号费」数据流的分析结果可知，「新建挂号」、「选择医生」和「生成挂号单」仍然可以分解为多个子「加工」，为此第三层数据流图是必需的，这部分留给读者作为后续思考和练习的环节。

到此为止，给读者展示如何根据需求分析阶段得到的当前系统物理和逻辑模型构建目标系统功能逻辑模型的方法和分析过程。有两点内容须进一步解释。

（1）数据流图作为目标系统功能逻辑模型的结果，如何判断是否正确？

需求分析的模型与数学模型不一样，其对与错到目前为止还没有一种方法可以证明；即便有，与其花很多的时间和成本证明，还不如将其理解为一种在用户和开发人员之间深入理解业务背景的一种方法，重要作用在于可以帮助正确理解真实的需求。为此，不存在对和错，只有是否合理和不合理。

（2）数据流图中外部实体与系统加工之间的数据流交互与实际操作中使用者点击系统提供的功能之间有什么区别？

使用者操作一个系统软件提供的功能，可以理解为外部实体向系统的某个功能发出某个指令、命令或者是事件，根据定义可将其理解为控制流。控制流的顺序决定某个需求执行的步骤。数据流图表明系统的某个功能在控制流作用之后，其执行的数据信息从哪里来，执行后的数据信息又去哪里。

一般来说，程序的调用既包含控制信息，也包含数据信息，但是注意此时的分析目标是构建目标系统的功能逻辑模型，只需定义每项功能，即「加工」做什么即可，后期在进行软件设计时描述软件功能的调用关系，明确表示功能之间的执行步骤。

== 系统行为建模

为了直观地分析系统的动作，从特定的视角出发描述系统的行为，须采用动态分析的方法。其中，最为常用的动态分析方法有状态迁移图、时序图、Petri网等。

=== 状态迁移图

行为建模给出需求分析方法所有的操作原则，但只有结构化分析方法的扩充版本才提供建模的符号。

利用如图5-16所示的状态迁移图（STD）或状态迁移表来描述系统或对象的状态，以及导致系统或对象状态改变的事件，从而描述系统的行为。

每一个状态代表系统或对象的一种行为模式。状态迁移图指明系统的状态如何响应外部的信号（事件）进行推移。在状态迁移图中，用圆圈「O」表示可得到的系统状态，用箭头「→」表示从一种状态向另一种状态迁移。在箭头上写上导致迁移的信号或事件的名称。如图5-16(a)所示，系统可取得的状态为S1、S2、S3，事件为t1、t2、t3、t4。事件t1引起系统状态S1向状态S3迁移，事件t2引起系统状态S3向状态S2迁移等。图5-16(b)是与5-16(a)等价的状态迁移表。

// #figure(
//   image("img/fig_5_16.png", width: 70%),
//   caption: [状态迁移图与其等价的状态迁移表例],
// )

另外，状态迁移图指明作为特定事件的结果（状态）。状态包含可能执行的行为（活动或「加工」）。

如果系统比较复杂，可以把状态迁移图分层表示。例如，在确定如图5-17所示的大状态S1、S2、S3之后，接下来可把状态S1、S2、S3细化。该图对状态S1进行细化。此外，在状态迁移图中，由一个状态和一个事件所决定的下一状态可能有多个，实际迁移到哪一个是由更详细的内部状态和更详细的事件信息来决定的。此时，可采用状态迁移图的一种变形，如图5-18所示，使用加进判断框和处理框的记法。

// #figure(
//   image("img/fig_5_17.png", width: 40%),
//   caption: [状态迁移图的细化],
// )

// #figure(
//   image("img/fig_5_18.png", width: 45%),
//   caption: [状态迁移图的变形],
// )

=== Petri网

Petri网是由德国人Carl Adam Petri于1962年提出来的，最初用来表达异步系统控制规则的图形表示法，后来Petri网在计算机科学中也得到广泛的应用，例如在性能评价、操作系统和软件工程等领域，尤其是处理并发系统当中的同步问题、资源竞争问题及死锁问题等。

（1）*基本概念*。

Petri网简称PNG（Petri Net Graph），是一种有向图，包含4种基本元素：一组位置P（Place）、一组转换T（Transition）、输入函数I（Input）及输出函数O（Output）。图5-19举例说明Petri网的组成部分。其中：

- 一组位置{P_1, P_2, P_3, P_4}，在图中用圆圈代表位置。
- 一组转换为{t_1, t_2}，在图中用短直线表示「转换」。
- 两个用于转换的输入函数，用由位置指向「转换」的箭头表示，I(t_1) = {P_1, P_2}，I(t_2) = {P_3}。
- 两个用于转换的输出函数，用由「转换」指向位置的箭头表示，O(t_1) = {P_3}，O(t_2) = {P_4}。

注意，输出函数O(t_1)有P_3，是因为有两个箭头由t_1指向P_3。

// #figure(
//   image("img/fig_5_19.png", width: 65%),
//   caption: [Petri网的组成],
// )

Petri网位置中如果加一个黑点，称之为标记（Token）。标记在位置中出现表明处理要求到来。如图5-20所示，有4个标记，其中一个在P_1中，两个在P_2中，P_3中没有标记，还有一个在P_4中。上述标记可用向量(1, 2, 0, 1)表示。

// #figure(
//   image("img/fig_5_20.png", width: 65%),
//   caption: [带标记的Petri网],
// )

由于P_1和P_2有标记，因此t_1启动（即被激发）。通常，当每个输入位置所拥有的标记数大于等于从该位置到转换的线数时，允许转换。当t_1被激发时，P_1和P_2上各有一个标记移出，而P_3则增加一个标记，如图5-21所示。Petri网标记总数不是固定不变的，在这个例子中两个标记移出，而P_3上只能增加一个标记。

// #figure(
//   image("img/fig_5_21.png", width: 65%),
//   caption: [T_1被激发后的Petri网],
// )

此时，P_3上只留有一个标记，因此t_2也可以激发。当t_2激发时，P_3移走一个标记，而P_4新增加两个标记，如图5-22所示。Petri网具有非确定性，即如果几个「转换」都达到激发条件，则其中任意一个都可以激发。

// #figure(
//   image("img/fig_5_22.png", width: 60%),
//   caption: [T_2被激发后的Petri网],
// )

（2）*简单的Petri网模型*。

下面以一个具体例子说明Petri网模型的建立过程。如图5-23所示，对于一个环形铁路，在A站与B站之间是单轨，在某一时刻只能走一列火车，但A站与B站都是双向运行的。上行/下行列车交替行驶。

// #figure(
//   image("img/fig_5_23.png", width: 55%),
//   caption: [环形铁路示例],
// )

用如图5-24所示的PNG描述两列火车在铁路上的运行实况。由于在A站和B站之间有一段单轨线路，在某一时刻只允许一列火车通过，因此只有当单线上没有列车通过时，火车才能进入单线运行，否则火车只能在A站或者B站等候，等待单线空出来使用。

// #figure(
//   image("img/fig_5_24.png", width: 65%),
//   caption: [环形铁路运行的PNG示例],
// )

== 数据词典

数据词典（Data Dictionary, DD）的作用是对于数据流图中出现所有被命名的图形元素，在数据词典中作为一个词条加以定义，使得每一个图形元素的名称都有一个确切的解释。数据词典中所有的定义应是严密的、精确的，不可模棱两可。

=== 数据词典的构成

对在数据流图中每一个命名的图形元素均给予定义，其内容有图形元素的名称、别名或编号、分类、描述、定义、位置等。以下是不同词条应给出的内容。

（1）*数据流词条描述*。数据流是数据结构在系统内传播的路径。一个数据流词条应有以下若干项内容。

// #table(
  columns: 2,
//   fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
//   stroke: 0.6pt + luma(160),
//   inset: (x: 8pt, y: 5pt),
  [数据流名称], [唯一标识数据流的名称],
  [简要描述], [简要介绍该数据流的作用，即它产生的原因和结果],
  [数据流来源], [来源于何处],
  [数据流去向], [流向何处],
  [数据流组成], [描述该数据流内部数据元素的组成成分],
  [备注], [需要时，描述数据流量和流通量等信息],
// )

（2）*数据元素词条描述*。数据流图中的每一个数据结构都是由数据元素构成的，数据元素是数据处理中的最小单元，且不可再细分，直接反映事物的某一特征。对于这些数据元素，必须在数据词典中给予描述，其具体内容如下所述。

// #table(
  columns: 2,
//   fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
//   stroke: 0.6pt + luma(160),
//   inset: (x: 8pt, y: 5pt),
  [数据元素名称], [唯一标识数据元素的名称或编号],
  [简要描述], [简要描述该数据元素的作用，以及位于哪一个数据结构内],
  [类型], [数字、字符等类型],
  [长度], [该数据类型规定的取值范围，例如姓名的长度为60个字符],
  [取值范围], [该姓名的取值范围必须大于2个字符，且小于60个字符],
  [备注], [],
// )

数据元素的取值可分数字型与文字型。数字型又有离散值与连续值之分。离散值或是枚举的，或是介于上下界的一组数；连续值一般是有取值范围的实数集。对于文字型，须给予编码类型，文字值须加以定义。

（3）*数据文件词条描述*。数据文件是数据结构保存的地方。一个数据文件词条应有以下若干项内容。

// #table(
  columns: 2,
//   fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
//   stroke: 0.6pt + luma(160),
//   inset: (x: 8pt, y: 5pt),
  [数据文件名称], [唯一标识数据文件的名称],
  [简要描述], [简要描述该数据文件存放什么数据],
  [输入数据], [写入该数据文件的数据内容或数据结构],
  [输出数据], [从该数据文件读出的数据内容或数据结构],
  [数据文件组成], [描述该数据文件的数据结构组成],
  [存储方式], [数据文件的操作方式及关键字],
  [备注], [],
// )

（4）*加工逻辑词条描述*。数据流图中的每一个「加工」除了进行基本信息的描述之外，还必须对该「加工」的逻辑或规则进行描述，采用的方法有判定表、判定树或结构化英语等。「加工」的逻辑词条主要有如下若干项。

// #table(
  columns: 2,
//   fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
//   stroke: 0.6pt + luma(160),
//   inset: (x: 8pt, y: 5pt),
  [加工名称], [唯一标识加工的名称],
  [简要描述], [描述「加工」逻辑和规则及功能简述],
  [加工编号], [反映该「加工」的层次],
  [输入数据流], [描述进入该「加工」的一个或多个数据流],
  [输出数据流], [描述流出该「加工」的一个或多个数据流],
  [加工逻辑], [描述该「加工」的逻辑或规则（如加工逻辑说明部分）],
  [备注], [],
// )

（5）*外部实体词条描述*。

// #table(
  columns: 2,
//   fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
//   stroke: 0.6pt + luma(160),
//   inset: (x: 8pt, y: 5pt),
  [外部实体名称], [唯一标识外部实体的名称],
  [简要描述], [指明该实体的性质及与系统之间的关系],
  [有关数据流], [指明该外部实体与系统之间交互的数据流有哪些],
  [备注], [],
// )

=== 数据词典的使用

在结构化分析的过程中，可以通过数据名称方便地查问数据的定义；同时，还可按各种要求，随时列出各种表，以满足分析员的需要。可以按描述内容（或定义）来查询数据的名称。通过检查各个加工的逻辑功能，实现和检查数据与程序保持一致性和完整性。在以后的设计与实现阶段，以至于到维护阶段，都需要参考数据词典进行设计、修改和查询。

在数据词典的编制中，分析员最常用描述数据结构的方式有定义式或Warnier图，这里主要介绍定义式。在数据流图中，数据流和数据文件都具有一定的数据结构。因此必须以一种清晰、准确、无二义性的方式来描述数据结构。表5-1给出的定义方式是一种严格的描述方式。

// #table(
  columns: 3,
//   fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
//   stroke: 0.6pt + luma(160),
//   inset: (x: 8pt, y: 5pt),
  [*符号*], [*含义*], [*举例*],
  [=], [被定义为], [例如，x = a + b，表示x由a和b组成],
  [+], [与], [],
  [[ | ]], [或], [例如，x = [a | b]，表示x由a或由b组成],
  [{...}], [重复], [例如，x = \{a\}，表示x由0个或多个a组成],
  [m\{...\}n], [重复], [例如，x = 3\{a\}8，表示x中至少出现3次a，至多出现8次a],
  [(...)], [可选], [例如，x = (a)，表示a可在x中出现，也可不出现],
  [「...」], [基本数据元素], [例如，x = 「a」，表示x为取值为a的数据元素],
 [..], [连结符], [例如，x = 1..9，表示x可取1到9之中的任一值],
// )

下面以银行存折作为数据结构描述的例子进行说明，银行存折的格式如图5-25所示。

// #figure(
//   image("img/fig_5_25.png", width: 65%),
//   caption: [存折格式],
// )

在银行的取款数据流图中，数据流或者数据文件「存折」的格式如图5-25所示，它在数据词典中数据结构和数据定义的格式如下所述。

```
存折 = 户名 + 所号 + 账号 + 开户日 + 性质 + (印密) + 1{存取行}50
户名 = 2{字母}24
所号 = "001" .. "999"        // 注：储蓄所编码，规定3位数字
账号 = "00000001" .. "99999999"    // 注：账号规定由8位数字组成
开户日 = 年 + 月 + 日
性质 = "1" .. "6"          // 注："1"表示普通户，"6"表示工资户等
印密 = "0"                  // 注：印密在存折上不显示
存取行 = 日期 + (摘要) + 支出 + 存入 + 余额 + 操作 + 复核
日期 = 年 + 月 + 日
年 = "00" .. "99"
月 = "01" .. "12"
日 = "01" .. "31"
摘要 = 1{字母}4
支出 = 金额
存入 = 金额
金额 = "0000000.01" .. "9999999.99"  // 注：金额规定不超过9999999.99元
操作 = "00001" .. "99999"
复核 = "00001" .. "99999"
字母 = ["a" .. "z" | "A" .. "Z"]
```

这表明存折由7部分组成，其中的「存取行」重复出现多次。如果重复次数是个常数，例如为50，则可表示为\{存取行\}50；如果重复次数是变量，那么应估计其变动范围，例如存取行从1到50，则可记为1\{存取行\}50。在存取行中，「摘要」加圆括号，表明它是可有可无的。「日期」由「年 + 月 + 日」组成，例如1997年7月15日表示成970715。「支出」和「存入」表明该存取行存取的金额。「余额」是经过存取之后存折上剩余的钱。「操作」、「复核」是银行职员的代码，用5位整数表示。

这种定义方法自顶向下，逐级给出定义式，直到最后给出基本数据元素为止。数据词典明确地定义各种信息项。随着系统规模的增大，数据词典的规模和复杂度迅速增加。

=== 加工逻辑说明

在数据流图中，每一个加工框只简单地写上一个加工名，这显然不能表达「加工」的全部内容。随着自顶向下逐层细化，功能越来越具体，加工逻辑也越来越精细。到最底一层，加工逻辑详细到可以实现的程度，因此称为「原子加工」或「基本加工」。如果能够写出每一个基本加工的全部详细逻辑功能，再自底向上综合，就能完成全部逻辑加工。目前，用于写加工逻辑说明的工具有结构化英语、判定表和判定树。

在写基本加工逻辑的说明时，应满足如下的要求：

- 对数据流图的每一个基本加工，必须有一个加工逻辑说明；
- 加工逻辑说明必须描述基本加工如何把输入数据流变换为输出数据流的加工规则；
- 加工逻辑说明必须描述实现加工的策略而不是实现加工的细节。

（1）*结构化英语*。

结构化英语（Structured English）也称为PDL，是一种介于自然语言和形式化语言之间的半形式化语言。它是在自然语言基础上加一些限制因素而得到的语言，使用有限的词汇和有限的语句来描述加工逻辑。结构化英语的词汇表由英语命令动词、数据词典中定义的名称、有限的自定义词和控制结构关键词IF_THEN_ELSE、WHILE_DO、REPEAT_UNTIL、CASE_OF等组成。其动词的含义应具体，尽可能少用或不用形容词和副词。

语言的正文用基本控制结构进行分割，加工中的操作用自然语言短语来表示。其基本控制结构有简单陈述句结构、判定结构和重复结构。此外，在书写时，必须按层次横向向右移行，续行也同样向右移行，对齐。

下面是商店业务处理系统中「检查发货票」功能使用结构化英语完成的加工逻辑描述：

```
IF the invoice exceeds $500 THEN
  IF the account has any invoice more than 60 days overdue THEN
    The confirmation pending resolution of the debt
  ELSE (account is in good standing)
    Issue confirmation and invoice
  ENDIF
ELSE (invoice equal $500 or less)
  IF the account has any invoice more than 60 days overdue THEN
    Issue confirmation, invoice and write message on credit action report
  ELSE (account is in good standing)
    Issue confirmation and invoice
  ENDIF
ENDIF
```

在具体的使用过程当中，除了控制结构关键词使用英语之外，其他的组成部分也可使用逻辑表达清晰的中文进行描述。

```
IF 发货单金额超过$500 THEN
  IF 欠款超过60天 THEN
    在偿还欠款前不予批准
  ELSE (欠款未超期)
    发批准书、发货票
  ENDIF
ELSE (发货票金额未超过$500)
  IF 欠款超过60天 THEN
    发批准书、发货票及催欠报告
  ELSE (欠款未超期)
    发批准书、发货票
  ENDIF
ENDIF
```

（2）*判定表*。

在某些数据处理问题中，某数据流图的「加工」须依赖多个逻辑条件的取值，即完成这一「加工」的一组动作是由于某一组条件取值的组合而引发的。这时使用判定表（Decision Table）来描述比较合适。下面以「检查发货单」为例说明判定表的构成，如图5-26所示。

// #figure(
//   image("img/fig_5_26.png", width: 65%),
//   caption: ["检查发货单"的判定表],
// )

判定表由4个部分组成，双线分割开的4部分如下所述。

- *条件桩（Condition Stub）*——左上部分：列出各种可能的条件。除去某些问题中对各个条件的先后次序有特定的要求以外，判定表中各条件的先后次序通常不要求。

- *条件项（Condition Entry）*——右上部分：给出各个条件取值的组合。

- *动作桩（Action Stub）*——左下部分：列出可能采取的动作。这些动作的排列顺序没有限制，但为便于阅读也可令其按适当的顺序排列。

- *动作项（Action Entry）*——右下部分：是和条件项紧密相关的，指出在条件项各种取值的组合情况下应采取什么动作。这里将任一条件取值组合及其相应待执行的动作称为规则，它在判定表中是纵贯条件项和动作项的一列。显然，判定表列出多少个条件取值的组合，也就有多少条规则，即条件项与动作项有多少列。

在实际使用判定表时，常先把它化简。如果表中两条或更多规则具有相同的动作，并且其条件项之间存在某种关系，可设法将它们合并。例如，图5-27(a)表示两个规则的动作项一致，条件项中第三条件的取值不同，这表明在第一、第二条件分别取真值和假值时，第三条件不论取何值，都执行同一动作，即执行的动作与第三条件的取值无关。这样，便可将这两条规则合并，合并后的第三条件取值用「—」表示，以示与取值无关。同样，无关条件项「-」在逻辑上又可包含其他的条件项取值，具有相同动作的规则还可进一步合并，如图5-27(b)所示。

// #figure(
//   image("img/fig_5_27.png", width: 65%),
//   caption: [动作相同的规则合并],
// )

判定表把在什么条件下，系统应完成哪些操作，表达得十分清楚、准确，这是用语言说明难以准确清楚表达的。但是，用判定表描述循环比较困难。有时，判定表可以和结构化英语结合使用。

（3）*判定树*。

判定树（Decision Tree）也是用来表达加工逻辑的一种工具，有时比判定表更直观。用它来描述「加工」，很容易为用户接受。下面把前面的「检查发货单」的例子用判定树表示。

// #figure(
//   image("img/fig_5_28.png", width: 60%),
//   caption: [判定树],
// )

没有一种统一的方法来构造判定树，也不可能有统一的方法，因为它是以结构化英语，甚至是自然语言写成的叙述文作为构造树的原始依据的。可以从中找些规律。首先，应从文字资料中分清哪些是判定条件，哪些是判定出的结论。

例如，判定条件是「金额>$500，欠款\>60天的发货单」，待判定的结论是「发给批准书和发货单」。然后，从资料叙述的一些连接词（如「除非」、「然而」、「但」、「并且」、「和」、「或」等）找出条件之间的关系。

在描述基本加工逻辑的3种工具，即结构化英语、判定表和判定树中，使用较多的是结构化英语和判定表。判定树在某些情况下比判定表更直观，易于理解和使用。这3种手段各有优缺点。总之，加工逻辑说明是结构化分析方法的一个组成部分，对每一个「加工」都要加以说明。使用的手段应当以结构化英语为主，对存在判断问题的加工逻辑可辅之以判定表和判定树。

== 结构化方法的软件需求规格说明书

经过漫长而复杂的需求分析过程之后，软件需求分析的最终成果体现在《软件需求规格说明书》中，它不仅是软件生命周期一个极为重要的里程碑，也是软件设计的坚实基础。对于《软件需求规格说明书》的内容和框架，不同的作者有不同的结构，国内也有相应的国标规范，本教材结合国标和实际的应用，提供一份仅供参考的基于数据流图结构化方法的《软件需求规格说明书》模板。

// #block(
  width: 100%,
//   inset: (x: 12pt, y: 8pt),
//   stroke: 0.8pt + luma(160),
  radius: 4pt,
)[
  *封面页*

  标题：XXX——系统软件需求分析说明书

  作者：x x x x

  完成时间：yyyy_mm_dd

  版本：v_1.1

  修改记录：描述该版本与上一版本的不同之处，由谁执笔修改，修改的时间

  *目录页*

  系统根据文档结构自动生成
// ]

== 1. 引言

*1.1 编写目的*：说明编写目的并指明读者对象。

*1.2 项目背景*：说明项目的委托单位、开发单位和主管部门。该系统与其他系统的关系。

*1.3 词汇*：列出文档中所使用专业术语的定义和缩写词的原文含义。

*1.4 书写规范*：说明需求文档的结构及需求分析所使用的方法。

== 2. 系统概述

*2.1 系统建设目标*：根据用户提供的需求书内容，描述系统建设的总体和分阶段目标。

*2.2 系统运行环境*：给出系统运行所必需的硬件和软件环境和配置要求。

*2.3 条件和限制因素*：根据系统阶段建设目标给出系统建设和运行的前提条件及其他限制因素。

== 3. 系统功能需求

*3.1 业务背景描述*：描述业务背景、业务的组织结构以及业务流程，即问题分析的成果。

*3.2 系统功能需求*：根据业务背景，给出系统的层次化数据流图并给出结合层次的数据流图，给出相应层次的数据词典。

*3.2.1 第0层数据流图*

- 3.2.1.1 数据流图：给出数据流图。
- 3.2.1.2 数据词典：根据教材「数据词典」部分所规定的格式进行以下内容的信息描述；在加工说明中如果须进行必要的业务逻辑规则说明时，建议采用结构化英语的方式进行描述。
  - 外部实体
  - 数据流
