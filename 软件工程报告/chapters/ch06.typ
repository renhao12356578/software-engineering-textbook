#set heading(numbering: "1.1")
#show heading: it => {
  let level = it.level
  if level == 1 {
    v(20pt)
    align(center)[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 18pt, weight: "bold"); #it]
    v(15pt)
  } else if level == 2 {
    v(12pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 14pt, weight: "bold"); #it]
    v(6pt)
  } else {
    v(8pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 12pt, weight: "bold"); #it]
    v(4pt)
  }
}

= 第 6 章 软件设计的概念及原则

经过需求分析阶段，基于系统必须"做什么"的需求规格说明书，必须进行软件系统的结构设计并描述和证明这些需求是"如何被软件系统执行"的过程，最终形成软件设计规格说明书。软件设计的最基本目标是回答"概括描述系统如何实现用户所提出来的功能和性能等方面的需求？"问题。因此，这个阶段的软件设计又可称为概要设计。在此基础上，软件设计还必须明确描述每一个模块的内部实现逻辑，这个设计过程称为软件详细设计。

== 软件设计的目标

软件设计的目标是根据软件需求分析的结果，设想并设计软件，即根据目标系统的逻辑模型确定目标系统的物理模型。软件设计包括软件系统的结构设计、处理方式的设计、数据结构和数据存储的设计，以及界面和可靠性设计等方面。

对于任何工程项目来说，为了能够得到一个可靠并能满足各项需求指标的产品，就必须在工程实施之前进行必要的工程设计。因此，设计往往是开发活动的第一步。通常，把设计定义为"应用各种技术和原理，对设备、过程或系统给出足够详细的定义，使之能够在物理上得以实现"。

软件设计是软件工程过程中的技术核心，是构造和验证软件所需的三项技术活动（设计、代码生成和测试）之一。软件设计也是后续开发步骤及软件维护工作的基础。如果没有软件设计，则只能建立一个不稳定和不可靠的系统结构，并且极有可能造成所构建的系统不能满足需求规格说明书中所要求的功能和性能，最终使得软件项目不能按期保质完成，甚至可能导致整个项目失败。

== 软件设计的过程

软件设计历经四十年的发展过程，早期的设计工作集中在模块化程序的开发标准和自顶向下求精软件结构的方法，进而发展成一种称为结构化程序设计的方法；之后又提出将数据流或数据结构转化为设计定义的方法；随着面向对象语言的飞速发展和应用，相应的面向对象软件设计方法占据目前软件设计的主导地位。当前，由于软件开发的市场需求和规模急速扩大，软件设计方面的着重点已转移到软件体系结构和可用于实现软件体系结构设计模式的研究领域上。每一种软件设计方法都引入独特的符号体系以用于展示软件各个角度的模型，这些方法和模型都具有以下共同特征：

#par(first-line-indent: 0em)[（1）用于将分析模型变换到设计模型的表示机制。]

#par(first-line-indent: 0em)[（2）用于表示功能及其接口的符号体系。]

#par(first-line-indent: 0em)[（3）用于求精和划分的启发信息和机制。]

软件设计是一个把软件需求变换成包含软件功能模型、数据模型及行为模型的过程。该过程从工程管理的角度来看一般分为两个阶段，即概要设计阶段，最初的设计模型只需描绘出可直接反映功能数据、行为需求的软件总体框架；详细设计阶段，进一步将设计模型逐步细化，即通过对软件的功能模块进行细化，得到各功能模块的详细数据结构和算法，使得功能模块在细节上非常接近源程序的软件设计模型。从管理和技术两个不同的角度对软件设计的理解可以通过图 6-1 表示。

#figure(
  image("../img/fig_6_1.png", width: 80%),
  caption: [软件设计过程的两种表示],
) <fig-6-1>

从技术开发的角度来看，软件设计主要分成三个部分：

#par(first-line-indent: 0em)[（1）*系统结构设计* 定义软件系统各主要元素（主要指功能模块）之间的关系，其中包括软件的模块接口设计，它特指软件内部各模块之间、软件与其他协同系统之间及软件与用户之间交互机制的设计。]

#par(first-line-indent: 0em)[（2）*数据设计* 将软件各模块所需处理的数据及系统需长久保存的数据进行数据结构和数据存储的设计。]

#par(first-line-indent: 0em)[（3）*过程设计* 把系统结构设计转换成软件的过程描述，主要是确定各功能模块内部结构的详细定义，包括模块主要算法逻辑和局部数据结构的定义。]

== 概要设计

为了获得软件的概要设计规格说明书，无论采用结构化、还是面向对象的设计方法，或其他的设计方法，在概要设计阶段软件设计人员都须执行和完成以下 8 项活动。

=== 制订设计规范

在进入软件开发阶段之初，首先应为软件开发小组制订设计阶段应该共同遵守的标准，它是后续设计和编码工作的基础，方便协调组内各成员可以在一个相同认可的语境下工作和讨论问题。它包括：

#par(first-line-indent: 0em)[（1）阅读和理解软件需求说明书，在给定预算范围内和现有的技术条件下，确认用户的需求能否实现。若不能实现，则须明确实现的条件，从而确定设计的目标，以及它们的优先顺序。]

#par(first-line-indent: 0em)[（2）根据目标确定最合适的设计方法，比如可以选择结构化或者面向对象的设计方法。]

#par(first-line-indent: 0em)[（3）规定设计文档的编制标准，包括设计文档体系、样式及格式、记述详细的程度、图形的画法等。]

#par(first-line-indent: 0em)[（4）规定编码的信息形式（代码体系）、与硬件和操作系统的接口规约、命名规则等。]

=== 软件系统结构设计

软件的概要设计一般来讲也可称为软件的系统结构设计。对根据需求分析阶段得到的分析模型，尤其是功能、数据模型及性能方面的要求进行分析，进而确定待构建软件系统的基本功能结构，比如单机版结构、层次化结构、分布式结构及云计算结构等，再进一步确定相应的数据存储模式和结构。在设计阶段后期，基于已确定的系统结构将系统的各个功能集成起来成为一个完整的系统，包括：

#par(first-line-indent: 0em)[（1）采用某种设计方法，将一个复杂的系统按功能划分成模块的层次结构。]

#par(first-line-indent: 0em)[（2）确定每个模块的功能，建立需求与功能之间的对应关系。]

#par(first-line-indent: 0em)[（3）确定模块间的调用关系及模块间的接口。]

#par(first-line-indent: 0em)[（4）优化已有结构使系统达到要求的性能指标。]

=== 处理方式设计

首先，为每一个已确定的功能模块定义所必需的算法，并评估算法的性能。其次，还须确定为满足软件系统的性能需求所必需的算法和模块间的控制方式（性能设计），性能主要指以下 4 个指标。

#par(first-line-indent: 0em)[（1）*周转时间*：即一旦向计算机发出要求处理的请求之后，从第一次输入开始，经过多次交互和处理，直到最后一次输出结果为止的整个时间。]

#par(first-line-indent: 0em)[（2）*响应时间*：用户一次请求的输入输出时间。]

#par(first-line-indent: 0em)[（3）*吞吐量*：单位时间内系统或某个模块能够处理的数据量称为吞吐量，表示系统处理能力的指标。]

#par(first-line-indent: 0em)[（4）*精度*：在进行科学计算或工程计算时运算精确度的要求。]

=== 数据设计

确定软件涉及的文件系统结构及数据库的模式、子模式，进行数据完整性和安全性的设计。它包括：

#par(first-line-indent: 0em)[（1）确定输入、输出文件的详细数据结构。]

#par(first-line-indent: 0em)[（2）结合算法设计，确定算法所必需的逻辑数据结构及其操作。]

#par(first-line-indent: 0em)[（3）确定对逻辑数据结构所必需的操作程序模块，并限制和确定各个数据设计决策的影响范围。]

#par(first-line-indent: 0em)[（4）数据的保护性设计：]

- *防卫性设计*。在软件设计中插入自动检错、报错和纠错的功能。
- *一致性设计*。有两个方面：其一是保证软件运行过程中所使用数据的类型和取值范围不变；其二是在并发处理过程中使用加锁和解锁机制，保持数据不被破坏。

=== 可靠性设计

可靠性设计也称为质量设计。软件可靠性指软件系统在长时间的运行过程中出现的错误数及系统恢复的能力，以及由此而发现的各类文档中出现的描述错误和设计错误。这些错误数是评价一个软件可靠性的重要指标。

软件可靠性与硬件不同，软件越使用可靠性越高。但是在运行过程中，为了适应环境的变化和用户新的要求，须经常对软件进行改造和修正，这就是软件维护。由于软件维护往往产生新的故障，所以要求在软件开发期间应当尽早找出差错，并在软件开发的一开始就要确定软件可靠性和其他质量指标，考虑相应的措施，以使得软件易于修改和易于维护。

=== 界面设计

首先，软件系统的界面不仅能够直观地反映软件的系统功能，而且也能体现软件设计人员是否正确理解软件需求，同时也是快速软件开发所必需的一个环节。在软件设计阶段可以根据所采用的生命周期模型来确定如何运用界面设计，以及实施界面设计的次序。其次，界面设计的方式方法还决定一个软件系统的易用性。

=== 编写概要设计阶段的文档

概要设计阶段完成时应编写以下文档。

#par(first-line-indent: 0em)[（1）*概要设计说明书*：给出系统目标、总体设计、数据设计、处理方式设计、运行设计、出错设计等。]

#par(first-line-indent: 0em)[（2）*数据库设计说明书*：给出软件系统所使用的数据库简介、数据模式设计、物理设计等。它可以是概要设计说明书的一部分，也可是独立的文档。]

#par(first-line-indent: 0em)[（3）*用户手册*：修订需求分析阶段编写的用户手册。]

#par(first-line-indent: 0em)[（4）*测试计划*：对测试的策略、方法和步骤提出明确的要求。]

=== 概要设计评审

在完成以上 7 项工作之后，应当组织对概要设计工作的评审工作。评审的对象是软件概要设计说明书及其附带的各类设计文档，评审内容包括以下 10 项。

#par(first-line-indent: 0em)[（1）*可追溯性*：分析该软件的系统结构、子系统结构，确认该软件设计是否覆盖所有已确定的软件需求，软件每一成分是否可追溯到某一项需求。]

#par(first-line-indent: 0em)[（2）*接口*：分析软件各部分之间的联系，确认该软件的内部接口与外部接口是否已经明确定义。模块是否满足高内聚和低耦合的要求。模块作用范围是否在其控制范围之内。]

#par(first-line-indent: 0em)[（3）*风险*：确认该软件设计在现有技术条件和预算范围内是否能按时实现。]

#par(first-line-indent: 0em)[（4）*实用性*：确认该软件设计对于需求的解决方案是否实用。]

#par(first-line-indent: 0em)[（5）*技术清晰度*：确认该软件设计是否以一种易于翻译成代码的形式表达。]

#par(first-line-indent: 0em)[（6）*可维护性*：从软件维护的角度出发，确认该软件设计是否考虑便于未来维护。]

#par(first-line-indent: 0em)[（7）*质量*：确认该软件设计是否表现出良好的质量特征。]

#par(first-line-indent: 0em)[（8）*各种选择方案*：是否考虑过其他方案，比较各种选择方案的标准。]

#par(first-line-indent: 0em)[（9）*限制条件*：评估对该软件的限制条件是否现实，是否与需求一致。]

#par(first-line-indent: 0em)[（10）*其他具体问题*：对文档、可测试性、设计过程等进行评估。]

== 详细设计

软件的详细设计也称为软件的过程设计。相对于概要设计，其性质相对简单，就是针对软件概要设计的结果进行功能模块内部结构的设计。在详细设计过程中，须完成的工作是：

#par(first-line-indent: 0em)[（1）确定软件各个功能模块内的算法及各功能模块的内部数据结构。]

#par(first-line-indent: 0em)[（2）选定某种表达方式来描述各种算法，比如程序流程图或者程序设计语言（PDL）等。]

#par(first-line-indent: 0em)[（3）进行详细设计的评审。]

== 软件设计模型

软件的设计模型由静态结构和动态结构两部分构成，其中设计模型的静态结构由软件的功能结构和数据结构组成，用于展示软件系统能够满足所有需求的框架结构；设计模型的动态结构则以某种方式表示功能响应需求时处理数据的过程或条件，用于进一步解释软件结构中各功能之间如何协调工作的机制。

软件的设计模型取决于需求分析结果的分析模型，其中核心的功能模型和数据模型分别对应于面向对象分析模型的用例模型和领域模型及结构化分析模型中的数据流图和实体关系图；设计模型的动态结构分别对应于面向对象分析模型中领域模型表示业务流程的活动图及结构化分析模型中的状态迁移图。

软件的设计模型通过一系列的软件设计活动而得到。软件的设计活动可概括为以下 6 部分的内容。

#par(first-line-indent: 0em)[（1）软件的系统结构设计：]

- 软件的体系结构设计。结合软件体系结构的已有类型或风格，决定当前软件系统的体系结构和框架，作为后续软件设计活动的基础。
- 软件的功能结构设计。根据需求分析的结果并结合已经确定的软件体系结构，进一步确定需求对应的软件功能模块及其功能模块之间的关系。

#par(first-line-indent: 0em)[（2）软件的数据设计：]

- 软件的数据存储结构设计。根据需求分析结果中的领域模型或者问题域描述，并结合软件体系结构，确定系统须持久保存数据的数据模型。
- 软件的局部数据结构设计。根据确定的软件功能结构，以及相应的业务处理逻辑，确定每个功能模块内部所需要的局部数据结构，并满足功能模块之间调用接口的要求。

#par(first-line-indent: 0em)[（3）软件的接口设计：]

- 功能模块之间及分层结构之间的接口设计。根据软件的功能结构，进一步确定功能模块之间调用的接口机制，以及软件层次结构之间功能模块调用的接口机制。此外，在具体的应用开发中可以参考软件设计模式的资料进行具体软件的接口设计，例如 WebService 应用。
- 与其他应用系统的接口设计。

#par(first-line-indent: 0em)[（4）软件的过程设计：]

- 功能模块的内部逻辑结构设计。根据已经确定的每个功能模块，进一步确定每个功能模块内部的处理逻辑，并结合软件局部数据结构的设计，完善每个功能模块的处理过程。
- 功能模块的处理能力设计。根据已经确定的模块内部的处理逻辑，进一步明确处理逻辑的响应时间、处理周期和吞吐量等性能指标。

#par(first-line-indent: 0em)[（5）软件的组件设计：根据需要并根据确定的接口设计原则封装一个或多个功能模块，并以特定的方式对外部提供服务，降低功能之间的耦合度。]

#par(first-line-indent: 0em)[（6）软件的结构优化设计：根据得到的软件设计初步模型，为了进一步满足显性或者隐性性能需求，对原有的软件结构进行调整的活动。]

== 软件设计的一般原则

软件设计既是过程，又是模型。设计过程是一系列迭代的设计活动，使设计者能够描述待构造软件的所有视图表示，即软件设计模型。然而，须注意的是，设计过程不仅是一种活动指南，只需按图索骥就可以设计软件的，还需要富有创造力的技能、以往的经验、对于形成"良好"软件的感觉及对质量的要求等因素都是设计成功的关键因素。

设计模型和建筑师的房屋设计图是类似的，它首先表示待构造事物的整体（例如房屋的三维表示），之后逐渐求精并细化事物以表示每个构造细节（例如管道布置、上下水通道）。同样，用于软件的设计模型提供计算机程序一系列不同维度的视图。Davis\[DAV95\]提出软件设计的一般原则，作为软件设计质量的评价依据。下面的内容对其进行修改和扩充：

- The design process should not suffer from "tunnel vision."
- The design should be traceable to the analysis model.
- The design should not reinvent the wheel.
- The design should "minimize the intellectual distance" between the software and the problem as it exists in the real world.
- The design should exhibit uniformity and integration.
- The design should be structured to accommodate change.
- The design should be structured to degrade gently, even when aberrant data, events, or operating conditions are encountered.
- Design is not coding, coding is not design.
- The design should be assessed for quality as it is being created, not after the fact.
- The design should be reviewed to minimize conceptual (semantic) errors.

正确应用上述设计原则时，软件开发人员创建的设计就会展现系统的外部和内部的质量因素。外部质量因素是用户能轻易观察到的软件特性（例如，正确性、可用性、速度和可靠性）。内部质量因素对软件工程师是非常重要的，从技术角度上能构造出高质量的软件设计。

#par(first-line-indent: 0em)[（1）*衡量设计过程的技术原则*。]

设计过程应该是可追踪和可回溯的。设计必须实现分析模型中描述的所有显式需求，必须满足用户希望的所有隐式需求。所谓隐式需求，即系统的安全性要求，降低或消除功能性错误，数据安全和完整性要求等。

对于开发者和未来的维护者而言，设计说明文档必须是可读、可理解的，使得将来易于编程，易于测试，易于维护。

#par(first-line-indent: 0em)[（2）*衡量设计模型的技术原则*。]

- 设计模型应该展现软件的全貌，包括从实现角度可看到的数据、功能、行为。
- 设计模型应该是一个分层结构。该结构：
  - 使用可识别的设计模式搭建系统结构。
  - 由具备良好设计特征的构件构成。
  - 可以用演化的方式实现。
- 设计应当模块化，即应当建立具有独立功能特征的构件。
- 设计应当建立能够降低模块与外部环境之间复杂连接的接口。
- 设计应当根据待实现的对象和数据模式导出合适的数据结构。

=== 软件模块化

#par(first-line-indent: 0em)[*（1）模块的定义。*]

整个软件被划分成若干单独命名和可编址的部分，称为模块。模块可以组装以满足整个问题域的需求。软件系统的层次结构是模块化的具体体现。

模块又称为构件，在传统的方法中指用一个名称就可调用的一段程序，类似于高级语言中的过程、函数等。它一般具有如下 3 个基本属性。

- *功能*：指该模块实现什么功能。
- *逻辑*：描述模块内部怎么做。
- *状态*：该模块使用时的环境和条件。

#par(first-line-indent: 0em)[*（2）模块的表示。*]

在描述一个模块时，还必须按模块的外部特性与内部特性分别描述。模块的外部特性是指模块的模块名、参数表、给程序乃至整个系统造成的影响。模块的内部特性则是指完成其功能的程序代码和仅供该模块内部使用的数据。

对于模块的外部环境（例如调用这个模块的上级模块）来说，只需了解这个模块的外部特性即可，不必了解它的内部特性。对于软件设计阶段，通常先确定模块的外部特性，然后确定它的内部特性。

#par(first-line-indent: 0em)[*（3）模块的划分。*]

一个大规模的软件，由于其总体结构复杂性，以及控制路径多、涉及问题范围广、变量多，使其相对于一个较小的软件而言难以被人们理解。在解决问题的实践中，如果把两个问题结合来作为一个问题来处理，其理解复杂性大于这两个问题被分开考虑时的理解复杂性之和。因此，把一个大而复杂的问题分解成一些独立且易于处理的小问题，解决起来容易得多。

进行问题分解时还必须注意，分解后的两个或多个小问题之间应该保持相对独立，即小问题之间的联系应该具有松散特性，否则导致分解问题本身的工作量比不分解问题所花费的工作量大得多，即接口复杂度增加。

基于上述考虑，把问题/子问题（功能/子功能）的分解与软件开发中的系统/子系统或者系统/模块对应起来，就能够把一个大而复杂的软件系统划分成易于理解且比较单纯的模块结构。所谓"比较单纯"，是指模块和其他模块之间的接口应尽可能独立。

实际上，如果模块相互独立，则模块变得越小，每个模块花费的工作量越低；当模块数增加时，模块间的联系也随之增加，把这些模块连接起来的工作量也增加。因此，如图 6-2 所示，存在一个模块个数 M，它使得总的开发成本最小。

#figure(
  image("../img/fig_6_2.png", width: 70%),
  caption: [模块大小、模块数目与费用的关系],
) <fig-6-2>

在考虑模块化时，可以参考如图 6-2 所示的曲线。注意，让划分出来的模块数处于 M 附近，避免划分出过多的模块或过少的模块。但是，如何才知道模块数已在 M 附近？应当如何把软件划分成模块？一个模块的规模应当由它的功能和用途决定。后续的章节介绍有助于确定合适的模块数目的设计方法，尤其是某个模块的扇入和扇出数目决定模块之间联接程度。

另外，对于一个系统，即使它必须"整体"实现，不能划分模块，也可以按照"模块化"的概念进行设计。例如，对于一个实时软件或一个微处理器软件，由于子程序调用可能导致速度太低或存储开销过大，就不适于划分模块。但是，软件也可以按模块化的原理进行设计，只是程序可以逐行编写，不划分成子程序。虽然从源程序上看不出模块，但是在程序结构设计上已经采用模块化的原理，因此这样的程序结构也具有模块化系统的优点。

模块化方法的优点：一方面，模块化设计降低系统的复杂度，使得系统容易修改；另一方面，推动系统各个部分并行开发，从而提高软件的生产效率。良好的模块设计方法的标准如下所述。

- *模块可分解性*：可将系统按问题/子问题分解的原则分解成系统的模块层次结构。
- *模块可组装性*：可利用已有的设计构件组装成新系统，不必一切从头开始。
- *模块可理解性*：一个模块可不参考其他模块而被理解。
- *模块连续性*：对软件需求的一些微小变更只导致对某个模块的修改而整个系统不大动。
- *模块保护*：将模块内出现异常情况的影响范围限制在模块内。

=== 信息隐藏

如何对一个软件的模块进行分解才能得到最佳的模块组合？为了明确怎么分解的方法，须了解 David Parnas 于 1972 年提出的"信息隐藏"概念。"信息隐藏"指每个模块的实现细节对于其他模块来说是隐蔽的，即模块中所包含的信息（包括数据和处理逻辑）不允许其他模块直接访问和修改。

通常，有效的模块化可以通过定义一组独立的模块来实现，这些模块相互间的通信仅使用于为实现软件功能来说是必要的信息。通过抽象，以确定组成软件的过程（或信息）实体。通过信息隐藏，则可定义和实施对模块的过程细节和局部数据结构的存取限制。

由于一个软件系统在整个软件生存期内须经过多次修改，所以在划分模块时须采取措施，使得大多数处理过程和数据对软件的其他部分是隐蔽的。这样，在将来修改软件时偶然引入错误所造成的影响就可以局限在一个或几个模块内部，不致波及软件的其他部分。

=== 模块的独立性

1978 年，Glenford J. Myers 在《Composite/Structured Design》一书中提出了"模块独立性"的概念，即模块间的内聚和模块的耦合。Larry Constantine 将模块的内聚和耦合分别建立 7 个度量准则。功能独立性是抽象、模块化和信息隐藏的直接产物。如果一个模块能够独立于其他模块被编程、测试和修改，而和软件系统中其他模块的接口是简单的，则该模块具有功能独立性。

*模块的耦合（Module Coupling）*：耦合是模块之间相对独立性（互相连接的紧密程度）的度量。模块之间的连接越紧密，联系越多，耦合性越高，而其模块独立性越弱。

*模块的内聚（Module Cohesion）*：内聚是模块功能强度（一个模块内部各个元素彼此结合的紧密程度）的度量。一个模块内部各个元素之间的联系越紧密，则它的内聚性越高；它与其他模块之间的耦合性减低，而模块独立性越强。因此，模块独立性比较强的模块应是高内聚低耦合的模块。

*1. 模块的内聚性*

内聚是模块功能强度的度量，用于表示一个模块内部各元素彼此结合的紧密程度。无论是结构化，还是面向对象的程序，一个模块（或是一个类）内部的功能具有相同或相似的目的，可以认为该模块（类）具有较高的内聚性。在理想情况下，一个内聚程度高的模块应当只负责一件事，比如拷贝（Copy）功能。一般模块的内聚性分为七种类型，如图 6-3 所示。

#figure(
  image("../img/fig_6_3.png", width: 85%),
  caption: [模块的内聚度量],
) <fig-6-3>

模块的内聚性是系统模块化设计中的一个关键因素，希望一个模块的内聚程度越高越好。通过图 6-3 可以看到，位于高端的几种内聚类型最好，位于中段的几种内聚类型是可以接受的，但位于低端的内聚类型很不好，一般不建议使用。模块的内聚性还可能由于以下两种情况而降低：

- 模块（或类）中定义多种不同用途的方法，且这些方法之间不具备共性。
- 模块（类）中的方法产生许多不同的输出结果，并且使用一些粗粒度和无关系的数据集合。

模块的内聚性降低导致以下软件结构的缺陷：

- 模块结构难以理解。
- 增加系统维护的难度，因为一个模块或者模块内部某个逻辑处理部分的改动引起其他相关联模块内部结构的改动。
- 降低模块的可复用程度。

内聚和耦合是相互关联的。在程序结构中各模块的内聚程度越高，模块间的耦合程度就越低。软件概要设计的目标是力求增加模块的内聚，尽量减少模块间的耦合。相对而言，增加模块的内聚比减少模块间的耦合更重要，因为模块之间不可能绝对独立，为此应当把更多的注意力集中到提高模块的内聚程度。

#par(first-line-indent: 0em)[*（1）巧合内聚（Coincidental Cohesion）*：几个模块内凑巧有一些程序段代码相同，又没有明确表现出独立的功能，程序员为了减少存储，把这些代码独立出来，建立一个新的模块，这个模块就是巧合内聚模块。它是内聚程度最低的模块。缺点是模块的内容不易理解，不易修改和维护。]

#figure(
  image("../img/fig_6_4.png", width: 60%),
  caption: [巧合内聚],
) <fig-6-4>

在图 6-4 中，模块 A、B 和 C 都包括 3 个同样的语句，而且在功能上并未给出该功能独立的含义，仅仅是为了节省空间，将这个同样的语句合并为一个新的模块 M。

此时，模块 B 由于应用上的需要，必须改动读取文件的方式，但模块 A 和模块 C 又不允许修改，这样在考虑各功能模块的关系时就陷入两难的境地。其次，这种模块由于没有明确的定义或含义，因此这种模块难以理解，难以描述它所完成的功能，反而增加程序的模糊程度。因此，在通常情况下应避免构造这种模块，除非系统受到存储空间的限制。

#par(first-line-indent: 0em)[*（2）逻辑内聚（Logical Cohesion）*：这种模块把几种相关的功能组合在一起，每次被调用时，由传送给模块的控制型参数来确定该模块应执行哪一种功能。逻辑内聚模块比巧合内聚模块的内聚程度高，因为它表明各部分之间在功能上的相关关系，如图 6-5 所示。]

#figure(
  image("../img/fig_6_5.png", width: 50%),
  caption: [逻辑内聚],
) <fig-6-5>

但是这种情况下它所执行的不是一种功能，而是若干功能中的一种，因此在需求变化较多的情况下，这种模块难以进行功能的修改。另外，在模块间进行调用时传递控制参数，这就相应地增加模块之间的耦合度。同时，在执行的过程中将不需要的代码部分调入内存，进而降低系统的执行效率。

#par(first-line-indent: 0em)[*（3）时间内聚（Classical Cohesion）*：这种模块大多为多功能模块，要求模块的各个功能必须在同一时间段内执行，例如初始化模块和终止模块。时间内聚模块比逻辑内聚模块的内聚程度稍高，因为时间内聚模块中所有部分都在同一时间内执行。在一般情形下，各部分可以以任意的顺序执行，所以它的内部逻辑更简单，存在的开关（或判定）转移更少。]

#par(first-line-indent: 0em)[*（4）过程内聚（Procedure Cohesion）*：一个模块由几个部分（子模块）组成，且通过一定的次序执行，这种模块称为过程内聚。这类模块的内聚程度比时间内聚模块的内聚程度更强。例如，模块首先检查一个数据文件的操作权限，然后执行文件的数据读取操作，那么这两个子模块属于过程内聚。另外，因为过程内聚模块仅包括完整功能的一部分，所以它的内聚程度仍然比较低，模块间的耦合程度还比较高。]

#par(first-line-indent: 0em)[*（5）通信内聚（Communicational Cohesion）*：如果一个模块内各子功能部分使用相同的数据，则称为通信内聚模块，有时也称为信息内聚。通常，通信内聚模块可以通过数据流图来确定的。如图 6-6 所示，虚线方框表示两个通信内聚模块。它们或者有相同的输入记录，或者有相同的输出结果。通信内聚模块的内聚程度比过程内聚模块的内聚程度高，因为通信内聚模块包括许多独立的功能。但是，由于模块中各功能部分使用相同的数据缓冲区，因而降低整个系统的效率。]

#figure(
  image("../img/fig_6_6.png", width: 75%),
  caption: [通信内聚],
) <fig-6-6>

#par(first-line-indent: 0em)[*（6）序列内聚（Sequential Cohesion）*：模块中某个子模块的输出数据是另一个子模块的输入数据。例如，一个模块从一个文件中读取数据，然后进行后续处理。序列内聚与过程内聚的差别在于过程内聚中的各子模块之间不一定传递数据，而序列内聚子模块之间须传递数据。]

#par(first-line-indent: 0em)[*（7）功能内聚（Functional Cohesion）*：一个模块中各个部分都是为完成一项具体功能而协同工作、紧密联系、不可分割的，则称该模块为功能内聚模块。功能内聚模块是内聚程度最强的模块。]

功能内聚模块的优点是它们容易修改和维护，因为它们的功能是明确的，模块间的耦合是简单的。但是，实践表明软件结构达到功能内聚比较困难，须付出很多附加的代价。通信内聚和序列内聚对于软件结构的设计而言是非常合适的，而且只要达到这个内聚程度，该软件结构可以认为是最佳的。巧合内聚和逻辑内聚一般是须特别注意避免产生的软件结构。

*2. 模块的耦合性*

相对于内聚讨论的是模块内部元素之间的紧密关系而言，耦合讨论的是模块之间连接的紧密程度的度量。它取决于各个模块之间接口的复杂程度、调用模块的方式及哪些信息通过接口。低耦合通常等同于高内聚，且具有一个很好的系统结构和优良的软件设计。模块的紧耦合对于软件结构的设计导致如下不良的影响：

#par(first-line-indent: 0em)[（1）一个模块的修改必定导致一系列与之相关联模块的改动，称为模块的涟漪效应。]

#par(first-line-indent: 0em)[（2）模块的整合则须花费更多的时间和努力。]

#par(first-line-indent: 0em)[（3）一个模块难以复用和测试。]

模块之间耦合性的七种度量如图 6-7 所示。

#figure(
  image("../img/fig_6_7.png", width: 85%),
  caption: [模块的耦合性度量],
) <fig-6-7>

#par(first-line-indent: 0em)[*（1）内容耦合（Content Coupling）*：有时也称为病态耦合，如果一个模块直接访问另一个模块的内部数据，参见图 6-8(a)，或一个模块不通过正常入口转到另一模块内部，或两个模块有一部分程序代码重叠，参见图 6-8(b)；或一个模块有多个入口，参见图 6-8(c)，则两个模块之间发生内容耦合。]

#figure(
  image("../img/fig_6_8.png", width: 80%),
  caption: [内容耦合],
) <fig-6-8>

对于内容耦合的情形，被访问模块的任何变更，或者用不同的编译器对它再编译，都会造成程序出错。这种耦合是模块独立性最弱的耦合。

#par(first-line-indent: 0em)[*（2）公共耦合（Common Coupling）*：有时也称为全局耦合，若一组模块都访问同一个公共数据环境，则它们之间的耦合就称为公共耦合。公共的数据环境可以是全局数据结构、共享的通信区、内存的公共覆盖区等。]

公共耦合的复杂程度随耦合模块的个数增加而显著增加。如图 6-9 所示，若只是两个模块之间有公共数据环境，则公共耦合有两种情况：松散公共耦合和紧密公共耦合。只有在模块之间共享的数据很多，且通过参数表传递不方便时，才使用公共耦合。

#figure(
  image("../img/fig_6_9.png", width: 80%),
  caption: [公共耦合],
) <fig-6-9>

公共耦合引起下列问题：

- 所有公共耦合模块都与某一个公共数据环境内部各项的物理安排有关，若修改某个数据的大小，则影响到所有的模块。
- 无法控制各个模块对公共数据的存取，严重影响软件模块的可靠性和适应性。
- 公共数据名的使用明显降低程序的可读程度。

#par(first-line-indent: 0em)[*（3）外部耦合（External Coupling）*：一组模块共享一个外部数据格式、通信协议或者设备接口定义，则称为外部耦合。外部耦合引起的问题类似于公共耦合，一般情况下多见于软件的模块与外部的工具或者设备进行通信连接。]

#par(first-line-indent: 0em)[*（4）控制耦合（Control Coupling）*：如果一个模块通过传送开关、标志、名字等控制信息，明显控制选择另一模块的功能，就是控制耦合。如图 6-10 所示。这种耦合的实质是在单一接口上选择多功能模块中的某项功能。因此，对被控制模块的任何修改都影响控制模块。另外，控制耦合也说明控制模块必须知道被控制模块内部的一些逻辑关系，这些都降低模块的独立性。]

#figure(
  image("../img/fig_6_10.png", width: 55%),
  caption: [控制耦合],
) <fig-6-10>

#par(first-line-indent: 0em)[*（5）标记耦合（Stamp Coupling）*：有时称为数据结构耦合，如果一组模块共享一组数据结构，且只使用数据结构中的某一部分，就是标记耦合。事实上，这组模块共享某一数据结构的子结构，而不是简单变量。这要求这些模块都必须清楚该记录的结构，并按结构要求对记录进行操作，如图 6-11 所示。]

#figure(
  image("../img/fig_6_11.png", width: 45%),
  caption: [标记耦合],
) <fig-6-11>

#par(first-line-indent: 0em)[*（6）数据耦合（Data Coupling）*：如果一个模块访问另一个模块，彼此之间是通过数据参数（不是控制参数、公共数据结构或外部变量）来交换输入、输出信息的，则称这种耦合为数据耦合，如图 6-12 所示。数据耦合是松散的耦合，模块之间的独立性比较强。]

#figure(
  image("../img/fig_6_12.png", width: 45%),
  caption: [数据耦合],
) <fig-6-12>

#par(first-line-indent: 0em)[*（7）非直接耦合（Non-direct Coupling）*：有时也称为消息耦合，如果两个模块之间没有直接关系，它们之间的联系完全是通过上级模块的控制和调用来实现的，这就是非直接耦合。这种耦合的模块独立性最强。]

以上 7 种耦合类型只是从耦合的机制上所划的分类，按耦合的松紧程度只是相对的关系。它给设计人员在设计程序结构时提供一个决策准则。实际上，开始时两个模块之间的耦合不只是一种类型，而是多种类型的混合。这就要求设计人员按照 Myers 提出的方法进行分析和比较逐步加以改进，以提高模块的独立性。

从原则上讲，模块化设计的最终目标是希望建立模块间耦合尽可能松散的系统，参见图 6-13。在这样一个系统中，设计、编码、测试和维护其中任何一个模块，无须对系统中其他模块有很多的了解。此外，由于模块间联系简单，发生在某一处的错误传播到整个系统的概率很小。因此，模块间的耦合情况很大程度影响到系统的可维护性。

#figure(
  image("../img/fig_6_13.png", width: 80%),
  caption: [松散的耦合类型],
) <fig-6-13>

模块之间的连接越紧密，联系越多，耦合性越高，而其模块独立性越弱。一个模块内部各个元素之间的联系越紧密，则它的内聚性越高；它与其他模块之间的耦合性减低，而模块独立性越强。因此，模块独立性比较强的模块应是高内聚低耦合的模块。

=== 模块耦合度计算

模块之间的耦合度计算公式为：

$ "Coupling"(C) = 1 - 1 / (d_i + 2 times c_i + d_o + 2 times c_o + g_d + 2 times g_c + w + r) $

式中，针对数据参数和控制参数的耦合项如下所述：

- $d_i$：数据参数的输入个数。
- $c_i$：控制参数的输入个数。
- $d_o$：数据参数的输出个数。
- $c_o$：控制参数的输出个数。

针对全局耦合项如下所述：

- $g_d$：作为数据的全局变量个数。
- $g_c$：作为控制的全局变量个数。

针对环境耦合项如下所述：

- $w$：调用的模块数，也称为扇出数。
- $r$：调用的模块数，也称为扇入数。

Coupling(C) 取值越大，说明模块之间的耦合度越大。根据经验，该公式的取值范围应该为 0.67（低耦合）到 1.0（高耦合）。举例说明如下，如果一个模块具有 5 个输入和输出的数据参数，以及相同数量的控制参数，访问 10 个全局数据变量，且该模块具有 3 个扇入数和 4 个扇出数，则根据公式可以得到 Coupling(C) = 0.98，说明该模块的和其他模块的耦合度非常高；如果一个模块只有一个简单的输入和输出数据参数，则根据公式可以得到 Coupling(C) = 0.67，说明这个模块具有最低的耦合度。

=== 降低模块间耦合度的方法

降低模块之间耦合的一种方式是功能设计（Functional Design），它根据功能的定义寻求一种限定模块职责的方式。如果两个模块（或类）A 和 B 之间存在以下关系，则 A 和 B 之间的耦合度增加：

#par(first-line-indent: 0em)[（1）A has an attribute that refers to (is of type) B.]

#par(first-line-indent: 0em)[（2）A calls on services of an object B.]

#par(first-line-indent: 0em)[（3）A has a method that references B (via return type or parameter).]

#par(first-line-indent: 0em)[（4）A is a subclass of (or implements) class B.]

在系统的模块化设计时，为了降低模块间的耦合度，可以参考以下 3 种方式。

#par(first-line-indent: 0em)[*（1）根据问题的特点选择适当的耦合类型。*]

在模块间传递的信息有两种：一种是数据信息，一种是控制信息。传送数据的模块耦合程度比传送控制信息的模块耦合程度低。

在模块调用时，传送的控制信息有两种：一种是传送地址，即调用模块直接转向所调用模块内部的某一地址。在这种情况下，一个模块的改动对其他模块有直接影响。另一种是传送判定参数，调用模块把判定参数传送给所调用模块，决定所调用模块如何执行。在这种情况下，模块间的耦合程度也很高，所以应当尽量减少和避免传送控制信息。

另一方面，不盲目地追求松散的耦合。例如，一个系统有上百种出错信息，若把它们集中放在一个错误处理模块中，通过调用模块传送错误类型到该模块的接口上，再进行处理就形成控制耦合。这样可以消除重复的信息，使所有错误信息格式标准化。所以，对于耦合类型的选择，应当根据实际情况，全面权衡，综合考虑。

#par(first-line-indent: 0em)[*（2）降低模块接口的复杂性。*]

模块接口的复杂性包括 3 个因素：一是传送信息的数量，即有关的公共数据与调用参数的数量；二是接口的调用方式；三是传送信息的结构。

在一般情况下，在模块的调用序列中若出现大量的参数，就表明所调用模块须执行许多任务。通过把这个所调用模块分解成更小的模块，使得每个小模块只完成一个任务，就可以减少模块接口的参数个数，降低模块接口的复杂性，从而降低模块间的耦合程度。

模块的调用方式有两种：call 方式和"直接引用"。前者使用标准的过程调用方式，模块间接口的复杂性较低，模块间的耦合程度低。后者是一个模块直接访问另一个模块内部的数据或指令，模块间的耦合程度高。所以，应当尽可能用 call 方式代替"直接引用"，以减少模块接口的复杂性。在参数类型上，尽量少使用指针、过程等类型的参数。

此外，在模块接口上传送的信息若能以标准直接的方式提供，则信息结构比较简单。若以非标准嵌套的方式提供，则信息结构比较复杂。例如，在模块中须调用画直线的命令 LINE，若命令要求直接给它直线两个端点的坐标 $(x_0, y_0), (x_1, y_1)$：

```
call LINE(x0, y0, x1, y1)
```

则接口复杂性比给 origin（起点）、end（终点）低。因为后者还定义 origin 和 end 的结构：

```
call LINE(origin, end)
```

#par(first-line-indent: 0em)[*（3）把模块的通信信息放在缓冲区中。*]

因为缓冲区可以看作是一个先进先出的队列，保持通信流中元素的顺序。沿着通信路径而操作的缓冲区减少模块间互相等待的时间。在模块化设计时，如果能够把缓冲区作为每次通信流的媒介，那么一个模块执行的速度、频率等问题一般不影响其他模块的设计。

== 面向对象设计原则

面向对象设计存在 7 个基本原则。在设计中应用这些原则，有助于正确地进行对象设计，提高设计模型的灵活性和可维护性，提高类的内聚度，降低类之间的耦合度。正确应用这些原则，可以提高设计的质量，解决微观上如何设计良好的用例实现方案的问题。

=== 单一职责原则

就一个类而言，应该仅有一个引起它变化的原因。

在单一职责原则（Single Responsibility Principle, SRP）中，将职责定义为"变化的原因"。如果有多于一个的原因改变一个类，那么这个类就具有多于一个的职责。类承担多个职责，等于这些职责耦合在了一起。一个职责的变化可能影响这个类完成其他职责的能力。因此，在构造对象时，应该将对象的不同职责分离至两个或多个类中，确保引起该类变化的原因只有一个，从而提高类的内聚度。

例如，考虑图 6-14 的设计。类"矩形"有两个方法。getArea() 用于计算矩形面积；draw() 用于在屏幕上绘制矩形。有两个不同的应用使用"矩形"类，类"计算面积应用"调用矩形的 getArea 方法，但是从来不在屏幕上绘制矩形；类"图形应用"在屏幕上绘制矩形，也可能调用 getArea 方法。

#figure(
  image("../img/fig_6_14.png", width: 55%),
  caption: [多于一个的职责],
) <fig-6-14>

这个设计违反单一职责原则。"矩形"类具有两个职责：第一个职责是提供矩形的面积计算；第二个职责是把矩形在一个图形用户界面上绘制出来。因违反 SRP 而导致一些严重的问题：

- 使"计算面积应用"类包含 GUI 代码。若是 C++，须将 GUI 代码链接进来，浪费链接时间、编译时间，同时占用内存；若是 Java，GUI 的 .class 文件必须被部署到目标平台。
- 如果"图形应用"类由于某些原因须修改 draw 方法，那么这个改变会迫使我们重新构建、测试和部署"计算面积应用"类，虽然"计算面积应用"类并不使用"矩形"类的绘制矩形功能。

一个较好的设计是把这两个职责分离到如图 6-15 所示的两个完全不同的类中。这个设计把"矩形"类中进行计算的部分移到"矩形面积"类中。此时，矩形绘制方式的变化不对"计算面积应用"类造成影响。

#figure(
  image("../img/fig_6_15.png", width: 55%),
  caption: [分离的职责],
) <fig-6-15>

另一个违反 SRP 的例子如图 6-16 所示。Employee 类包含了 CalculatePay（计算工资）和 Store（将 Employee 对象持久化）两个职责，其中前一个职责和具体业务规则相关，后一个职责是持久化相关的，和具体业务规则无关。这两个职责在大多数情况决不应该混合。因为业务规则往往频繁变化，而持久化的方式却不会如此频繁变化，并且变化的原因也是完全不同的，把业务规则和持久化绑定在一起的做法是自讨苦吃。应该将对 Employee 的持久化职责分配给另一个类。

#figure(
  image("../img/fig_6_16.png", width: 40%),
  caption: [被耦合在一起的持久化职责],
) <fig-6-16>

=== 开闭原则

软件实体（类、模块、函数等）应该可以扩展，但不可修改。

如果程序中的一处变化产生连锁反应，导致一系列相关模块的变化，那么这种设计不够灵活。应用开闭原则（Open Closed Principle, OCP）原则设计出的模块具有两个主要的特征，它们是：

- 对于"扩展"是开放的（Open for extension）。这说明模块的行为是可以扩展的。当需求改变时，对模块进行扩展，以满足需求的变化。
- 对于"更改"是封闭的（Close for modification）。其含义是对模块行为进行扩展时，不必改动客户端模块的源代码或者二进制代码。

这两个特征似乎相互矛盾。扩展模块行为的通常方式就是修改该模块的源代码。怎么可能在对模块进行扩展后无须改动客户端模块的源代码呢？

先考察图 6-17 的例子。类 ContactManager 中的方法 getContactMech 接收一个对象，返回联系方式。由于对象可能是 Phone 的对象，也可能是 Email 的对象，因此在 getContactMech 方法体里判断参数类型，从而决定返回电话号码还是 Email 地址。如果再增加其他新的通信方式，例如传真（Fax），则 getContactMech 的方法体里还须加入针对 Fax 的处理代码。因此，通信类的扩展对客户端类 ContactManager 来说是不封闭的，须修改 ContactManager 的代码。

#figure(
  image("../img/fig_6_17.png", width: 75%),
  caption: [不符合 OCP 的设计],
) <fig-6-17>

如何能在扩展模块功能（如增加 Fax 类）的同时不修改类 ContactManager 的代码？关键是抽象。通过抽象，可以发现类 Phone 和 Email 存在共同的行为，即都向 ContactManager 类提供联系方式；但提供联系方式这个共同行为的实现方法又有所不同，一个提供电话号码，另一个提供电子邮箱地址。此时，可以新建一个抽象类 ContactMech 作为 Phone 和 Email 的超类，如图 6-18 所示，将 Phone 和 Email 的提供联系方式这个共同行为抽象为 ContactMech 中的抽象操作 getContactMech，在 Phone 和 Email 中再分别对这个抽象操作进行不同的操作（Phone 返回电话号码，Email 返回电子邮箱地址）。同时，让 ContactManager 只依赖抽象类 ContactMech。这样，ContactManager 的 getContactMech 方法体只和抽象类 ContactMech 的对象有关系。此时，如果计划增加传真这种新的联系方式，只须新增一个 Fax 类，而其他的类无须任何变动从而实现 OCP。"开放"体现在系统可扩展，可以方便地增加新的通信方式。"封闭"体现在 ContactManager 对于通信方式的变化是封闭的；增加新的通信方式、从一种通信方式变到另一种通信方式，都不会影响它。

#figure(
  image("../img/fig_6_18.png", width: 55%),
  caption: [符合 OCP 的设计],
) <fig-6-18>

因此，符合 OCP 的程序只通过增加代码来变化，而不通过更改现有代码来变化，因此这样的程序不引起像非开放-封闭程序的连锁反应。实现 OCP 的关键是使用"抽象"来识别不同类之间的共性和变化点，利用封装技术对变化点进行封装。设计人员必须能猜测出最有可能发生的变化种类，然后构造"抽象"来隔离这些变化。如上例中不同沟通方式是一个变化点，所以构造抽象类 ContactMech 来隔离这个变化，让 ContactManager 只和抽象类交互。注意，只对程序中频繁变化的部分进行抽象，拒绝不成熟的"抽象"。

在进行面向对象设计时尽量考虑接口封装机制、抽象机制和多态技术。该原则同样适合于非面向对象设计的方法，这是软件工程设计方法的重要原则之一。

=== 里氏替换原则

子类应当可以替换父类并出现在父类能够出现的任何地方。

这是 Liskov 于 1987 年提出的设计原则，即里氏替换原则（Liskov Substitution Principle, LSP），原始定义如下：对于每一个类型 S 的对象 $o_1$，若都存在一个类型 T 的对象 $o_2$，使得在所有针对 T 编写的程序 P 中，用 $o_1$ 替换 $o_2$ 后，程序 P 的行为功能不变，则 S 是 T 的子类型。

由 LSP 可知：如果一个软件实体是父类型的，那么它出现的所有地方都可以被子类型实体替换。如图 6-19 所示，类 A 使用类 B（服务器类）的操作 A，类 C 是类 B 的子类，实际运行时，类 C 的实例可以替换类 B 的实例，完成操作 A 的功能。对于类 A 而言，这种替换过程是透明的。

#figure(
  image("../img/fig_6_19.png", width: 55%),
  caption: [里氏替换原则],
) <fig-6-19>

由于服务器类的扩展变化并不影响客户类，因此，在进行面向对象设计时，针对类的操作行为存在多种变化的情况下，尽量定义抽象类来为客户类提供服务，将操作行为的具体实现延迟到运行时刻绑定。

OCP 强调对变化的类进行抽象，不允许对抽象类进行修改，但允许对抽象类进行扩展；LSP 强调实现抽象化的具体规范。LSP 清楚地指出：继承关系是针对类型而言的，即就行为功能而言。行为功能不是内在且私有的，而是外在且公开的，是客户类所依赖的。所有子类的行为功能必须和客户类对其父类所期望的行为功能保持一致。

=== 依赖倒置原则

高层模块不应依赖低层模块。两者都应依赖"抽象"。

"抽象"不应依赖细节。细节应依赖"抽象"。

许多传统的软件开发方法，比如结构化分析和设计，总是倾向于创建一些高层模块依赖低层模块、策略依赖细节的软件结构。实际上这些方法的目的之一是定义子程序层次结构。

高层模块包含一个应用程序中重要的策略选择和业务模型。正是这些高层模块才使得其所在的应用程序区别于其他。然而，如果这些高层模块依赖低层模块，那么对低层模块的改动直接影响到高层模块，从而迫使它们依次改动。

这种情形非常不合理，本应该是高层的策略设置模块影响低层的细节实现模块的。包含高层业务规则的模块应该优先并独立于包含实现细节的模块。无论如何高层模块都不应依赖低层模块。

此外，开发人员更希望能够重用的是高层的策略设置模块。开发人员已经非常擅长通过子程序库的形式来重用低层模块。如果高层模块依赖低层模块，那么在不同的上下文中重用高层模块变得非常困难。然而，如果高层模块独立于低层模块，那么高层模块可以非常容易地被重用。该原则是框架设计的核心原则。

Booch 曾经说过："……所有结构良好的面向对象架构都具有清晰的层次定义，每个层次通过一个定义良好的、受控的接口向外提供一组内聚的服务。"对这个陈述的简单理解可能导致设计者设计出类似图 6-20 的结构分层包图。高层的 Policy Layer 依赖低层的 Mechanism Layer，而 Mechanism Layer 依赖更低层的 Utility Layer。这看起来似乎是正确的，然而却潜藏一个问题，即 Policy Layer 对于 Mechanism Layer 的改动是敏感的。而由于依赖关系是传递的，因此 Policy Layer 也依赖 Utility Layer，对于 Utility Layer 的改动也是敏感的。

#figure(
  image("../img/fig_6_20.png", width: 50%),
  caption: [一个简单的层次化方案],
) <fig-6-20>

图 6-21 展示一个更为合适的模型。每个较高层次都为它所需要的服务声明一个抽象接口，较低的层次实现这些抽象接口，每个高层类都通过该抽象接口使用下一层的服务，这样高层不依赖低层。低层反而依赖在高层中声明的抽象服务接口。这不仅解除 Policy Layer 对于 Utility Layer 的传递依赖关系，甚至也解除 Policy Layer 对于 Mechanism Utility Layer 的依赖关系。

#figure(
  image("../img/fig_6_21.png", width: 70%),
  caption: [倒置的层次],
) <fig-6-21>

注意：这里的"倒置"不仅是依赖关系的"倒置"，它也是接口所有权的"倒置"。通常认为服务类应该拥有各自的接口来让客户类调用。但是，应用 Dependency Inversion Principle（DIP）时，发现往往是客户类拥有抽象接口，而它们的服务类则从这些抽象接口派生。

DIP 还揭示的一条启发式规则，即依赖"抽象"，而不是具体的类。换言之，程序中所有的依赖关系应该终止于抽象类或者接口。根据这个启发式规则，可知：

- 任何变量都不应该持有一个指向具体类的指针或者引用。
- 任何类都不应该从具体类派生，或者继承自一个具体类。
- 任何方法都不应该覆盖它的任何基类中已经实现的方法。

当然，每个程序中都有违反该启发规则的情况。有时必须创建具体类的实例，而创建这些实例的模块依赖它们。此外，该启发规则对于那些具体稳定的类来说似乎不太合理。如果一个具体类不太改变，并且也不创建其他类似的派生类，那么依赖它并不会造成损害。比如，在大多数的系统中，描述字符串的类都是具体而且稳定的，因此直接依赖它不会造成损害。

然而，在应用程序中所编写的大多数具体类都是不稳定的。不可直接依赖这些不稳定的具体类。通过把它们隐藏在抽象接口的后面，可以隔离它们的不稳定状态。如图 6-22 所示，类 Copy 的职责是读取从键盘输入的数据，并输出到打印机。由于 Copy 类直接依赖具体类 KeyBoardReader 和 PrinterWriter，所以当输入源从键盘改成文件，或者输出目标从打印机改成磁盘文件时，须修改 Copy 类。这是非常困难的，因为输入输出设备的变化是很常见的。

#figure(
  image("../img/fig_6_22.png", width: 45%),
  caption: [依赖具体类],
) <fig-6-22>

图 6-23 展示一个更为合适的模型。在这个设计里，Copy 类只和抽象类 Reader 和 Writer 有关联，输入输出设备的变化不影响 Copy 类。这是依赖抽象原则的应用实例。

#figure(
  image("../img/fig_6_23.png", width: 45%),
  caption: [依赖"抽象"],
) <fig-6-23>

=== 接口隔离原则

采用多个与特定客户类有关的接口比采用一个通用的涵盖多个业务方法的接口好。

接口隔离原则（Interface Segregation Principle, ISP）的本质相当简单：如果一个服务器类为多个客户类提供不同的服务，那么服务器类应该为每一个客户类创建特定的业务接口，而不为所有客户类提供统一的业务接口，除非这些客户类请求的服务相同。如图 6-24 所示，客户 A、客户 B 和客户 C 分别需要服务实现类提供服务 A、服务 B 和服务 C，按照 ISP 原则，应该按照图 6-24(b) 方式为服务实现类设计 3 个不同的服务接口，不同的客户使用恰当的服务。图 6-24(a) 方式则将 3 种不同的服务以统一的服务抽象类为 3 个不同的客户类提供服务，尽管每个客户都能得到服务实现类提供的服务，但是违背 ISP 原则，带来的后果是：

- 当任何一个客户需要请求新的服务时，必须变更统一的服务抽象类，从而影响到其他客户的服务接口；同样，当服务实现类须与某个客户协商修改服务接口时，也影响到其他客户，即使这些客户不需要这些服务接口。
- 当需要扩展服务实现类时，新的服务实现类必须提供所有服务接口，如果新的服务实现类不能实现所有接口，就无法完成扩展。这种情况在图 6-24(b) 中是可以避免的，即单独提供服务 A、服务 B 和服务 C 的类也可以分别为客户 A、客户 B 和客户 C 提供扩展服务。

#figure(
  image("../img/fig_6_24.png", width: 85%),
  caption: [接口隔离原则],
) <fig-6-24>

从通俗意义上来讲，ISP 说明：向一个客户提供超过客户要求的服务承诺时，给服务提供方带来不必要的维护负担。

=== 组合/聚合复用原则

在一个新对象里使用一些已有的对象，使之成为新对象的一部分；新对象通过向已有的对象委托一部分责任而达到复用已有对象的目的。

面向对象设计有两种实现复用的手段：一种是继承，一种是组合/聚合。

*1. 继承复用*

可以通过从父类派生子类的方式实现复用，子类可以继承父类的行为和属性特征。继承复用必须遵循 LSP，即子类不可以破坏父类的行为语义。

#par(first-line-indent: 0em)[（1）继承复用的优点：]

- 子类实现较为容易，父类的大部分行为功能可以通过继承关系自动复用到子类。
- 在子类中修改或扩展从父类继承而来的功能比较容易，但必须遵循 LSP。

#par(first-line-indent: 0em)[（2）继承复用的缺点：]

- 继承复用破坏封装性。因为"继承"将父类的实现细节暴露给子类，而当子类修改父类的行为而破坏父类对外提供服务的语义时，LSP 被破坏，尽管在语法上子类可以替换父类出现的任何地方，但在语义上已经出现不可替换的问题，而且这种问题很难发现。由于父类的内部细节对于子类是透明的，所以继承复用又称为白盒复用。
- 如果父类改变，那么子类的实现方法必须改变。因此，继承复用限制父类更改。
- 从父类继承而来的实现方法是静态的，不可能在运行时刻改变，不足够灵活。
- 继承复用只能在一个具有相似语义类层次的上下文环境中使用。

*2. 组合/聚合复用*

通过将已有对象组合/聚合为一个新对象的方式实现对已有对象行为功能的复用。

#par(first-line-indent: 0em)[（1）组合/聚合复用的优点：]

- 新对象访问已有对象的唯一方法是通过已有对象的接口，因此，新对象无法知道已有对象内部实现细节，从而支持对象封装。正是因为新对象无法知晓已有对象的内部实现细节，所以组合/聚合复用又称为黑盒复用。
- 由于新对象将大部分职责委托给已有对象完成，因此，新对象可以将焦点集中在一个任务上，从而遵循 SRP。
- 组合/聚合复用可以在运行时刻动态进行，新对象可以动态引用与已有对象接口相同的对象。
- 作为复用手段可以应用到几乎任何环境中。

#par(first-line-indent: 0em)[（2）组合/聚合复用的缺点：]

- 新对象形成后，将另外已有的对象扩充到该新对象中比较困难，只能重新采用组合/聚合方法生成另外的新对象而扩充。
- 采用组合/聚合方法实现复用时产生大量的新对象，给对象管理带来困难。

组合/聚合复用原则（Composite/Aggregation Reuse Principle, CARP）：实现复用时应首先使用"组合/聚合"，其次才考虑"继承"。在使用"继承"时，严格遵循 LSP。如果两个类具有 has-a 关系，则应使用"组合/聚合"；如果具有 is-a 关系，则可使用"继承"。is-a 在严格的分类学意义上定义，表示一个类是另一个类的"一种"，而 has-a 则不同，它表示某个角色具有某项责任。有效地使用"继承"有助于理解问题，降低复杂度，而滥用"继承"则增加系统构建、维护时的难度及系统的复杂度。

=== 迪米特法则

最少知识原则（迪米特法则，Law of Demeter）：一个对象应当尽可能少地了解其他对象。只与你直接的朋友们通信，不与陌生人说话。符合下列条件的对象即为朋友：

- 当前对象本身（this）。
- 以参量形式传入到当前对象方法中的对象。
- 当前对象的实例变量直接引用的对象。
- 当前对象的实例变量如果是一个"聚集"，那么"聚集"中的元素也都是朋友。
- 当前对象所创建的对象。

== 软件设计基础

=== 自顶向下，逐步细化

Niklaus Emil Wirth 提出的设计策略：将软件的体系结构按自顶向下方式，对各个层次的过程细节和数据细节逐层细化，直到用程序设计语言的语句能够实现为止，从而最后确立整个的体系结构。最初的说明只是概念地描述系统的功能或信息，但并未提供有关功能的内部实现机制或有关信息内部结构的任何信息。设计人员对初始说明认真推敲，进行功能细化或信息细化，给出实现的细节，划分出若干成分。然后，再对这些成分施行同样的细化工作。随着细化工作的逐步展开，设计人员能得到越来越多的细节。

=== 系统控制结构

系统控制结构表明程序构件（模块）的组织情况。控制层次往往用程序的层次（树形或网状）结构来表示，如图 6-25 所示。位于最上层根部是顶层模块，是程序的主模块。与其联系的有若干下属模块，各下属模块还可以进一步引出更下一层的下属模块。模块 M 是顶层模块，如果视为第 0 层，则其下属模块 A、B 和 C 为第 1 层，模块 D、E、K、L 和 N 是第 2 层等。

#figure(
  image("../img/fig_6_25.png", width: 80%),
  caption: [程序的层次结构图示例],
) <fig-6-25>

- *程序结构的深度*：程序结构的层次数称为结构的深度。结构的深度在一定意义上反映程序结构的规模和复杂程度。
- *程序结构的宽度*：层次结构中同一层模块的最大模块个数称为结构的宽度。
- *模块的"扇入"和"扇出"*："扇出"表示一个模块直接调用（或控制）的其他模块数目；"扇入"则定义为调用（或控制）一个给定模块的模块个数。多"扇出"则说明控制和协调许多下属模块，而多扇入的模块通常是公用模块。

注意，程序结构是软件的过程表示，但并未表明软件的某些过程性特征，比如进程序列、事件/决策的顺序或其他的软件动态特性。

=== 结构划分和结构图

程序结构可以按水平方向或垂直方向进行划分。水平划分按主要的程序功能来定义模块结构的各个分支。顶层模块是控制模块，用来协调程序各个功能之间的通信和运行。其下级模块最简单的水平划分方法是建立 3 个分支：输入、处理（数据变换）和输出。这种划分的优点是：主要的功能相互分离，易于修改，易于扩充，而且没有副作用。缺点是：通过模块接口传递更多的数据，使程序流的整体控制复杂化。

垂直划分也称为因子划分，主要用在程序的体系结构中，且工作自顶向下逐层分布：顶层模块执行控制功能，较少处理实际的工作，而低层模块是实际输入、计算和输出的具体执行者。这种划分的优点是：对低层模块的修改不太可能引起副作用，而恰恰对计算机程序的修改常发生在低层的输入计算或输出模块中。因此，程序的整体控制结构不太可能修改，便于将来维护。

结构图是精确表达程序结构的图形表示方法。它清楚地反映程序中模块间的层次调用关系和联系：不仅严格地定义各个模块的名称、功能和接口，而且还反映设计思想。它以特定的符号表示模块、模块间的调用关系和模块间信息的传递方式。结构图的主要内容有 3 种。

*模块*：在结构图中，模块用矩形框表示，并用模块的名称标记。模块的名称应当能够表明该模块的功能。对于现成的模块，则以双纵边矩形框表示，如图 6-26 所示。

#figure(
  image("../img/fig_6_26.png", width: 75%),
  caption: [模块的表示图],
) <fig-6-26>

*模块的调用关系和接口*：在结构图 6-27 中，两个模块之间用单向箭头连接。箭头从调用模块指向所调用模块，表示调用模块调用所调用模块，其中隐含一层意思，即执行所调用模块完成之后，控制信息返回调用模块。图 6-27(a) 表示模块 A 调用模块 B。

#figure(
  image("../img/fig_6_27.png", width: 80%),
  caption: [模块间的调用关系和接口表示图],
) <fig-6-27>

*模块间的信息传递*：当一个模块调用另一个模块时，调用模块把数据和控制信息传送给所调用模块，以使所调用模块能够运行，而在执行所调用模块的过程中又把它产生的数据或控制信息回送给调用模块。为了表示在模块之间传递的数据和控制信息，在联结模块的箭头旁边另给出箭头，并且用尾端带有空心圆的短箭头表示数据信息，用尾端带有实心圆的短箭头表示控制信息，通常在短箭头附近应注信息的名称，如图 6-27(b) 所示。

两个辅助符号如图 6-28 所示，当模块 A 有条件地调用另一个模块时，在模块 A 的箭头尾部标以一个菱形符号。当一个模块 A 反复地调用模块 C 和模块 D 时，在调用箭头尾部则标以一个弧形符号。

#figure(
  image("../img/fig_6_28.png", width: 45%),
  caption: [条件调用和循环调用],
) <fig-6-28>

=== 数据结构

数据结构是数据各个元素之间逻辑关系的一种表示方法。数据结构设计应确定数据的组织、存取方式、相关程度，以及信息的不同处理方法。数据结构的组织方法和复杂程度可以灵活多样，但典型的数据结构种类是有限的，它们是构成一些更复杂结构的基本构件块。图 6-29 表示这些典型的数据结构。

#figure(
  image("../img/fig_6_29.png", width: 85%),
  caption: [典型的数据结构],
) <fig-6-29>

标量是最简单的一种数据结构。所谓"标量"就是单个的数据元素，例如一个布尔量、整数、实数或一个字符串。可以通过名称对它们进行存取。

若把多个标量项组织成一个表或者顺序邻接为一组时，就形成顺序向量。顺序向量又称为一维数组，通常可以通过下标及数组名来访问数组中的某一元素。把顺序向量扩展到二维、三维，直至任意维，形成 n 维向量空间。最常见的 n 维向量空间是二维和矩阵。

链表是一种更灵活的数据结构，它把不相邻的标量项、向量或空间结构用拉链指针链接，使得它们可以像表一样处理。

组合上述基本数据结构可以构成其他数据结构。例如，可以用包含标量项、向量或 n 维空间的多重链表来建立分层结构和网络结构。利用它们又可以实现多种集合的存储。

必须注意，数据结构和程序结构一样，可以在不同的抽象层次上表示。例如，栈是一种线性结构的逻辑模型，其特点是只允许在结构的一端进行插入或删除运算。它可以用向量实现，也可以用链表实现。

=== 软件过程

程序结构描述整个程序的控制层次关系和各个部分的接口情况，如图 6-30 所示的软件过程则着重描述各个模块的处理细节。

#figure(
  image("../img/fig_6_30.png", width: 80%),
  caption: [一个模块内的软件过程],
) <fig-6-30>

软件过程必须提供精确的处理说明，包括事件的顺序、正确的判定点、重复的操作，直至数据的组织和结构等。程序结构与软件过程是有关系的。对每个模块处理时，必须指明该模块所在的上下级环境。软件过程遵从程序结构的主从关系，因此它也是层次化的。

== 软件体系结构简介

=== 软件体系结构概述

软件工程的研究起源于 20 世纪 60 年代的软件危机。最初的软件设计重点在数据结构和算法的研究上，但随着软件系统规模越来越大，越来越复杂，现有的软件工程的方法显得力不从心。对于大规模的复杂软件系统来说，系统的总体结构设计和规格说明比数据结构和算法选择重要得多。在这种背景下，人们认识到软件体系结构的重要作用，并认为对软件体系结构进行系统、深入的研究将成为提高软件生产率和解决软件维护问题最有希望的方向。

软件体系结构虽然脱胎于软件工程，但其形成时同时借鉴计算机体系结构和网络体系结构中很多的思想和方法，近几年软件体系结构的研究已完全独立于软件工程的研究，成为计算机科学一个最新的研究方向和独立的学科分支。

软件体系结构研究的主要内容涉及软件体系结构描述、软件体系结构风格、软件体系结构评价和软件体系结构的形式化方法等。其根本目的是解决软件重用、软件质量和软件维护问题。

=== 软件体系结构的定义

虽然软件体系结构的研究成果已经广泛应用于软件工程，但时至今日，学术界对于软件体系结构的定义还没有一个统一的意见。许多专家学者从不同的角度和侧面对软件体系结构给出了不同的定义。

*1. Booch、Rumbaugh 和 Jacobson 定义*

软件体系结构是一系列重要决策的集合，这些决策与软件组织、构成系统的结构元素及其接口，以及这些元素在相互协作中表现的行为有重要的关系。这些结构元素和行为元素进一步组合构成更大规模的子系统，并进一步组合、组织成为体系结构风格。

*2. Bass 定义*

Bass 等在《Software Architecture in Practice》一书中提出，程序和计算机系统的软件体系结构是系统的一个或多个结构（Structure），包括软件构件（Components）、构件的外部可视属性（Properties）和构件之间的关系（Relationships）。其中，外部可视属性是指软件构件所提供的服务性能、特性、错误处理、共享资源使用等。

这个定义在软件体系结构中引入"构件"的概念，进而描述构件间如何相互交互，这说明体系结构略去那些仅与某构件自身有关的信息。同时，这个定义明确指出系统可以包含多个结构，但其中的任何一个都不能称为体系结构。这个定义还说明每一个软件系统都有一个体系结构。

*3. Shaw 定义*

在第一届软件体系结构国际讨论会上，Mary Shaw 对于当时术语使用的混乱情况予以澄清，整理出定义和观点，并对当时的各种观点作如下的分类。

- *结构模型*：结构模型认为，软件体系结构由构件、构件之间的连接和一些其他方面组成。
- *框架模型*：框架模型的观点与结构模型相似，但其重点在于整个系统的连贯结构（这种结构通常是唯一的），这与重视其组成恰好相反。框架模型常以某种特定领域或某类问题为目标，例如 CORBA 或基于 CORBA 的软件体系结构模型和特定领域的构件仓库。
- *动态模型*：动态模型强调系统的行为质量。"动态"可以有多种含义：可以指整个系统配置的变化，也可以是禁止预先激活的通信或交互，还可以是计算中表现的动态特性，如改变数据的值。
- *过程模型*：过程模型关注系统结构的构建及其步骤和过程。在这一观点下，体系结构是软件开发所进行一系列过程的结果。

*4. Garlan 和 Shaw 定义*

软件体系结构 = {构件，连接件，约束}

构件可以是一组代码，也可以是一个独立的程序。构件是一组对象的集合，可以实现某些计算逻辑。这些对象或是结构相关或是逻辑相关。构件相对独立，仅通过接口与外部进行交互，可作为独立单元嵌入到不同的应用系统中。

连接件可以是过程调用、管道、远程过程调用等，用于表示构件之间的相互作用。连接件把不同的构件连接起来，构成体系结构的一部分。连接件也是一组对象，表现为框架式对象或转换式对象等。

"约束"一般为对象连接时的规则，或指明构件连接的条件：如上层构件可要求下层构件的服务，反之不可；两个对象不得递归地发送消息；在什么条件下此种连接无效等。

Garlan 和 Shaw 认为软件体系结构是软件设计过程的一个层次，这一层次超越计算过程中的算法设计和数据结构设计。体系结构问题包括总体组织和全局控制、通信协议、同步、数据存取，以及给设计元素分配特定功能，也包括设计元素的组织、规模和性能，以及在各设计方案间进行选择等。软件体系结构处理算法与数据结构中关于整体系统结构设计和描述方面的一些问题，如全局组织和全局控制结构，关于通信、同步与数据存取的协议，涉及构件功能定义，物理分布与合成，设计方案的选择、评估与实现等。

=== 软件体系结构三要素

软件体系结构的三要素是程序构件（模块）的层次结构、构件之间交互的方式，以及数据的结构。软件设计的一个目标是建立软件的体系结构表示。将这个体系结构表示当作一个框架，以从事更详细的设计活动。Shaw 和 Garlan 提出在软件体系结构设计中应保持的 3 个性质：

#par(first-line-indent: 0em)[（1）*结构*。体系结构设计应当定义系统的构件，以及这些构件打包的方式和相互交互的方式，如将对象打包以封装数据和操作数据，并通过相关操作调用来进行交互。]

#par(first-line-indent: 0em)[（2）*附属的功能*。体系结构设计应当描述设计出来的体系结构如何实现功能、性能、可靠性、安全性、适应性，以及其他的系统需求。]

#par(first-line-indent: 0em)[（3）*可复用*。体系结构设计应当描述为一种可复用的模式，以便在以后类似的系统族设计中使用它们。此外，设计应能复用体系结构中的构造块。]

表 6-1 列出可能的软件构件，表 6-2 列出可能的构件间的连接方式。

#figure(
  table(
    columns: (auto, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [*构件*], [*特点和示例*],
    [纯计算构件], [具有简单的输入/输出关系，没有运行状态的变化。例如，数值计算、过滤器（Filters）、转换器（Transformers）等],
    [存储构件], [存放共享、永久、结构化的数据。例如，数据库、文件、符号表、超文本等],
    [执行构件], [执行的操作与运行状态紧密耦合。例如，抽象数据类型（ADT）、面向对象系统中的对象、服务器（Servers）等],
    [控制构件], [管理其他构件运行的时间、时机及次序。例如，调度器、同步器等],
    [链接构件], [在实体之间传递信息。例如，通信机制、用户界面等],
  ),
  caption: [软件构件分类],
) <tab-6-1>

#figure(
  table(
    columns: (auto, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [*连接方式*], [*说明*],
    [过程调用], [在某一个执行路径中传递执行指针。例如，普通过程调用（同一个命名空间）、远程过程调用（不同的命名空间）],
    [数据流], [相互独立的处理通过数据流进行交互，在得到数据的同时被赋予控制权限。例如，UNIX 系统中的管道],
    [间接激活], [处理是因事件的发生而激活的，在处理之间没有直接交互。例如，事件驱动系统、自动垃圾回收等],
    [消息传递], [相互独立的处理之间明确交互，通过显式离散方式的数据传递。这种传递可以是同步的，也可以是异步的。例如，TCP/IP],
    [共享数据], [构件通过同一个数据空间进行并发的操作。例如，多用户数据库、数据黑板系统],
  ),
  caption: [构件之间的连接方式],
) <tab-6-2>

=== 软件体系结构在软件设计阶段的作用

软件体系结构的选择很大程度上取决于应用系统的特点，为此主要阐述迄今为止软件行业通用的一些体系结构的风格和特点。软件体系结构的选择和使用在某种程度上还体现软件开发团队对于软件技术架构的熟练程度。

当软件的需求分析阶段结束，甚至于需求分析阶段中的关键需求和技术特性要求确定后，技术团队就可以着手进行技术架构的选择和确定工作。一旦确定软件体系结构，无论采用何种软件设计方法，都遵循该体系结构的框架和结构进行后续的系统结构和功能结构的设计工作。采用和迭代模型进行开发时，尽早确定软件体系结构就能快速和及时地构造系统原型，并通过原型的反馈及时纠正技术架构存在的问题。

除此之外，一个成熟的软件体系结构是经过实际项目检验的，也是新的软件项目成功和质量的保障依据。

=== 软件体系结构风格

软件体系结构设计的一个核心问题是能否使用重复的体系结构模式，即能否达到体系结构级的软件重用，即能否在不同的软件系统中使用同一体系结构。基于这个目的，学者们开始研究和实践软件体系结构的风格和类型问题。

软件体系结构风格是描述某一特定应用领域中系统组织方式的惯用模式（idiomatic paradigm）。体系结构风格定义一个系统家族，即一个体系结构定义一个词汇表和一组约束条件。词汇表包含一些构件和连接件类型，而这组约束条件指出系统是如何将这些构件和连接件组合起来的。体系结构风格反映领域中众多系统所共有的结构和语义特性，并指导如何将各个模块和子系统有效地组织成一个完整的系统。按这种方式理解，软件体系结构风格定义用于描述系统的术语表和一组指导构件系统的规则。

对软件体系结构风格的研究和实践促进对设计的重用，一些经过实践证实的解决方案也能可靠地用于解决新的问题。体系结构风格的不变部分使不同的系统可以共享同一个实现代码。

体系结构风格具有 4 个主要元素，即提供一个词汇表、定义一套配置规则、定义一套语义解释原则和定义对基于这种风格的系统所进行的分析。Garlan 和 Shaw 根据此框架给出通用体系结构风格的分类。

*1. 管道和过滤器风格*

管道/过滤器风格最早出现在 UNIX 系统中，已经有超过 40 年的历史。它适用于对有序数据进行一系列已经定义的独立计算的应用程序。如图 6-31 所示，这种风格的特征包括如下两点。

#figure(
  image("../img/fig_6_31.png", width: 80%),
  caption: [管道和过滤器风格体系结构],
) <fig-6-31>

#par(first-line-indent: 0em)[（1）*构件*：在管道和过滤器风格中，构件被称为过滤器。它对输入流进行处理、转换，处理后的结果在输出端流出。这种计算处理方式是递进的，所以可能在全部的输入接受完之前开始输出。此外，系统可以并行地使用过滤器。]

#par(first-line-indent: 0em)[（2）*连接件*：连接件位于过滤器之间，起到信息流的导管作用，称为管道。]

每个构件都有输入和输出，构件在输入处读取数据流，并在输出处生成数据流。

过滤器必须是独立的实体，它们不了解信息流从哪个过滤器流出，也不知道信息流流入哪个过滤器。可以指定输入的格式，并确保输出的结果，但是它们可能不知道在管道之后是什么样的构件。过滤器之间不共享状态。

管道和过滤器构成的网络输出正确与否不依赖过滤器的递进处理顺序。

常见特殊化的管道和过滤器体系结构有以下 3 种。

#par(first-line-indent: 0em)[（1）*管道线*：体系结构的拓扑被限制为过滤器的线性序列。]

#par(first-line-indent: 0em)[（2）*有界管道*：对管道中一次流过的数据数量进行限制。]

#par(first-line-indent: 0em)[（3）*批处理*：当每个过滤器作为一个单独的实体处理它的所有输入时，这个管道和过滤器结构退化成为一个批处理顺序系统。]

管道和过滤器风格的软件体系结构具有以下优点：

- 软构件具有良好的隐蔽性和高内聚、低耦合的特点。
- 允许设计者将整个系统的输入/输出行为看成是多个过滤器行为的简单合成。
- 支持软件重用。只要提供适合在两个过滤器之间传送的数据，任何两个过滤器都可被连接。
- 系统维护和增强系统性能简单。新过滤器可以添加到现有系统中，旧过滤器可以被移除。
- 允许对吞吐量、死锁等属性进行分析。
- 支持并行执行。每个过滤器作为一个单独的任务完成，因此可与其他任务并行执行。

但是，这样的系统也存在若干不利因素：

- 通常导致进程成为批处理的结构。这是因为虽然过滤器可增量式地处理数据，但它们是独立的，所以设计者必须将每个过滤器看成是一个完整的从输入到输出的转换工具。
- 不适合处理交互的应用。当须增量地显示变化时，这个问题尤为严重。
- 在处理两个独立，但相关的数据流之间的同步时可能遇到困难。
- 在数据传输上没有通用的标准，每个过滤器都增加解析和合成数据的工作。

管道和过滤器体系结构最著名的例子就是 UNIX 的 shell 程序。比如，这样一个 shell 命令：`cat file | grep xyz | sort | uniq > out`，系统首先将文件中查找含有 xyz 的行，然后进行排序，再去掉相同的行，最后结果放到 out 中。各个 UNIX 进程作为构件，管道在文件系统中创建。编译器也是个典型的例子，词法分析—句法分析—语义分析—代码生成。其他的例子包括信号处理系统、并行计算等。

*2. 调用和返回风格*

调用/返回风格的体系结构在过去的 30 年之间占有重要的地位，是大型软件开发中的主流风格的体系结构。这类系统中呈现出比较明显的调用/返回关系，分为以下 3 种子风格。

#par(first-line-indent: 0em)[（1）*主/子程序风格的体系结构*：是一种经典的编程范型，主要应用在结构化程序设计中。这种风格的主要目的是将程序划分为若干个小片段，从而使程序更加可更改。这种风格有一定的层次，主程序位于一层，下面可以再划分一级子程序、二级子程序甚至更多。主/子程序体系结构风格是单线程控制的。同一时刻只有一个子结点的子程序可以得到父结点程序的控制。该风格的特点：]

- 单线程控制，计算的顺序得以保障。
- 有用的计算结果在同一时刻只产生一个。
- 单线程控制，可以直接由程序设计语言来支持。
- 分层推理机制，子程序正确与否与它调用的子程序正确与否有关。

#par(first-line-indent: 0em)[（2）*对象风格的体系结构*：这种风格建立在数据抽象和面向对象的基础上，数据的表示方法和它们的相应操作封装在一个抽象数据类型或对象中。这种风格的构件是对象，或者是抽象数据类型的实例。对象是一种称作管理者的构件，它负责保持资源完整（例如实现对属性方法的封装）。对象是通过函数和过程调用来交互的，适用于以相互关联的数据实体标识和保护为中心问题的应用程序。对象风格的体系结构具有以下的特点：]

- 对象抽象使得构件和构件之间的操作以黑箱的方式进行。
- "封装"使得细节内容对外部环境得以良好地隐藏。对象之间的访问是通过方法调用来实现的。
- 考虑操作和属性的"关联"，封装完成相关功能和属性的包装，并由对象来对它们进行管理。
- 使用某个对象提供的服务，并无须知道服务内部是如何实现的。

数据抽象和面向对象风格的体系结构在现代的软件开发中广泛应用。但是，面向对象体系结构也存在某些问题：

- 对象之间的耦合度比较紧。为了使一个对象和另一个对象通过过程调用等进行交互，必须知道对象的标识。只要一个对象的标识改变，就必须修改所有其他明确调用它的对象。
- 必须修改所有显式调用它的其他对象，并消除由此带来的一些副作用。例如 A 使用对象 B，C 也使用对象 B，那么 C 对 B 的使用所造成对 A 的影响可能是不可预测的。

#par(first-line-indent: 0em)[（3）*分层风格的体系结构*：将系统组织成一个层次（layer）结构，每一层为上层提供服务，并作为下层的客户端。在分层风格的体系结构中，一般内部的层只对相邻的层可见。层之间的连接器（connector）通过决定层间如何交互的协议来定义。]

这种风格支持基于可增加抽象层的设计。这样，允许将一个复杂问题分解成一个增量步骤序列的"实现"。由于每一层最多只影响两层，同时只要给相邻层提供相同的接口，允许每层用不同的方法实现，同样为软件复用提供强大的支持力度。分层风格的体系结构有许多可取的属性：

- 支持基于抽象程度递增的系统设计：使设计者可以把一个复杂系统按递增的步骤进行分解。
- 支持功能增强：因为每一层至多和相邻的上下层交互，因此改变的功能最多影响相邻的上下层。
- 支持复用：只要提供的服务接口定义不变，同一层的不同实现可以交换使用。这样，就可以定义一组标准的接口，而允许各种不同的实现方法。

分层风格的体系结构也有不足之处：

- 并不是每个系统都可以很容易地划分为分层风格的体系结构，甚至即使一个系统的逻辑结构是层次化的，出于对系统性能的考虑，系统设计师必须把一些低级或高级的功能综合起来。
- 很难找到一个合适、正确的层次抽象方法。

*3. 基于事件的风格*

基于事件（event-based）的风格，又称为隐式调用（Implicit Invoke）的风格。如图 6-32 所示，在此风格的系统结构中，构件并不直接调用一个过程，而是声明或广播一个或多个事件。系统中的其他构件可以把一个过程注册为与它所关心的事件相关联。当某一事件发生时，系统调用所有与之相关联的过程，即一个事件的触发隐含地导致对其他模块的过程调用，实际上发挥隐式调用的作用。

#figure(
  image("../img/fig_6_32.png", width: 80%),
  caption: [基于事件的体系结构风格],
) <fig-6-32>

在基于事件的体系结构中，构件的接口不仅提供一个过程的集合，也提供一个事件的集合。这些过程既可以用一般的方式调用，也可能被注册为与某些事件相关。构件可以声明或广播一个或多个事件，或者向系统注册用以表明它希望响应一个或多个事件。连接件的两种类是：对事件的显式调用和对事件的隐式调用。

这种风格适用于设计低耦合构件集合的应用程序，其中每个构件完成一定的操作，并可能触发其他构件的操作。对于必须动态重配置的应用程序尤其有用，因为在这种风格的体系结构中服务提供者便于维护和修改，也易于启动或停止某些功能。

基于事件风格的优点表现在：

- 事件广播者不必知道哪些部件被构件影响，构件之间的关系较弱。
- 隐式调用有助于软件复用，因为它允许任何构件注册其相关的事件。
- 由于可以在不影响其余构件的情况下替换某个构件，因而系统的演化、升级变得简单。

基于事件风格的缺点表现在：

- 构件对系统进行的计算放弃主动控制。一个构件不能假设其他构件对它的请求进行响应，也不能得知事件处理的先后顺序。在有共享数据存储的系统中，资源管理器的性能和准确度成为十分关键因素。
- 很难对系统的正确程度进行推理，因为声明或广播某个事件过程的含义依赖它被调用时的上下文环境。

基于事件风格的体系结构最早出现在守护进程、约束满足性检查和包交换网络等方面的应用程序中。它经常用于如下领域中：

- 在程序设计环境中用于集成各种工具。
- 在数据库管理系统中用于检查数据库的一致性约束条件。
- 在用户界面中分离数据和表示。

*4. 客户端服务器风格*

客户端服务器风格设计的目标是达到可测量的需求，并适用于应用程序的数据和处理分布在一定范围内的多个构件上，且构件之间通过网络连接，如图 6-33 所示。服务器用于向一个或者多个客户端提供数据服务，这个环境经常与网络连接一起出现。客户端向服务器发出一个请求，服务器同步或者异步执行客户端的请求。如果工作是同步的，服务器返回给客户端数据和客户端控制权；如果是异步的，服务器只返回给客户端数据，因为这时服务器有控制线程。

#figure(
  image("../img/fig_6_33.png", width: 55%),
  caption: [客户端服务器风格],
) <fig-6-33>

有两种常见的客户端服务器风格的体系结构：代理风格（broker）和 P2P（peer-to-peer）风格。在代理风格中，服务器将其服务发布给一个代理，客户端向代理访问服务，如图 6-34 所示。代理风格的典型例子有 CORBA、SOAP、Web Service 等。P2P 风格的特点是对称的客户端服务器模型，典型的例子有 Window 系统（X11）。

客户端服务器风格的优点有：

- 有利于分布式的数据组织。
- 构件间是位置透明的，客户端和服务器都不考虑对方的运行位置。
- 便于异构平台融合与匹配，客户端和服务器可以运行在不同的操作系统上。
- 具有良好的扩展性，易于对服务器进行维护和修改或增加服务器。

客户端服务器风格的缺点表现在：

- 客户端必须知道服务器的访问标识，否则很难知道哪些服务可以使用。

*5. 解释器风格（虚拟机）*

解释器风格（Interpreters）通常用于建立一种虚拟机，用来弥合程序的语义与作为计算引擎的硬件差异，如图 6-35 所示。由于解释器实际上创建了一个软件虚拟出来的硬件机器，所以这种风格又称为虚拟机（Virtual Machine）风格。这种风格适用于应用程序不能直接运行在最合适的机器上或不能直接以最适合的语言执行，比如：

- 程序设计语言的编译器，如 Java 和 Smalltalk 等。
- 基于规则的系统，比如专家系统领域的 Prolog 等。
- 脚本语言，比如 Awk 和 Perl 等。

#figure(
  image("../img/fig_6_34.png", width: 45%),
  caption: [代理风格],
) <fig-6-34>

#figure(
  image("../img/fig_6_35.png", width: 55%),
  caption: [解释器风格的体系结构],
) <fig-6-35>

解释器风格的体系结构通常包括一个作为执行引擎的状态机和 3 个存储器，即体系结构由 4 个构件组成：正在被解释的程序、执行引擎、被解释的程序状态、执行引擎的当前状态。连接件包括过程调用和直接存储器访问。

解释器风格的体系结构的优点：

- 有助于应用程序移植性和程序设计语言的跨平台能力。
- 可以对未实现的硬件进行仿真，因为实际测试可能是复杂、昂贵或危险的。

解释器风格的体系结构的缺点：

- 额外的间接层次降低系统的性能。

*6. 仓库风格和黑板风格*

仓库风格的体系结构由两种构件组成：一个是中央数据结构，表示当前状态；另一个是独立构件的集合，对中央数据结构进行操作。

对系统中数据和状态的控制方法有两种，其中传统的方法是由输入事务选择进行何种处理，并把执行结果作为当前状态存储到中央数据结构中，此时仓库是一个传统的数据库体系结构；另一种方法是由中央数据结构的当前状态决定进行何种处理，此时仓库是一个黑板体系结构。黑板体系结构是仓库体系结构的特例。

黑板系统通常用于在信号处理方面进行复杂解释的应用程序，以及松散的构件访问共享数据的应用程序，也是某些对人类行为进行模拟的人工智能应用系统的重要设计方法之一，例如语音识别、模式识别、三维分子结构建模。这种风格适用于须解决冲突并处理可能存在的不确定因素。黑板的体系结构由以下 3 个部分构成，如图 6-36 所示。

- *知识源*：是特定应用程序知识的独立碎片。知识源之间的交互只在黑板内部发生。知识源代理如同学生都按照各自的方式工作在其感兴趣的方面，并在可能时向黑板添加新的知识，以供其他知识源开展进一步的工作。
- *黑板数据结构*：反映应用程序求解状态的数据。它是按照层次结构组织的，这种层次结构依赖应用程序的类型。知识源不断地对黑板数据进行修改，直到得出问题的答案。黑板数据结构发挥知识源之间通信机制的作用。
- *控制器*：控制器（即对知识源的调用）是由黑板的状态决定的。一旦黑板数据改变，某个知识源成为可用的，知识源就会被控制模块激活。控制器还承担限制知识源代理对黑板访问的工作，以防止两个代理同时写入黑板的某一空间。

#figure(
  image("../img/fig_6_36.png", width: 80%),
  caption: [黑板风格的体系结构],
) <fig-6-36>

黑板风格的体系结构和传统的体系结构有显著的区别，它追求可能随时间变化的目标，各个代理需要不同资源、关心不同问题，但用一种相互协作的方式使用和维护共享数据结构。

黑板风格体系结构的优点：

- 便于多客户共享大量数据，客户不用关心数据何时产生的，谁提供的，怎样提供的。
- 既便于添加新的作为知识源代理的应用程序，也便于扩展共享的黑板数据结构。

黑板风格体系结构的缺点：

- 不同的知识源代理对于共享数据结构达成一致，同时考虑到各个代理的调用时，对黑板数据结构的修改较为困难。
- 需要一定的同步及加锁机制保证数据结构完整性和一致性，增加系统复杂度。

*7. 模型-视图-控制器风格*

模型-视图-控制器风格通常简称为 MVC（Model-View-Controller）风格，主要用于处理软件用户界面开发中所面临的实际问题。

软件系统中用户界面经常变化，例如系统中心添加的功能要求在界面上有所体现、在不同的系统平台之间有不同的外观标准和要求、用户界面适应不同用户的喜好与风格等。MVC 风格提供一种十分简洁的解决方法，如图 6-37 所示。

#figure(
  image("../img/fig_6_37.png", width: 70%),
  caption: [模型-视图-控制器风格的体系结构],
) <fig-6-37>

MVC 风格的体系结构分为 3 种构件：

- *视图*：为用户显示模型信息。视图从模型获取数据，一个模型可以对应有多个视图。
- *模型*：模型是应用程序的核心，封装内部数据与状态。对模型的修改扩散到所有视图中。所有从模型中获取数据的对象都必须注册为模型的视图。
- *控制器*：提供给用户进行操作的接口。每个视图与一个控制器构件相关联。控制器接受用户输入，输入事件转换成服务请求，传送到模型或视图。用户只通过控制器与系统进行交互。

将模型与视图和控制器分开，从而允许为一个模型建立多个视图。如果用户通过一个视图的控制器改变模型，则其他的视图也反映出这个变化。为此，模型在其外部数据被改变时须通知所有的视图，视图则据此更新显示信息。由此允许改变应用的子系统而对其他的子系统产生重大的影响。

模型-视图-控制器风格的体系结构优点：

- 将各方面问题分解开来进行考虑，简化系统设计，保证系统可扩展性。
- 改变界面不影响应用程序的功能内核，使得系统易于演化开发，可维护性良好。
- 易于改变，甚至可能在运行时进行改变，提供良好的动态机制。

模型-视图-控制器风格的体系结构缺点：

- 主要局限在应用软件的用户界面开发领域中。

*8. 异质体系结构*

一个系统的体系结构往往是不同风格的组合。这种异质体系结构是不可避免的。因为不同风格的体系结构适合于不同的应用环境，而且新开发的软件系统须与陈旧的系统协调工作，因而须采用不同的体系结构风格。组合这些风格的有以下 3 种方法。

- *空间异质（Spatial/Location heterogeneity）*：这种方法允许构建使用不同连接件的混合。系统以一种体系结构实现一个子系统，以另外一种体系结构实现另一个子系统。例如，一个构件既可以通过它的部分接口访问仓库，也可以用管道和系统中的其他构件交互，还可以通过它的其他接口接受中央信息。
- *时间异质（Temporal/Simultaneous heterogeneity）*：系统在不同的时间采用不同的风格。例如，一个管道过滤器系统的构件实现为一个独立的进程。这时，整个系统的运行时体系结构随有无此进程的参与而发生变化。
- *层次异质（Hierarchical heterogeneity）*：这种方法按照层次结构进行组织。系统虽然采用某种体系结构的风格，但其构件和连接件内部可以是另一种体系结构。也就是说，构件或者连接件内部都可以有各自的风格。例如，分层系统的某一层可以用独立的风格实现。

一个例子是 UNIX 的管道过滤器系统：文件系统起到仓库的作用，接收初始命令的控制，并通过管道与其他构件交互。另一个典型的例子是 X Window，实现一个客户端服务器体系结构，而其中的构件又都采用分层风格的体系结构。

== 软件设计方法

基于以上软件设计的概念和原则，软件设计的核心任务是运用某种合适的软件设计方法将既定的软件需求转换成软件功能结构。从软件系统设计的角度出发，目前软件设计方法主要采用以下两种：

第一类是根据系统的数据流进行设计，称为面向数据流的设计或者过程驱动的设计，以结构化设计方法为代表。

第二类设计方法即面向对象的设计。

本教材的第 7 章和第 8 章主要描述面向对象的软件设计方法和面向数据流的结构化软件设计方法。

== 习题

(1) 给出概要设计与详细设计的关系。

(2) 说明软件设计阶段的任务和过程。

(3) 举例说明"自顶向下逐步求精"的含义及与"抽象"概念之间的关系。

(4) 模块的独立性与信息隐蔽之间有何种关系？

(5) 为什么要求模块的作用范围应在模块的控制范围之内？举例说明。

(6) 试说明软件体系结构在软件设计阶段中的重要作用。
