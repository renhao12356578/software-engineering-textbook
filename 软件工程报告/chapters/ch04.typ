#set heading(numbering: "1.1")
#show heading: it => {
  let level = it.level
  if level == 1 {
    v(20pt)
    align(center)[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 18pt, weight: "bold"); #it]
    v(15pt)
  } else if level == 2 {
    v(12pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 14pt, weight: "bold"); #it]
    v(6pt)
  } else {
    v(8pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 12pt, weight: "bold"); #it]
    v(4pt)
  }
}

= 第 4 章 面向对象需求分析方法

// 第4章 面向对象需求分析方法

== 面向对象建模方法的发展历程

面向对象的需求分析方法采用统一建模语言（Unified Modeling Language, UML）的建模语言构建领域模型和用例模型，这种模型突出从用户的角度展示功能需求的方式，而非直接采用数据流图的方式展示系统的功能。面向对象的需求分析和设计的运用取决于生命周期模型的选择，无论是RUP，还是敏捷思想的极限编程等方法都采用迭代开发方式，每个迭代周期产生一个可运行的版本。为此在实际的开发过程中，一个迭代周期内不仅有面向对象的分析，还有面向对象的设计及测试。面向对象的需求分析结果是领域模型和用例模型；面向对象的设计结果是设计模型。面向对象的基本概念和UML的内容介绍参见附录。

随着20世纪80年代后出现的大批面向对象（Object-Oriented, OO）语言，以及这些OO语言对于软件开发方式带来的冲击和变革，应运而生就是1986年Grady Booch首先提出的"面向对象设计"的概念。随后，投入到面向对象研究的人员和组织越来越多，面向对象方法向软件开发的前期阶段发展，包括面向对象设计、面向对象分析，以及面向对象的软件工程，诸如RUP和极限编程为代表的敏捷思想。

20世纪90年代以后，面向对象分析和面向对象设计方法逐渐开始实用，一些专家按照面向对象思想，对系统分析和设计工作的步骤方法、图形工具等进行详细的研究，并提出许多不同的实施方案，其中有P. Coad和E. Yourdon提出的OOA和OOD、Gr. Booch方法、J. Rumbaugh提出的OMT方法和Ivar Jacobson的OOSE方法。这些方法各有各的特点，在不同的领域得到一些组织和开发者的应用和推广，但是各自的缺陷及各种方法的相互制约造成这些方法无法很好地发展，为此Booch、Rumbaugh和Jacobson打破各自的堡垒，共同推出业界及OMG组织认可并支持的UML。

=== OOA/OOD

OOA（Object-Oriented Analysis）/OOD（Object-Oriented Design）方法是由P. Coad和E. Yourdon在1991年提出的。这是一种逐步演进的面向对象建模方法，其特点是概念清晰，简单易学。

OOA使用基本的结构化原则，并同面向对象的观点结合起来。OOA方法主要包括下面5个步骤：

#par(first-line-indent: 0em)[（1）确定类与对象：主要描述如何找到类和对象。从应用系统需求出发，以整个应用为基础标识类与对象，然后按这些类与对象分析系统的职责。另外，分析调查系统的环境，也可获得有价值的信息。]

#par(first-line-indent: 0em)[（2）标识结构：按照两种不同的原则进行。第一种是按照一般化/特殊化结构，确定已标识出类之间的继承层次关系。第二种是按照整体/部分关系，确定一个对象怎样由其他对象组成，以及对象怎样组合成更大的复杂对象。]

#par(first-line-indent: 0em)[（3）标识主题：通过把类与对象划分成更大的单元来完成。主题是一组类与对象。主题的大小应合适地选择，使得人们可以从模型很好地理解系统。主题是从更高层次看待系统的一种方法，可以按照定义好的结构来确定主题。]

#par(first-line-indent: 0em)[（4）标识属性：通过标识与类有关的信息和关联来完成。对于每个类，只需标识必需的属性。标识好的属性应放在合适的继承层次上。"关联"通过检查问题域上的关系标识。属性用名称和描述来标识。属性上的特殊限制条件也应该标识出来。]

#par(first-line-indent: 0em)[（5）定义服务：定义类上的操作。主要通过定义对象状态，以及诸如创建、访问、连接、计算、监控等服务来完成。对象间的消息通信关系用消息连接来标识。消息序列用执行线程来表达。服务用类似流程图的方式来表达。]

OOA方法的结果是结构化的图文档。模型自顶向下包括5个层次：

- 主题层；
- 类与对象层；
- 结构层；
- 属性层；
- 服务层。

OOA本质上是一种面向对象的方法，把诸如类、实例、继承、封装和对象间的通信等概念都统一在一起。寻找对象的技术是启发式的，没有一种按部就班的方法来标识系统中的对象。OOA适用于小型系统的开发。用户界面的描述不在分析的范围内，它被放在设计中完成。OOA没有具体的方法来描述对象的动态特性。执行线程是一条途径，但它在分析的后期才确定，而且主要用来验证操作正确性。

OOD负责系统设计，包括4个步骤：

#par(first-line-indent: 0em)[（1）设计问题域部分（细化分析结果）。问题域部分实际上是OOA工作的进一步延伸，在OOA工作基础上进行。值得说明的是，OOD的问题域设计部分和OOA并没有严格的分界线，这种分析和设计之间的无缝连接更反映开发活动的本质。]

#par(first-line-indent: 0em)[（2）设计人机交互部分（设计用户界面）。这部分突出如何使用系统，以及系统如何向用户提交信息。]

#par(first-line-indent: 0em)[（3）设计任务管理部分（确定系统资源的分配）。任务是进程的别名，任务管理部分用来管理任务的运行、交互等。任务管理部分可设计如下的策略：]

- 识别事件驱动任务；
- 识别时钟驱动任务；
- 识别优先任务和关键任务；
- 识别协调者；
- 定义每一个任务。

#par(first-line-indent: 0em)[（4）设计数据管理部分（确定持久对象的存储）。这部分的设计既包括数据存放方法的设计（采用关系型数据库，还是面向对象数据库），又包括相应的服务设计（设计哪些类来实现数据的持久化服务，它们应包含哪些属性和操作）。]

OOA把系统横向划分为5个层次，OOD把系统纵向划分为4个部分，从而形成一个完整的系统模型。该方法强调面向对象基本概念的应用，但是多少留有结构化分析与设计的痕迹。

=== Booch方法

Booch方法在面向对象的设计中主要强调多次重复和开发者的创造力。方法本身是一组启发性的过程式建议，并不依从硬性的限制条件。这种方法强调迭代开发的特征。设计过程从发现类和对象、形成问题域的字典（具有结构化数据词典的痕迹）开始，直到不再发现新的抽象与机制，或者所有发现的类和对象已经可以由现有的类和对象实现为止。

#par(first-line-indent: 0em)[（1）标识类与对象：主要在问题域中寻找关键的抽象，以及在对象上提供动态行为的机制。这些关键抽象可以通过与问题域的专家交谈和学习问题域的术语获得。]

#par(first-line-indent: 0em)[（2）标识语义：主要标识出类和对象的含义。开发者应该从外部看待对象，并定义出对象之间协作的协议。研究其他对象如何使用该对象是标识语义的一个重要部分。标识语义是OOD中最难的一步，通常须多次反复才能完成。]

#par(first-line-indent: 0em)[（3）标识关系：主要寻找已经获得的类和对象彼此间的关系，以及标识对象间如何交互。]

#par(first-line-indent: 0em)[（4）实现对象和类：深入内部并确定如何实现。]

Booch方法最大的特点是引入图形元素进行建模，并将几类不同的图有机结合起来，反映系统的各个方面是如何联系和相互影响的。Booch方法的图主要包括4个主图和两个辅图。在4个主图中，类图描述类之间的关系；对象图描述具体的对象和在对象之间传递的消息；类和对象被分配给具体的程序构件，模块图用来描述这些程序构件；进程图描述过程（进程）如何分配给特定的处理器，这个图主要用于须在分布式环境中应用的面向对象系统。两个辅图是状态转换图和时序图。状态转换图用于描述某个类的状态空间和状态变化；时序图描述不同对象间的动态交互。

Booch方法的主要贡献在于重点突出面向对象的迭代开发机制和使用图形元素进行建模；其缺点在于不能有效地找出每个对象和类的操作。模块图多少留有结构化设计的思想。

=== 对象建模技术

对象建模技术（Object Modeling Technique, OMT）最早是由Loomis、Shan和Rumbaugh在1987年提出的，曾扩展应用于关系数据库设计。Rumbaugh在1991年正式把OMT应用于面向对象的分析和设计。这个方法是在实体关系模型上扩展类、继承和行为而得到的。

OMT覆盖分析、设计和实现3个阶段，包括一组定义得很好并且相互关联的概念，分别是类（Class）、对象（Object）、泛化（Generalization）、继承（Inheritance）、链（Link）、链属性（Link Attribute）、聚合（Aggregation）、操作（Operation）、事件（Event）、场景（Scenario）、属性（Attribute）、子系统（Subsystem）、模块（Module）等。

OMT包含4个步骤：分析、系统设计、对象设计和实现；定义3种模型，这些模型贯穿于每个步骤，在每个步骤中被不断地精化和扩充。这3种模型是：

- 对象模型，用类和关系来描述系统的静态结构。
- 功能模型，按照对象的操作来描述如何从输入给出输出结果。
- 动态模型，描述系统中对象的时序行为及状态变化。

分析的目的是建立可理解的现实世界模型。分析模型由上述3种模型组成。初始的需求用问题陈述来表达。由问题陈述可以抽取领域相关的类、类之间的关系，以及类的属性。这些与继承关系和模块共同构成对象模型。动态模型是通过从事件踪迹图查找事件获得的。事件可以获得对象的状态转换图。功能模型是系统中实际事务的数据流图。这些模型通常都经过反复分析才能完善。

系统设计确定高层次的开发策略。系统划分成子系统，并分配到处理器和任务。数据库使用、全局资源及控制的实现策略也被确定。对象设计的目标是确定对象的细节，包括定义对象的操作和算法。分析阶段确定的对象是对象设计的构架。三种模型结合以设计对象。可以引入中间对象来支持设计。设计还包括优化。OMT的最后步骤是实现对象，是在良好的面向对象编程风格和编码原则指导下进行的，可以由面向对象语言或非面向对象语言来实现。

OMT是一种比较成熟的方法，用几种不同的观念来适应不同的建模场合，但应用所有的OMT技术来建立一个一致的模型是非常困难的，而且各阶段3个模型之间的关系也不是十分清晰。为建立一个一致的模型，OMT的许多概念和语义还需要形式的定义。

OMT在许多重要观念上受到关系数据库设计的影响，从OMT到关系数据库设计的转换在OMT方法中有详尽的描述。总之，OMT是一种比较完善和有效的分析与设计方法。

=== 面向对象软件工程方法

面向对象软件工程（OOSE）是由Jacobson提出的。将面向对象的思想贯穿到软件工程中，目的是为了得到一个能适应变化、健壮、可维护的系统。OOSE采用5个模型来完成其分析和设计过程：

#par(first-line-indent: 0em)[（1）需求模型（RM）：需求模型从用户的观点出发完整地刻画系统的功能需求，因此比较容易按这个模型与最终用户交流。主要建模手段有用例（Use Case）、问题域对象模型、人与系统的交互界面。]

#par(first-line-indent: 0em)[（2）分析模型（AM）。分析模型是在需求模型基础上建立的。主要的目的是建立健壮、可扩展的系统基本结构。OOSE定义3种对象类型：实体对象、界面对象和控制对象。通过将RM中的对象分别识别到AM中不同的对象类型并分析对象间的关系实现分析模型。]

#par(first-line-indent: 0em)[（3）设计模型（DM）。DM将AM的对象定义为模块，这实际上是考虑具体实现的表现。OOSE认为AM完全可以不考虑系统真实运行环境的约束，而只注重系统逻辑的构造。进入设计阶段后，考虑真实运行环境，这时对于系统逻辑的修改不太大，而且AM本身具有较好的可扩展性。DM最终表现为一个个类（对象）模块，并且这些类（对象）有详细的定义。]

#par(first-line-indent: 0em)[（4）实现模型（IM）。用某种程序设计语言（最好是支持面向对象）来实现DM。]

#par(first-line-indent: 0em)[（5）测试模型（TM）。关于类（对象）的底层测试（如类方法和类之间通信等的测试）可由程序员完成，但集成测试应该由独立于开发组的测试人员完成。实际上，TM是一个正规的测试报告。]

OOSE认为开发活动主要有3个步骤：分析、构造和测试。其中，分析产生RM和AM；两者作为构造活动的输入产生DM和IM；最后对实现模型进行测试，是TM。OOSE贡献在于首次使用Use Case词汇来描述用户对于系统功能的需求，其次系统化地提出各阶段的建模方法。

== 统一建模语言简介

随着面向对象技术的迅速发展，在20世纪80年代到1993年期间，面向对象方法出现百家争鸣的局面，进而集中体现在上述的四大流派：OOA/OOD、Booch方法、OMT和OOSE。由于用户很难在不同方法的模型间相互转换，这时需一种集众家之长统一的建模语言，UML正是在这种背景下应运而生。统一建模语言（Unified Modeling Language, UML）由Grady Booch、Ivar Jacobson和James Rumbaugh发起，在Booch方法、OOSE方法和OMT方法基础上，广泛征求意见，集众家之长，几经修改而形成一种面向对象分析与设计建模语言。这种建模语言得到工业界的广泛支持，由OMG（Object Management Group）采纳作为面向对象建模的行业标准，成为软件行业第一种统一的建模语言。

=== UML的诞生和发展

UML从诞生开始到最终成为行业标准经历了一个相对漫长的发展过程，共有4个发展阶段，分别是独立发展、统一、标准化和工业界应用。

#par(first-line-indent: 0em)[（1）独立发展：各种方法不可避免地存在差异，这种差异限制所有方法的推广使用。Booch（Booch方法）和Rumbaugh（OMT方法）首先意识到这一点，于是决定改变这种现状。]

#par(first-line-indent: 0em)[（2）合并统一：1994年10月，在Rational软件有限公司工作的Booch和Rumbaugh开始致力于这项工作。一年以后，Booch和Rumbaugh完成两种方法结合后的第一个草案，称之为UM 0.8（Unified Method 0.8）。UML 0.8在1995年10月的OOPSLA会议上发布之后，新加入Rational软件有限公司的OOSE方法创始人Jacobson也加盟这一工作。经过共同努力，于1996年6月和10月发布两个新的版本UML 0.9和UML 0.91，并正式命名为UML。]

#par(first-line-indent: 0em)[（3）标准化阶段：当UML对一些公司变得至关重要时，曾经制定过CORBA、接口定义语言（IDL）、基于Internet的ORB协议（IIOP）等标准的对象管理组织（OMG）开始对此表示出浓厚的兴趣。不久，为了使UML标准更加完善，OMG发布征求建议书（RFP）。随后，Rational软件有限公司建立UML Partners联盟，继续致力于由三位专家所开创的工作。这个联盟包含很多开发商和系统集成公司，它们是Digital Equipment Corporation、HP、i-Logix、IntelliCorp、IBM、ICON Computing、MCI Systemhouse、Microsoft、Oracle、Rational Software、TI和Unisys，并于1997年发布UML 1.0。]

与此同时，另外一些公司（IBM & ObjectTime、Platinum Technologies、Ptech、Taskon & Reich Technologies和Softteam）研究并提交关于UML的另一套建议。在UML的发展史上，这套建议和前面那套建议不是互相竞争的，而是互相补充的。后面的团队后来也加入UML Partners联盟，研究成果被结合，这就是1997年9月发布的UML 1.1。UML 1.1被OMG采纳为标准，这是UML发展的第3个阶段。

#par(first-line-indent: 0em)[（4）行业应用：1998年，OMG接管UML标准的维护工作，并且又制订两个新的UML修订版。UML已成为软件工业界事实上的标准，并且仍在继续发展。UML 1.3版、1.4版和1.5版先后产生，最新的版本是2.0版。]

=== UML概述

UML是综合Booch、OMT和OOSE三种方法（以及其他一些方法）得到的建模语言，同时也是这三种方法的演化结果。它在Booch、OMT和OOSE的基础上引入一些新的理论和描述方法。

UML是一种标准的图形化建模语言，是面向对象分析与设计的一种标准表示。

- 它不是一种可视化的程序设计语言，而是一种可视化的建模语言；
- 它不是工具或知识库的规格说明，而是一种建模语言规格说明，是一种表示的标准；
- 它不是过程，也不是方法，但允许任何一种过程和方法使用它。

*1. UML的目标*

为了使标准既实际，又耐用，OMG对UML制定一组目标：

- 为建模者提供现成、易用、表达能力强的可视化建模语言，以开发和交换有意义的模型；
- 提供可扩展和特殊化机制以延伸核心概念；
- 与具体的实现无关，可应用于任何语言平台和工具平台；
- 与具体的过程无关，可应用于任何软件开发的过程；
- 支持更高级的开发概念，例如构件、协作、框架和模式，强调在软件开发中对架构框架、模式和构件的重用（UML 1.4规范）；
- 与最好的软件工程实践经验集成；
- 可升级，具有广阔的适用性和可用性；
- 推动对象工具市场成长。

*2. UML的组成*

作为一种建模语言，UML的定义包含语义和语法两部分。UML的语义描述基于UML提供的精确元模型（Meta-Model）的定义。元模型是定义和构造UML模型的必要手段和描述方法，为UML所有的元素在语法和语义上提供简单一致、通用的定义性说明，这使得开发者在语义上取得一致意见。此外，UML还提供扩展机制，包括约束条件（Constraint）、版型（Stereotype）和标记值（Tagged Value），使得人们可以扩展UML的元模型。UML语法定义UML的概念、元素、符号表示法，以及用法，这为开发者或开发工具使用这些图形符号和文本语法进行建模提供标准和规范。

#par(first-line-indent: 0em)[（1）基本构造块（Basic Building Block）：]

- 事物（Thing）；
- 关系（Relationship）；
- 图（Diagram）。

#par(first-line-indent: 0em)[（2）语义规则（Rule）：]

- Name、Scope、Visibility、Integrity、Execution。

#par(first-line-indent: 0em)[（3）通用机制（Common Mechanism）：]

- Specification、Adornment、Common Division、Extensibility Mechanism。

#par(first-line-indent: 0em)[（4）事物（Thing）：]

- 结构事物（Structural Thing）：Class、Interface、Collaboration、Use Case、Component、Node；
- 行为事物（Behavior Thing）：Interaction、State Machine；
- 分组事物（Group Thing）：Package；
- 注释事物（Annotation Thing）：Note。

#par(first-line-indent: 0em)[（5）关系（Relationship）：]

- 依赖（Dependency）；
- 关联（Association）；
- 泛化（Generalization）；
- 实现（Realization）。

*3. UML的视图和图*

UML是用来描述模型的，用模型来描述系统的结构（或静态特征）及行为（或动态特征）。它从不同的视角为系统的架构建模，以用例视图为核心描述系统的不同视图（View），称为"4+1视图"，如图4-1所示。

#figure(
  image("img/fig_4_1.png", width: 70%),
  caption: [UML的4+1视图],
) <fig_4_1>

#par(first-line-indent: 0em)[（1）用例视图（Use Case View）：强调从用户的角度看到的或需要的系统功能，这种视图也称为用户模型视图（User Model View）或场景视图（Scenario View）。]

#par(first-line-indent: 0em)[（2）逻辑视图（Logical View）：展现系统的静态或结构组成及特征，也称为结构模型视图（Structural Model View）或静态视图（Static View）。]

#par(first-line-indent: 0em)[（3）进程视图（Process View）：描述设计的并发和同步等特性，关注系统非功能性需求，也称为行为模型视图（Behavioral Model View）、过程视图（Process View）、协作视图（Collaborative View）和动态视图（Dynamic View）。]

#par(first-line-indent: 0em)[（4）组件视图（Component View）：关注软件代码的静态组织与管理，也称为实现模型视图（Implementation Model View）和开发视图（Development View）。]

#par(first-line-indent: 0em)[（5）部署视图（Deployment View）：描述硬件的拓扑结构，以及软件和硬件的映射问题，关注系统非功能性需求（性能、可靠性等），也称为环境模型视图或物理视图（Physical View）。]

每一种UML的视图都是由一个或多个图（Diagram）组成的，一个图就是系统架构在某个侧面的表示，所有的图组成系统的完整视图。UML规范提供下述9种基本的图形元素：

#par(first-line-indent: 0em)[（1）用例图（Use Case Diagram）：从使用者的角度描述对系统功能的要求。]

#par(first-line-indent: 0em)[（2）类图（Class Diagram）：描述系统的静态结构（类及其相互关系）。]

#par(first-line-indent: 0em)[（3）对象图（Object Diagram）：描述系统在某个时刻的静态结构（对象及其相互关系）。]

#par(first-line-indent: 0em)[（4）顺序图（Sequence Diagram）：按时间顺序描述对象之间的交互。]

#par(first-line-indent: 0em)[（5）协作图（Collaboration Diagram）：从对象之间的关系描述它们之间的交互顺序。]

#par(first-line-indent: 0em)[（6）状态图（State Diagram）：描述对象的不同状态之间转换的条件和响应。]

#par(first-line-indent: 0em)[（7）活动图（Activity Diagram）：描述为了完成某个用例，对象或对象之间所必须执行的活动次序。]

#par(first-line-indent: 0em)[（8）组件图（Component Diagram）：描述系统为了完成某个或者某些用例，对象封装的组织结构。]

#par(first-line-indent: 0em)[（9）部署图（Deployment Diagram）：描述环境元素的配置，并把实现系统的元素映射到配置上。]

== 面向对象的需求分析建模

面向对象需求分析活动的目标同样是构建目标系统的逻辑模型，其前提条件同样是当前系统的物理和逻辑模型。结构化分析方法并没有一种明确的建模方法来表示当前系统的逻辑模型，更多采用文字描述的形式描述业务背景、业务概念、业务流程及业务数据。面向对象的分析方法突出强调当前系统逻辑模型的重要作用，也就是领域模型（Domain Model），并使用UML一些诸如对象和类的图形元素规范表示其业务的逻辑模型。

基于领域模型及对用户需求的一些了解，在需求分析过程中还可以同时进行用例建模，也就是目标系统的逻辑模型。在对业务知识有一定了解的基础上，使用标准的UML用例图等元素表达用户对于系统功能的需求。在具体的建模过程中，领域模型和用例模型是相辅相成的，它们的关系如图4-2所示。

#figure(
  image("img/fig_4_2.png", width: 80%),
  caption: [面向对象需求分析建模的活动],
) <fig_4_2>

从图4-2可以看出，第一项活动（业务需求描述）可以理解为当前系统物理模型的活动内容，从业务的角度阐述各种功能需求的来源，并通过一些具体的业务场景来说明这些功能需求的用处。基于这些分析活动的结果，需求分析活动又分为两个主要的环节。

#par(first-line-indent: 0em)[（1）领域建模：通过对一些业务场景的分析，可以得到一些须关注的业务概念及一些必须保存的业务数据，并将其归纳为领域模型中的静态结构，使用UML类图进行表示；如果其结果还须对一些动态的业务流程进行描述，则可以使用UML的活动图来表达，最终形成能够表达当前系统的逻辑模型。]

#par(first-line-indent: 0em)[（2）用例建模：通过对业务需求的理解，并对一些场景描述进行整理和分析，从用户的角度使用UML用例图来清晰地表达对系统功能的要求；除此之外，为了方便将分析结果能够用于系统的功能设计，还进一步使用UML的交互图展示用户使用系统功能的交互次序，并声明系统"做什么"的结果。用例模型由以下4个基本内容组成：]

- 用例图（Use Case Diagram）。
- 用例描述（Use Case Scenario）。
- 系统顺序图（System Sequence Diagram）。
- 操作契约（Operation Contract）。

此外，还须说明的是，在迭代增量式软件开发过程指导下进行面向对象分析时，一次迭代往往只是选取系统的部分用例，甚至只是某个用例的部分场景进行分析、描述，进而识别系统事件，得到系统操作契约。在识别领域模型时，也往往是针对当前用例所相关的业务环境识别关键概念。

== 领域建模

对于当前系统的理解程度很大程度上决定需求分析结果的正确性，为此领域模型的重要作用无须赘言，是软件开发人员所必须重视的一环，是将业务背景知识和软件开发人员结合的有效方法之一。其原因在于软件开发人员是面向所有业务领域的，但同时也不可能事先花费大量时间和精力掌握相应的业务知识后才可以进行软件开发，所以将特定领域知识的了解也纳入软件开发过程中。为此须把握住一些全局性的概念及核心流程，并对这些概念和概念之间的关系及流程进行必要的分析和研究，形成当前系统的逻辑模型，即领域模型。

=== 领域模型的定义

领域模型是针对某一特定领域内概念类或者对象的抽象可视化表示[MO95, Fowler96]，也称为概念模型、领域对象模型或者分析对象模型。

须特别注意的是，其中的模型元素仅仅是概念类或对象，而非软件对象、软件类或者软件架构中的领域层。它重点描述领域内的关键概念，使得开发人员摆脱繁杂的业务文字描述或者具体的业务物理模型，除此之外其最重要的作用是可以为软件架构领域层中软件对象的设计提供足够的信息。

=== 领域模型的表示

领域模型使用UML的类图来表达概念及其关系，使用活动图来表示业务流程，用于辅助解释类图。领域模型中的类称为概念类，其原因是这些概念还不是软件系统中的对象，而是用来分析和考察的对象。概念类之间的关系也使用UML类之间的关系表达，诸如关联、继承、聚合/组合及依赖来表示。领域模型中概念类及其关系的创建步骤如下：

#par(first-line-indent: 0em)[（1）识别或抽象出领域的概念类或对象。]

#par(first-line-indent: 0em)[（2）建立概念类之间的关系。]

#par(first-line-indent: 0em)[（3）设置概念类的关键属性。]

通过领域模型，开发人员可以了解到领域内的关键概念、核心词汇和基本信息，所以某种程度上领域模型称为可视化的字典。图4-3使用UML的类图描述领域模型的组成元素及其关系。

#figure(
  image("img/fig_4_3.png", width: 85%),
  caption: [领域模型元素及结构的UML表示],
) <fig_4_3>

=== 领域模型元素与软件对象的区别

须特别注意，领域模型描述的内容与软件对象无关，是纯粹对现实客观世界的抽象描述。软件开发人员经常遇到系统结构的领域层或者系统结构的业务（应用）逻辑层，这里的"领域层"强调位于界面层之下的一组软件对象，这些对象又表达业务对象的逻辑，故统称为领域层。

虽然两种词汇有本质的区别，但是它们之间也有内在的联系。面向对象的一种核心思想就是利用领域模型中概念类的名称、属性等信息启发式地进行系统领域层类的设计，运用这种方法可以大大降低需求和设计之间的跳转差异。

=== 识别概念类

面向对象的需求分析也是基于迭代增量式的开发模式，为此并非要求一次性识别所有的概念类，而仅仅要求在当前迭代范围内明确的问题域寻找概念类。为此，在每次迭代周期中进行领域建模时，都须明确以下两项必备条件：

#par(first-line-indent: 0em)[（1）确定当前的迭代范围。]

#par(first-line-indent: 0em)[（2）确定当前的问题域范围。]

在识别概念类时，须遵循下述原则：用大粒度的概念类充分描述领域模型比粗略描述好。当遇到分析阶段漏掉的一些概念类，在后续的设计阶段才发现这些概念类必需时，则还须对领域模型进行必要的修改和添加。下面提供一种简单实用的识别概念类技巧——根据名词短语识别概念类。

这是一个实用的寻找概念类的方法，通过识别问题域中有关业务描述中的名词或者名词短语，然后将它们作为候选的概念类。如果此时已经具备用例模型中某个用例的场景描述，则详细的用例描述文档是通过名词短语识别领域概念的最佳选择。

运用这种方法时，须特别注意名词可能是概念类，也可能是概念类的属性。属性一般是可以赋值的，比如数字或者文本；概念类是不能被赋值，在面向对象领域中概念类只能被实例化。如果对一个名词是概念类还是属性举棋不定时，最好将其作为概念类处理。例如：

#par(first-line-indent: 0em)[（1）"人民医院是北京的三甲医院"这一句话中有"人民医院"、"北京"和"三甲医院"三个名词短语，其中只能抽象出一个概念类：医院，而其他诸如"人民"、"北京"和"三甲"都是形容或者说明医院的特征值，可以归纳为该概念类的属性，分别是名称、地区和级别。]

#par(first-line-indent: 0em)[（2）"大三下学期计算机学院开设软件工程的课程，课时48小时"，这一句话中有"大三学期"、"计算机学院"和"课程"、"课时"这4个名词短语，可以抽象成概念类的是"学期"、"学院"和"课程"，而"大三"和"计算机"和"48小时"都是进一步说明这些概念类的特征值。]

表4-1是某在线考试系统中学生参加上机考试用例的一个场景描述，粗体字即是识别出可能的概念类和属性。

#figure(
  table(
    columns: 1,
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [表4-1 根据名词短语识别概念类示例],
    [学生在系统主窗口中选择参加考试],
    [系统列出该学生该时段能参加的所有考试课程名称],
    [学生选择其中的一门课程，要求考试],
    [系统弹出登录框，要求学生输入考试密码],
    [学生输入从监考教师处获取的考试密码，登录],
    [系统显示欢迎界面，展示考试课程名称和考试时长，询问学生是否开始考试],
    [学生选择开始考试],
    [系统按照预先设计好的考卷生成规则自动生成一套考卷],
    [系统显示考题],
    [学生答题，并提交该题答案],
    [系统记录答案，并使"上一题"或"下一题"按钮生效],
    [学生选择上一题或者下一题。系统重复步骤9--12，直到学生选择结束考试或者考试时间到],
    [系统显示学生的选择题得分],
    [系统询问学生是否退出考试],
    [学生选择退出],
    [系统回到系统主窗口],
  ),
  caption: [根据名词短语识别概念类示例],
)

根据分析可以得到概念类：学生、监考教师、课程、考卷生成规则、考试、考卷和考题。名词：密码、时长、得分都是属于考试概念类的特征值。

=== 识别和添加概念类之间的关系

UML对"关联"的定义是：两个或多个类之间有关其实例链接的语义定义。"关联"常与静态动词或动词短语相对应。据此，可以通过上述业务场景的文字描述寻找两个概念类之间是否存在动词。概念之间的"关联"通常表示必须知道这个关系存在，并且这个关系须保存一段时间——数年或者百万分之一秒，取决于关联所处的语境。换句话说，哪两个对象之间的关系须记住？

领域模型中的"关联"可分为两种。一种是将概念之间的关系信息保持一段时间的"关联"，称为"须知道"型"关联"。另一种是有助于增强对领域中关键概念理解的"关联"，称为"只需理解型"关联"。领域模型须着重考虑"须知道型""关联"。对于"只需理解"型关联，虽然在最终的软件系统中无须将这种"关联"保存一段时间，但如果这种"关联"真实存在于问题域中，并且如果这种"关联"有助于增强对问题域某些关键概念的理解，则有必要建模这种"关联"。例如："人民医院是北京的三甲医院"，上面的分析结果只有一个概念类，没有第2个与之关联的概念类，所以这句话中概念类"医院"不存在关联关系。如果这句话后面还有"该医院具有30个科室及300多名医生"，那么除了"医院"概念类之外，还可以抽象出"科室"和"医生"两个概念类，进而通过"具有"的动词短语很清楚地表明医院的规模和组织关系，那么这3个概念类之间就存在某种需相互知道的关系。

又如"大三下学期计算机学院开设软件工程的课程，课时48小时"，这句话已知具有"学院"、"学期"和"课程"3个概念类。"学院"和"课程"之间通过"开设"关联，"学期"和"课程"通过时间段某种须知道关联。

在识别"关联"时，一方面可以借助分析人员自身对问题的理解，另一方面可以借助如表4-2所示的通用关联列表。

#figure(
  table(
    columns: (1.6fr, 1.4fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [分类], [示例],
    [A在物理上是B的一部分], [树木--森林],
    [A在逻辑上是B的一部分], [销售项--销售、考题--考卷],
    [A在物理上包含在B中或者依赖于B], [商品--货架],
    [A在逻辑上包含在B中], [考卷生成规则项--考卷生成规则],
    [A是对B的描述], [考题规格说明--考题],
    [A是交易或者报表B中的一项], [销售项--销售、考题--考卷],
    [A是B的一个成员], [收银员--商店],
    [A是B的一个组织子单元], [销售部--商店],
    [A使用或管理B], [收银员--POS机],
    [A与B通信], [顾客--收银员],
    [A与一个交易B有关], [学生--考试、顾客--销售],
    [A是一个与另一个交易B有关的事务], [支付--销售],
    [A与B相邻], [商品--商品],
    [A为B所拥有], [POS机--商店],
    [A是一个与B有关的事件], [考试--学生、销售--顾客],
  ),
  caption: [通用关联列表],
)

其中，通用关联列表中的下述3种分类是高优先级的关联分类，它们在领域模型中有广泛应用：

#par(first-line-indent: 0em)[（1）A在物理上或逻辑上是B的一部分，如树木组成森林；]

#par(first-line-indent: 0em)[（2）A在物理上或逻辑上包含在B中或依赖于B，如商品放在货架上；]

#par(first-line-indent: 0em)[（3）A是对B的描述，如考题规格说明是对考题的描述。]

"关联"是重要的，但是不应该花费过多的时间研究和寻找，而且领域模型中的"关联"也不是越多越好。寻找"关联"时遵循下述指导原则：

#par(first-line-indent: 0em)[（1）识别概念类比识别"关联"更重要。]

#par(first-line-indent: 0em)[（2）将注意力集中在"须知道"型"关联"。根据"须知道"型原则获取最小集合的"关联"，然后利用"只需理解"型"关联"增强对领域中关键概念的理解。]

#par(first-line-indent: 0em)[（3）太多的"关联"不仅不能有效地表示领域模型，反而容易使领域模型变得混乱。]

#par(first-line-indent: 0em)[（4）避免显示冗余或导出"关联"。]

=== UML概念类及关联关系表示

类的UML表示是一个矩形，垂直地分为3个区，如图4-4所示。顶部区域显示类的名称，中间的区域列出类的属性，底部的区域给出类的操作。

#figure(
  table(
    columns: (1fr, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    image("img/fig_4_4.png", width: 100%),
    image("img/fig_4_5.png", width: 100%),
  ),
  caption: [类的UML基本结构（左）与UML类的3种表示方法（右）],
)

在绘制类图时，上顶端的区域是必需的，下面的两个区域是可选择的，其主要原因是，在不同的建模过程中所关注的要点是不一样的。图4-5左侧表示只有类名称的类，图中间表示缺少操作的类，图右侧表示缺少属性的类。在构建领域模型时只需用前两种表示法即可。

#figure(
  image("img/fig_4_6.png", width: 50%),
  caption: [IBMRSA中类及其属性的表示],
) <fig_4_6>

比如，图书馆的主要概念类"图书"就可以具有"图书名"、"图书号"、"借阅时间"等属性，而这些属性位于类图中的第2个区域中并表示为图4-6。

- "图书号"前的圆形表示Public；
- "图书名"前的正方形表示Private；
- "出版社"前的菱形表示Protected；
- "借阅时间"前的长方形表示Package。

UML规范定义以下6种类的关系，并且按照类关系由弱到强的次序进行描述，如图4-7所示。

#figure(
  image("img/fig_4_7.png", width: 90%),
  caption: [UML类之间的关系及图形表示],
) <fig_4_7>

==== 1. 类的依赖关系（Dependency）

当发现一个类"须知道"另一个类并使用它的对象时，这两个对象之间存在依赖关系。比如，"医院就诊管理系统"中的"医生"概念类与"挂号人员"或者"队列信息"概念类之间存在依赖关系，具体说医生的问诊依赖挂号人员的正常操作结果或者医生的问诊叫号依赖"队列信息"的内容。注意：此时声明两个类之间存在依赖关系时，仅仅说明它们在对象的级别上存在关系，而非对象的内部属性和操作，因此依赖关系是类之间关系最薄弱的环节。UML规范中用带有箭头的虚线表示依赖关系，并且特别注意两个概念类之间谁依赖谁。

在现实生活当中，谈及热门话题"小升初"时，最直接的两个概念类就是"小学"和"初中"，其中关系是如果能升入满意而且比较好的初中，首要条件就是有一个好的"小学"。这里的"小学"必须是某一个具体的小学，而非小学里面的硬件设施或者某位好教师，如图4-8所示。

#figure(
  image("img/fig_4_8.png", width: 70%),
  caption: [类之间的依赖关系示意图],
) <fig_4_8>

后期进行软件类设计时也会遇到大量类之间的依赖关系，软件对象之间的依赖关系可以直接理解为某个软件对象调用另一个软件对象的某个方法，在调用之前这个软件对象首先必须知道被调用软件对象及其方法，调用的返回值结果决定调用对象的后续执行。下面的代码展示一个ClassMan调用另外3个类Car、Light及ATM的方法，图4-9展示这4个类之间的依赖关系。

```java
public class Man {
    public void drive(Car car) {
        car.start();
    }
    public void sleep() {
        Light light = new Light();
        light.off();
    }
    public int getMoney(int amountOfNeed) {
        return ATM.fetch(amountOfNeed);
    }
}
```

#figure(
  image("img/fig_4_9.png", width: 75%),
  caption: [类之间的依赖关系],
) <fig_4_9>

==== 2. 类的关联关系（Association）

当一个类的属性声明另一个类的对象或者定义另一个类的对象引用时，说明这两个类之间存在关联关系。在通常情况下，在分析的语境中发现一个概念类与另一个类的对象为了某个目的协同工作时，相对于依赖关系，这两个类之间很大程度上更有可能存在关联关系。通俗的解释就是，一个类可以将另一个类的某些属性或者类对象的整体作为其属性时，它们之间就存在关联关系。

类的关联关系可分为双向关联、单向关联（即导航关联）、递归关联，以及关联类四种。UML规范使用没有箭头的实线表示关联关系，如图4-10所示为双向关联关系。

#figure(
  image("img/fig_4_10.png", width: 60%),
  caption: [类之间的双向关联关系],
) <fig_4_10>

图4-10描述的"关联"所对应的代码为：

```java
public class Student {
    private Book[] book;
    // other attributes & methods
}
public class Book {
    private Student student;
    // other attributes & methods
}
```

如果关联关系之间存在导航关系，说明一个类是否可以访问另一个类的属性或者该类是否在属性中包含另一个类对象的引用，如图4-11所示为单向关联关系。

#figure(
  image("img/fig_4_11.png", width: 60%),
  caption: [类之间具有导航的关联关系],
) <fig_4_11>

图4-11描述的"关联"所对应的代码为：

```java
public class Student {
    private Book[] book;
    // other attributes & methods
}
public class Book {
    // private Student student;
    // other attributes & methods
}
```

==== 3. 类的聚合关系（Aggregation）

当一个类A（整体类）拥有另一个类B（部分类），同时其他的类C也可分享类B，即类B不完全被类A所拥有时，它们之间存在聚合关系。换言之，部分类的对象不会因为整体类消失而不存在，这是聚合关系与组合关系之间最重要的区别。例如，学校由教师、学生、教学、科研等对象组成，则学校和教师之间存在聚合关系。在UML中，聚合关系由带空心菱形的直线表示，空心菱形一方连接整体类，另一端连接部分类，学校和教师的聚合关系如图4-12所示。

#figure(
  image("img/fig_4_12.png", width: 60%),
  caption: [类的聚合关系一],
) <fig_4_12>

聚合关系的代码表示与关联关系是一致的，都体现在属性的引用上。比如下面的一段代码，如图4-13所示。

```java
public class Shipment {
    private Ship ship;
    private Customer[] customer;
}
public class Ship {
    private String id;
    private String owner;
    private Shipment shipment;
}
public class Customer {
    private String Name;
    private String creditCardNumber;
    private Shipment shipment;
}
```

#figure(
  image("img/fig_4_13.png", width: 55%),
  caption: [类的聚合关系二],
) <fig_4_13>

==== 4. 类的组合关系（Composition）

当一个类A（整体类）完全拥有另一个类B（部分类），且其他任何类都不能分享类B时，它们之间存在组合关系。当整体类消失时，部分类也不会存在。例如，文档由章节组成，如果没有文档，章节不会独立存在。在UML中，由带有实心菱形的直线表示组合关系，实心菱形一方连接整体类，另一端连接部分类，文档与章节的组合关系如图4-14所示。

#figure(
  image("img/fig_4_14.png", width: 60%),
  caption: [类的组合关系一],
) <fig_4_14>

类之间的组合关系体现在代码上时，一般由整体类对部分类进行实例操作，比如下面的代码，如图4-15所示。

```java
public class Window extends Frame {
    TextField txt = new TextField("hello");
    Button btn = new Button("ok");
    MenuBar mbr = new MenuBar();
    MenuItem item = new MenuItem("Exit");
    Menu m = new Menu("File");
}
```

#figure(
  image("img/fig_4_15.png", width: 70%),
  caption: [类的组合关系二],
) <fig_4_15>

==== 5. 类的继承关系（Inheritance）

当一个类（父类）是另外一个类或一些类（子类）的类型时，它们之间存在继承或者泛化关系。例如，在电信运营商开账户时，可以选择不同的账户类型，包括GSM账户和CDMA账户，GSM账户是一种账户类型，所以GSM账户与账户存在继承关系。在UML中，由带有空心三角形的直线表示继承关系，空心三角形一方连接父类，另一端连接子类。GSM账户和CDMA账户与账户之间的继承关系如图4-16所示。

#figure(
  table(
    columns: (1fr, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    image("img/fig_4_16.png", width: 100%),
    image("img/fig_4_17.png", width: 100%),
  ),
  caption: [类的继承关系（左）与关联类（右）],
)

子类可以拥有父类所有的属性和操作，且可以定义自身的一些属性和操作，这种关系充分体现面向对象强调的软件复用原则。类的继承关系体现在代码上主要使用的关键字extends，如图4-16对应的代码。

==== 6. 关联类

在关联建模中，存在一些情况下，需要其他类，因为它包含关于关联有价值的信息。对于这种情况，使用关联类来绑定这些基本关联。关联类和一般类一样表示。不同的是，主类和关联类之间用一条相交的点线连接。

在图4-17显示的类图中，课程类和教师类之间的"关联"产生称为授课表的关联类。这说明当教师类的一个实例关联到课程类的一个实例时，产生授课表类的一个实例，即在课程表中插入一条该教师的授课记录。

实例一：针对前面举例的在线考试系统学生参加上机考试的用例，根据已经得到的概念类：学生、监考教师、课程、考卷生成规则、考卷生成规则项、考试、考卷、考题、考题规格说明，根据用例说明中的一些动词短语进行如下分析，表4-3中的"关联"是上述概念类之间的"须知道"型"关联"，表4-4是通过关联列表识别得到的概念类之间关系。

#figure(
  table(
    columns: (1fr, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [关联], [含义],
    [学生"参加"考试], [为了知道学生是否须参加该项考试],
    [教师"监考"考试], [为了知道由哪（几）位教师监考],
    [考卷"记录"考题], [为了知道一份考卷由哪些考题组成],
    [考卷生成规则"对应于"课程], [为了知道是哪门课程的考卷生成规则],
    [考卷生成规则"应用于"考试], [为了知道某次考试使用哪套考卷生成规则],
    [考卷生成规则"记录"考卷生成规则项], [为了知道一套考卷生成规则由哪些细项组成],
    [考题规格说明"详细描述"考题], [为了知道一道考题的详细描述],
  ),
  caption: [考试用例相关的"须知道"型"关联"],
)

#figure(
  table(
    columns: (1.6fr, 1.4fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [分类], [示例],
    [A在物理上是B的一部分], [不适用],
    [A在逻辑上是B的一部分], [考题--考卷],
    [A在物理上包含在B中或者依赖B], [不适用],
    [A在逻辑上包含在B中], [考卷生成规则项--考卷生成规则],
    [A是对B的描述], [考题规格说明--考题、课程规格说明--课程],
    [A是交易或者报表B中的一项], [考题--考卷],
    [A为B所知/为B所记录/录入B中/为B所捕获], [不适用],
    [A是B的一个成员], [不适用],
    [A是B的一个组织子单元], [不适用],
    [A使用或管理B], [不适用],
    [A与B通信], [不适用],
    [A与一个交易B有关], [学生--考试],
    [A是一个与另一个交易B有关的事务], [不适用],
    [A与B相邻], [考题--考题],
    [A为B所拥有], [不适用],
    [A是一个与B有关的事件], [考试--学生],
  ),
  caption: [考试用例相关的部分"关联"],
)

根据上面的分析过程和分析结果，可以得到初步的领域模型，如图4-18所示。其中，学生选课得到关联类"选课"，记录课程总评等信息；教师教授某门课程得到关联类"授课"，记录授课时间、地点等信息；教师监考某门课程得到关联类"监考"，记录监考时间、监考地点等信息，同时对考题进行细分，可以得到选择题、填空题、简答题和程序设计题。选择题又可进一步细分为单选题和多选题。关联类的概念和用法参见附录。

#figure(
  image("img/fig_4_18.png", width: 85%),
  caption: [在线考试系统部分领域模型],
) <fig_4_18>

实例二：下面结合某高等学校的组织结构，说明类的关联关系表示方法，其基本的文字描述如下：

某高校包含10个教学单位和5个行政机构，计算机学院是其中一个学院，内部由院党委、行政、教学和科研及学生组成。其中，教学单位分为5个中心，学生由本科生、硕士研究生组成；本科生分为4个年级，每个年级有3个大班，每个大班有5个小班；硕士研究生分为3个年级且分别属于教学单位的5个中心。

通过对以上描述的分析，找到该领域包含的概念类有：大学、行政机构、教学单位、院党委、院行政部门、科研单位、学生、教学中心、本科生、硕士研究生、年级、大班、小班。

这些概念类之间的关系如图4-19所示。

#figure(
  image("img/fig_4_19.png", width: 85%),
  caption: [某大学组织结构领域模型的类图表示],
) <fig_4_19>
=== 添加属性

属性是类和对象的数据特性，也是一个类或对象区别于另一个类和对象的重要特征，例如重量、颜色和速度等。领域模型中的属性往往是需求（用例）建议或者须记忆的信息。属性应当优先定义为简单数据类型，如Boolean、date、number、string、text、time，以及其他常见的简单数据类型，如address、color、zip等。属性值不应该是对象，应该使用"关联"而不是属性来联系概念类。

识别属性时，不把寻找属性的过程放得过大。只考虑那些与应用相关的属性。首先获取最重要的属性，以后可以添加细节。在分析过程中，避免只因为实现而使用属性。对每一个属性赋予一个有意义的名称。

对属性的建模主要包括属性名、类型名、可见性和初始值。如表4-5所示，在识别概念类的属性时，只需要建模属性的名称、类型和初始值（如果有），还不考虑属性的可见性。属性可见性应该推迟到设计阶段考虑。

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [概念], [属性],
    [课程], [开课时间……],
    [课程规格说明], [课程编号、课程名称、学分、课程简要介绍……],
    [学生], [学号、班级、姓名、联系Email……],
    [选课], [选课年份、课程总评成绩……],
    [教师], [工作证号、姓名、职称、所在院系名称、联系电话、联系Email……],
    [授课], [授课时间、授课教室……],
    [监考], [应到人数、实到人数、考场情况简述……],
    [考试], [考试时间、考试时长、考试地点……],
    [考卷], [总分……],
    [考卷生成规则], [测试对象、测试目的、内容范围综述、考卷难度、规则创建人、规则创建时间、规则生效标志……],
    [考卷生成规则项], [题目类型、题目难度、内容分类、题目数量……],
    [考题], [解答、得分……],
    [考题规格说明], [题目类型、题目难度、内容分类、分数、创建时间、创建人、生效标志……],
  ),
  caption: [考试用例相关概念的部分属性],
)

== 用例建模

用例模型一般由用例图及用来解释用例实现过程的用例描述组成，为了能够与后期的系统结构设计形成一个有机的整体，又提出附加的两个内容用以补充用例模型，即系统顺序图和操作契约。

=== 用例及用例模型的定义

用例（Use Case）是从使用系统的角色出发描述使用者与系统或者系统某个功能之间的交互场景，即用例可以描述用户对于系统的功能需求。无论系统的功能是否成功，每个用例必须返回给使用者一个明确的交互结果来说明使用者对于功能需求的目的。一个用例描述使用者为完成某个目的明确的功能与系统之间一个或者多个系统事件的交互过程。

用例模型以用例为核心从使用者的角度描述和解释对于待构建软件系统的功能需求，并通过UML的用例图规范化地表示，使用系统顺序图和操作契约为软件对象确定和软件系统结构设计提供充分的依据。

=== 用例模型的组成结构

用例模型由4个部分组成，其中用例图和用例说明是基础，系统顺序图和操作契约是基于用例图的附加说明，参见图4-20。当某个用例比较简单时，可以省略系统顺序图和操作契约。创建用例模型的步骤如下：

#par(first-line-indent: 0em)[（1）确定问题域的分析范围。]

#par(first-line-indent: 0em)[（2）确定该范围内可能出现的角色。]

#par(first-line-indent: 0em)[（3）根据业务背景或者领域模型，确定每个角色需要的用例，并形成用例图。]

#par(first-line-indent: 0em)[（4）基于确定的用例，整理成规范的用例描述文本。]

#par(first-line-indent: 0em)[（5）在可能的情况下，将多个角色的用例图整合成一个相对完整的用例图。]

#par(first-line-indent: 0em)[（6）针对每个用例，并结合相应的用例描述，确定系统顺序图中角色与系统之间的事件交互（也称为操作），绘制基于用例的系统顺序图。]

#par(first-line-indent: 0em)[（7）基于每个系统顺序图，确定每个事件交互经过系统处理后应该返回给角色的声明。]

#figure(
  image("img/fig_4_20.png", width: 85%),
  caption: [用例模型的基本元素、结构及与领域模型的关系],
) <fig_4_20>

=== 用例图

用例图由角色（Actor）及对应的一个或多个用例组成。用例图中角色的多少隐含地说明问题域的范围。为了更加清晰地展示功能需求，基于用例场景的内容将一个用例分解成粒度更小的用例。可以理解为一个用户的功能需求可能由多个系统级的用例所对应。比如，银行储户使用ATM取款机取钱，从用户的角度出发只需描述成"取款"用例，但是经过具体的分析，"取款"用例还附加"密码验证"，以及"打印凭据"等子用例的情况。图4-21展示基本的用例图结构。

#figure(
  image("img/fig_4_21.png", width: 75%),
  caption: [ATM取款机用例图],
) <fig_4_21>

==== 1. 问题域边界

问题域边界用来说明当前讨论用例图应用的范围，用一个矩形表示，边界名称标识在矩形的内部或上边。参与系统交互的角色画在系统边界外部，系统提供的用例画在矩形内部。图4-21描述的用例图表示该系统是ATM取款机系统，其中包含取款、查询余额和验证密码三个用例，参与系统交互的角色有银行客户。

==== 2. 角色（Actor）

为了能准确地表示需求，必须从用户的角度出发。角色也称为参与者，是一类用户的总称，是一种抽象的表达方式，代表这一类用户在使用系统的过程中对功能的具体要求。与角色相对应的是系统功能，当所有使用系统的角色确定后，从一定程度上就能反映出系统的边界，也可以明确待构建系统的范围和大小，进而帮助明确系统的功能需求。UML的角色表示为一个带有角色名称的小人图标或带有标识的矩形，如图4-22所示。

#figure(
  table(
    columns: (1fr, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    image("img/fig_4_22.png", width: 100%),
    image("img/fig_4_23.png", width: 100%),
  ),
  caption: [角色的UML表示（左）与用例的UML表示（右）],
)

与系统进行交互的角色一般情况下可分为3种。

#par(first-line-indent: 0em)[（1）人：使用系统各种功能的角色，例如银行客户。]

#par(first-line-indent: 0em)[（2）其他系统或设备：与系统进行信息交互且位于该系统边界之外的其他系统和设备，例如银行主机系统。]

#par(first-line-indent: 0em)[（3）时钟（Timer）：作为一种位于系统内部的特殊角色，由于时钟的作用类似于系统外部的角色，即它可以触发一些系统的功能，表示在此时刻系统应该提供哪些功能。]

识别系统角色是用例建模的第一步，通过回答以下一些提示性问题，可以帮助系统分析人员快速定位角色：

- 系统的主要用户是谁？
- 谁使用系统完成日常工作？
- 谁关注系统中记录的信息？
- 谁来维护系统，使系统正常运行？
- 系统与哪些其他系统交互？
- 在预定的时刻，是否有事件自动发生？
- 系统控制的硬件设备有哪些？

==== 3. 用例（Use Case）

在明确系统的角色后，从角色要完成的业务环境中去寻找其使用系统所要完成的功能，也就是该角色为了完成某些特定的任务而必须执行的一系列动作，这一系列动作的集合就可称为一个业务场景，即用例。UML的用例表示为一个具有功能名称的椭圆图标，如图4-23所示。

用例描述系统能够提供的一组服务，是系统功能的概要表示，隐藏用例实现的细节。用例名称代表每个用例定义的功能，通常用动词加上名词进行描述，例如取款、查询余额等。用例名称重点描述用例的目标，而不是用例实现的过程。用例名称既可以写在用例的下方，也可以写在用例的里面，但是避免在一幅用例图中混合使用。

在识别用例的过程中，通过回答以下提示性问题有助于分析人员发现和定义系统用例：

- 系统承载的日常业务有哪些？
- 为了承载日常业务，系统需要提供哪些功能？
- 系统需要存储的数据信息有哪些？
- 系统对这些信息提供哪些处理功能？
- 各种角色从系统中得到哪些查询、统计、报表或分析结果？
- 哪些事件可以影响系统状态？

==== 4. 关联

如果角色需要使用某个用例，则需要在角色和用例之间建立一个"关联"，UML用一条连接角色与用例的直线表示，如图4-24所示。

#figure(
  table(
    columns: (1fr, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    image("img/fig_4_24.png", width: 100%),
    image("img/fig_4_25.png", width: 100%),
  ),
  caption: [角色与用例之间的"关联"（左）与具有多角色关联的用例图（右）],
)

对于一个用例来说，它至少有一个角色相关联，但也可以和多个角色相关联，如图4-25所示。"关联"表示角色与用例之间的使用关系，并不强调交互的方向，如果在关联上用箭头标识方向，则代表交互信息流的方向，或谁启动用例。

以医院就诊管理系统为例，根据前面的分析结果作为已知条件，即医院具有4个部门，分别是挂号处、科室、交费处和取药处。

#par(first-line-indent: 0em)[（1）如果此时分析的问题域限定于挂号处，也就是创建用例模型的第一个步骤。]

#par(first-line-indent: 0em)[（2）挂号处的主要工作人员是挂号人员，则确定该问题域所关心的角色。]

#par(first-line-indent: 0em)[（3）挂号人员的主要工作性质是面向具有病历的病人进行挂号，则可以确定挂号人员的角色对应的主要用例是"新建挂号"。]

#par(first-line-indent: 0em)[（4）如果病人没有病历，怎么办？如果医院规定挂号处还具有创建新病历的职责，那么挂号人员对应的另一个用例就是"创建病历"。]

经过简单的分析后，可以根据初步的分析结果构建用例模型的第一个主要部分——用例图，如图4-26所示。

#figure(
  image("img/fig_4_26.png", width: 55%),
  caption: [角色挂号人员的两个用例示例],
) <fig_4_26>

#par(first-line-indent: 0em)[（1）存在的问题一。]

根据结构化章节中数据流图的需求描述内容，挂号过程中还须根据病历号查询和确认病人基础信息，然后才能进行后续的挂号处理。那么，这种需求又该如何使用用例图表示？

#par(first-line-indent: 0em)[（2）存在的问题二。]

这个用例图表明角色挂号人员需要两个功能，按照这种画法只能说明这是两个相互独立的功能，即一旦进入到挂号用例中，就不能执行新病历的创建，必须完成挂号或者中断并退出挂号，才能执行新病历的创建过程。那么，如果存在一种在挂号用例中可以创建新病历，且该新创建的病历号又能与挂号进行绑定关联的功能需求时，用例图又该如何表示？

=== 用例之间的关系

第一个问题涉及"新建挂号"用例中存在一个可能的功能需求"病历查询"，它既可以独立存在，也可以存在于某个用例之中。解决第一个问题的方法是利用用例的包含关系来表示。第二个问题涉及两个用例之间在某种特殊情况下存在调用的关系，其解决方法是利用用例的扩展关系来表示。

#par(first-line-indent: 0em)[（1）用例的包含关系定义：在一些用例描述说明中，发现一段或几段相似的操作步骤，而且这些操作步骤都与角色有交互，同时可表示为一个独立的系统功能，这时可以把这部分操作步骤提取出来作为一个新的用例。其表示形式如图4-27所示。]

#par(first-line-indent: 0em)[（2）用例的扩展关系定义：用例描述的场景中存在具有条件判断分支的情况，此时可将分支部分的操作步骤独立为一个子用例，但是该子用例又与角色之间没有直接的关联关系，必须在基础用例的执行过程中某个条件成立时被调用，为此将这部分用例视为基础用例具有扩展关系的子用例，其表示形式如图4-28所示。]

#figure(
  table(
    columns: (1fr, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    image("img/fig_4_27.png", width: 100%),
    image("img/fig_4_28.png", width: 100%),
  ),
  caption: [具有包含关系的用例图（左）与具有扩展关系的用例图（右）],
)

经过上述内容的分析和修改，图4-26的结构可以变化为图4-29的内容，图中添加一个新的子用例"病历查询"，它是"新建挂号"用例的一个包含用例，解释成为"新建挂号"用例在执行到某一个环节时，将执行"病历查询"子用例，该子用例执行完成后，再继续执行新建用例的后续操作；除此之外，"创建病历"也可以成为"新建挂号"用例的一个扩展子用例，表明"新建挂号"用例在执行到某一步骤必须执行新建病历的情况时，可以调用子用例创建病历。

#figure(
  image("img/fig_4_29.png", width: 75%),
  caption: [具有子用例的挂号用例图],
) <fig_4_29>

#par(first-line-indent: 0em)[（1）常见误区一说明：非目的性用例。]

很多软件开发人员都习惯将角色使用系统功能之前的系统登录或者密码验证等类似的操作作为一个用例来表示。编者在这里特别说明这种表示的两种不合理之处。

首先，面向对象的需求分析过程还没有进行到系统设计和开发阶段，重点在于确定使用者正确理解和确认功能需求，为此不建议登录系统进行身份验证的需求在此时进行考虑。

其次，根据用例的定义，用例表示使用者具有明确目的性的某种活动，或者通过系统的功能来完成某种目的性要求的活动，然而登录系统进行身份验证，虽然在某种程度上可以理解为系统必须提供的某种安全性功能，但还不能作为业务层面用户的直接功能需求。最好的理解是，类似的情况可以作为某些用例的子用例，密码验证或者是系统登录一般可以认为是一种包含子用例。

比如，银行储户使用ATM取款机取款及查询余额的过程存在输入密码的系统交互过程，为此在构建用例图时特别注意不将"密码验证"作为独立的用例存在。出现这种情况时，需反问这样的问题：登录系统后没有其他的操作么？登录系统的目的是什么？读者能够区分如图4-30所示的图中两个用例图哪一个更加合理。

#figure(
  image("img/fig_4_30.png", width: 70%),
  caption: [两种表示密码验证用例的表示方法],
) <fig_4_30>

#par(first-line-indent: 0em)[（2）常见误区二说明：系统操作型用例。]

软件开发人员习惯将一些系统级的操作看作用例，比如数据库连接、数据通信等类型，其原因在于开发人员认为这些操作都属于系统的功能，既然需求分析阶段寻找的目标是系统功能，那么可以将这些操作视为用例。编者认为，用例是功能的一种表示，是从系统使用者的角度定义的功能，但是系统的功能未必就是用例。其原因在于使用者提出对系统的功能需求相对而言是宏观的，一个用例可能对应于系统级的多个功能，反过来一个系统级的功能未必是一个用例。使用用例的目的在于通过使用者的角度明确需求，而非直接深入到系统级别上，比如结构化方法中的数据流图很大程度上描述为响应一个数据流系统内部需要多少个功能体。

举例说明，当角色提出一个某类信息的查询时，比如挂号人员查询和调取病历信息，从需求分析的角度而言，只描述成"病历查询"即可，而不必进一步将其分解成：（1）数据库连接；（2）数据通信；（3）信息获取等具体的操作步骤。这些系统级别的操作步骤在软件对象设计时进行特殊的考虑，比如设计成一个专门负责数据库链接和操作的软件对象（DAO），由它负责所有业务级别对象的数据库表操作。图4-31展示这种问题两种错误的用例表达方式，图4-32展示正确用例的表达方式。

#figure(
  image("img/fig_4_31.png", width: 80%),
  caption: [两种错误的系统操作型用例图表达方式],
) <fig_4_31>

#figure(
  image("img/fig_4_32.png", width: 45%),
  caption: [正确的用例表达方式],
) <fig_4_32>

#par(first-line-indent: 0em)[（3）常见误区三说明：用例表示的粒度。]

在获取信息管理系统的需求时，经常遇到某类表单增删改查的4种基本操作，为此许多初学者将表单的管理表示成该表单的4个用例：表单添加、表单查询、表单修改和表单删除。一个信息管理系统如果存在大量的表单操作，则会发现存在一大堆同样的4种增删改查的用例，这就产生用例表示的粒度问题。究其原因还是归结为对用例定义的理解层面上，在这里特别提醒初学者如果遇到诸如此类的表单4种操作，只需将其表示成"表单管理"用例即可。图4-33展示不合理（左）和合理（右）的用例图表达粒度。

#figure(
  image("img/fig_4_33.png", width: 80%),
  caption: [不合理（左）及合理（右）的用例粒度],
) <fig_4_33>

=== 1. 用例描述

用例图从可视化的角度将用户对系统的功能需求进行粗略的表达，还不能将该功能的操作细节描述清楚，这对于软件的分析人员来说依然存在着巨大的风险，为此用例模型要求将用例图中每个用例的交互场景进行细化，不仅描述成功的交互，还描述多种失败的场景，这就是用例描述。用例描述的模板如表4-6所示。

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [用例编号], [每一个用例一个唯一的编号，方便在文档中索引],
    [用例名称], [（状语+）动词+（定语+）宾语，体现参与者的目标],
    [范围], [软件系统中该用例的作用范围],
    [级别], [企业目标级别/用户目标级别/子系统目标级别],
    [参与者], [调用系统服务来完成目标的主要参与者],
    [项目相关人员及其兴趣], [用户应包含满足所有相关人员兴趣的内容],
    [前置条件], [规定在用例中的一个场景开始之前必须为"真"的条件],
    [后置条件], [规定用例成功结束后必须为"真"的条件],
    [成功场景], [描述满足用例目标能够成功的交互路径，不包括条件和分支],
    [扩展（或替代流程）], [说明成功场景之外其他的场景或分支],
    [\*a], [描述任何一个步骤都有可能发生的动作，前边加\*，比如中断退出],
    [5a], [对基本路径中某个步骤的扩展描述，前边加成功场景的编号],
    [特殊需求], [与用例相关的非功能性需求],
  ),
  caption: [用例描述的标准模板],
)

用例描述的示例参见表4-7。

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [用例编号], [Uc_006],
    [用例名称], [订单处理],
    [范围], [餐馆信息管理系统],
    [级别], [用户目标级别],
    [参与者], [台面服务员],
    [项目相关人员及其兴趣], [台面服务员：登录系统后可以在PDA上方便地点菜，并生成订单，传送给后厨和吧台。后厨人员：及时看到新来的订单以准备菜品。吧台服务员：根据订单上所列菜品计算费用],
    [前置条件], [顾客落座且请求点菜],
    [后置条件], [生成该台面的订单],
    [步骤1], [顾客请求点菜],
    [步骤2], [台面服务员使用PDA开始点菜服务],
    [步骤3], [包含用例：登录系统 Uc_006],
    [步骤4], [台面服务员输入台面号],
    [步骤5], [记录顾客所点菜品，如有忌口则转扩展用例：忌口选择 Uc_006_3],
    [步骤6], [重复步骤5，直至顾客示意点菜完毕],
    [步骤7], [服务员向顾客复述所点菜品，以示确认],
    [步骤8], [顾客确认后，服务员打印订单，包含用例：打印订单 Uc_006_4],
    [步骤9], [点菜服务完成，用例结束],
  ),
  caption: [某餐厅信息管理系统的订单处理用例描述],
)

图4-34是顾客通过某电子网站购买机票和预订酒店的两个用例场景，这两个基本用例都使用一个"填写联系资料"的包含子用例，还有一个购买机票的基本用例使用的一个扩展子用例"积分换里程"。

#figure(
  image("img/fig_4_34.png", width: 70%),
  caption: [顾客购买机票和预订酒店的用例图],
) <fig_4_34>

下面给出根据这个用例图对应的用例和子用例描述。

*用例编号：1*

用例名称：购买机票

成功场景：

#par(first-line-indent: 0em)[（1）顾客输入航班查询条件。]

#par(first-line-indent: 0em)[（2）系统显示查询结果。]

#par(first-line-indent: 0em)[（11）顾客填写联系方式，包含"填写联系资料"用例。]

#par(first-line-indent: 0em)[（12）顾客支付机票费用（扩展点：现金、信用卡、积分换机票）。]

*用例编号：10*

用例名称：填写联系资料

级别：子功能

#par(first-line-indent: 0em)[（1）顾客在联系方式界面内输入办公电话、手机、Email、邮寄地址和邮箱，单击提交。]

#par(first-line-indent: 0em)[（2）系统验证上述信息格式有效。扩展（或替代流程）：]

#par(first-line-indent: 0em)[（2a）系统发现有必填信息没有填写]

- 系统给出提示信息。
- 顾客填写信息。
- 返回主要成功场景的步骤（2）。

#par(first-line-indent: 0em)[（2b）系统验证信息格式不正确]

- 回到主要成功场景的步骤（2）。

*用例编号：2*

用例名称：预订酒店

成功场景：

#par(first-line-indent: 0em)[（1）顾客输入酒店查询条件。]

#par(first-line-indent: 0em)[（2）系统显示查询结果。]

#par(first-line-indent: 0em)[（3）顾客填写入住时间。]

#par(first-line-indent: 0em)[（8）顾客填写联系方式，包含"填写联系资料"用例。]

*用例编号：12*

用例名称：积分换机票

级别：子功能

触发条件：顾客选择用积分支付航班费用

扩展点：购买机票用例中的步骤（12）

成功场景：

#par(first-line-indent: 0em)[（1）验证顾客是否有足够的积分。]

#par(first-line-indent: 0em)[（2）扣除相应的积分，将剩余积分告知顾客。]

=== 2. 系统顺序图

用例描述已经将用例的实现过程，即角色与系统之间的信息交互进行详细的描述，但其中的文字内容有可能太过复杂，容易造成理解上的偏差。为此，还须进一步将交互的详细文字内容抽象成角色与系统之间交互的系统事件（或者称之为消息），并将其表示成UML的交互图，并要求在系统顺序图上明确每条消息的名称、传递的参数及类型、返回值的结果。

系统顺序图（System Sequence Diagram, SSD）是通过使用UML的交互图元素，描述一个用例中角色与系统之间某一个场景的消息交互形式。SSD只有两类对象，一个是该用例中的角色对象，另一个是代表待构建系统的对象。一般情况下SSD只描述两个而不是两类对象，只有某些特殊的场景可能出现第3个角色对象，但是代表系统的对象只能是一个，角色对象将其视为一个黑盒对象，软件设计阶段对该对象内部进行设计，进一步描述软件系统内哪些软件对象响应角色发送给系统对象的系统事件。如图4-35展示系统顺序图的基本结构。

#figure(
  image("img/fig_4_35.png", width: 70%),
  caption: [系统顺序图的基本结构],
) <fig_4_35>

系统顺序图中除了两个参与交互的对象之外，最主要描述的内容就是对象之间传递的消息。相对于用文本进行书写的用例描述，系统顺序图更加确切地命名消息的名称和参数，同时对于同步消息的返回也进行定义。

同步消息Message_A(p_1, p_2, ..., p_n)发送到系统之后，系统经过处理产生一条标准的同步返回消息，并用构造型`<<return>>`来明确这条消息的返回类型。当然，在建模的过程中如果知道该消息返回的结果，可以显式声明返回消息的内容，比如同步消息Message_B(p_1, p_2, ..., p_m)的返回消息就是`<<return>> X, Y`。

建议和表示，为每一个用例的主要场景绘制一个系统顺序图，并为一些使用频率高和复杂的场景构建系统顺序图。表4-7用例描述内容对应的系统顺序图如图4-36所示。

#figure(
  image("img/fig_4_36.png", width: 65%),
  caption: [Uc_006 订单处理用例的系统顺序图],
) <fig_4_36>

循环部分表示服务员在点菜的过程循环记录顾客所点的每一道菜品，在有忌口的选择下使用"可选（Option）"片断来表示用例说明中相应的扩展用例部分。循环与可选的表示方法如图4-37所示。

#figure(
  image("img/fig_4_37.png", width: 65%),
  caption: [循环与可选的表示方法],
) <fig_4_37>

=== 3. 操作契约

系统顺序图上的代表待构建的软件系统对象接收角色发送的系统事件请求后，系统对象根据需求的具体内容返回一个明确的结果，这个结果称为操作契约（Operation Contract），即根据明确的角色的要求（系统事件）系统必须返回的契约结果。

用例模型的目的除了能够明确表示用户需求之外，还必须为软件的结构设计提供必需而充分的信息，为此该契约性结果还必须结合领域模型确定当前系统中哪些概念类对象参与该用例的系统事件的响应及处理，并根据当前系统中这些概念类对象为得到契约结果执行某些操作后的状态，称为该系统事件的后置条件。

在后续的软件设计过程中，可以参考以上分析结果进行软件系统内部结构的对象设计，一定程度上降低了软件开发人员为实现需求所规定的功能时必须具备的某些发明性或创造性能力，操作契约很大程度上简化了软件对象的设计，是软件对象设计的参考和依据。操作契约的组成结构如表4-8所示。

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [操作], [操作名称及其参数],
    [交叉引用], [产生该操作的用例],
    [前置条件], [在执行该操作之前的系统或者领域模型对象的状态],
    [后置条件], [在执行该操作之后领域模型对象的状态：\ (1) 对象创建或者消除;\ (2) 对象之间的"关联"创建或者消除;\ (3) 对象的属性值修改],
  ),
  caption: [操作契约的结构],
)

创建和编写操作契约的步骤：

#par(first-line-indent: 0em)[（1）根据系统顺序图识别进入到系统内的所有系统事件，即操作。]

#par(first-line-indent: 0em)[（2）针对每一个系统操作结合对应的用例领域模型，找到与此操作相关的概念类对象。]

#par(first-line-indent: 0em)[（3）对那些相对复杂及用例描述中不清楚的系统操作创建操作契约。]

#par(first-line-indent: 0em)[（4）按照以下内容描述并确定领域模型中对象的状态变化，即后置条件：]

- 对象创建或者消除；
- 对象之间的"关联"创建或者消除；
- 对象的属性值修改。

注意，在创建操作契约之前，除了观察系统顺序图，还同时观察该用例相关的领域模型，这是面向对象的分析和设计方法有别于传统的瀑布模型的地方。在完成操作契约的构建之后，同样分析操作契约的内容是否有哪些内容是可以用来修订原有的领域模型，比如出现新的概念类、类之间产生新的关系、哪些类具有某些新的属性等。此时提及的词汇"操作"（Operation）相对于UML中的"方法"（Method）应该是抽象的概念而非"方法"的具体实现。

举例说明，根据图4-36的系统顺序图，可以得到某餐馆服务员进行点菜服务时使用PDA向系统发送如下系统事件：

#par(first-line-indent: 0em)[（1）MakeNewOrder(TableId)：开始一个点菜服务请求。]

#par(first-line-indent: 0em)[（2）AddFoodItem(ItemId, quantity)：添加菜品。]

#par(first-line-indent: 0em)[（3）AddSpecialTaste(text)：添加特殊忌口信息。]

#par(first-line-indent: 0em)[（4）ListOrder(OrderNo)：列出所选菜品清单。]

#par(first-line-indent: 0em)[（5）Print(OrderNo)：打印所选菜品订单，一式三份。]

#par(first-line-indent: 0em)[（6）EndOrder()：结束一次点菜服务。]

*MakeNewOrder(TableId)：开始一个点菜服务请求。*

参考领域模型分析并说明：开始一次点菜服务，说明须创建一个新的订单。

#par(first-line-indent: 0em)[（1）根据领域模型，该订单必须与台面相关联。]

#par(first-line-indent: 0em)[（2）必须与管理该台面的服务员相关联。]

#par(first-line-indent: 0em)[（3）该订单流水号、订单创建时间必须动态创建。]

#par(first-line-indent: 0em)[（4）为了能够记录多项菜品信息，还必须初始化一个"集合"项来添加多个菜品。]

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [系统事件], [MakeNewOrder(TableId)],
    [交叉引用], [订单处理],
    [前置条件], [服务员身份验证通过，开始订单处理],
    [后置条件], [(1) 一个新的（概念类）订单创建;\ (2) 订单与（概念类）台面建立"关联";\ (3) 订单与（概念类）台面服务员建立"关联";\ (4) 订单的属性初始化：订单流水号、订单时间、存储菜品的数组等],
  ),
  caption: [操作契约 MakeNewOrder(TableId)],
)

*AddFoodItem(ItemId, quantity)：添加菜品。*

参考领域模型分析并说明：在记录顾客所点菜品的同时，说明菜品的对象被创建。

#par(first-line-indent: 0em)[（1）隐含说明订单与菜品之间的"关联"被创建。]

#par(first-line-indent: 0em)[（2）为了能显示该菜品的详细信息，订单还必须对菜品号与菜品描述概念类建立"关联"。]

#par(first-line-indent: 0em)[（3）顾客所点菜品的数量，比如5碗米饭等，被赋值。]

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [系统事件], [AddFoodItem(ItemId, quantity)],
    [交叉引用], [订单处理],
    [前置条件], [服务员正在处理订单],
    [后置条件], [(1) 一个新的（概念类）菜品创建;\ (2) 菜品与订单建立"关联";\ (3) 订单与菜品描述建立"关联";\ (4) 菜品属性被修改：quantity],
  ),
  caption: [操作契约 AddFoodItem(ItemId, quantity)],
)

按照相同的分析方法，可以得到后续系统事件的操作契约，参见表4-11至表4-14。

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [系统事件], [AddSpecialTaste(text)],
    [交叉引用], [订单处理],
    [前置条件], [服务员正在处理订单],
    [后置条件], [菜品的属性被修改：SpecialTaste],
  ),
  caption: [操作契约 AddSpecialTaste(text)],
)

说明：有关菜品忌口的选择可以通过该菜品的属性SpecialTaste来确定。

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [系统事件], [ListOrder(OrderNo)],
    [交叉引用], [订单处理],
    [前置条件], [服务员完成处理订单],
    [后置条件], [订单属性被修改：isComplete],
  ),
  caption: [操作契约 ListOrder(OrderNo)],
)

说明：当订单完成点菜的属性isComplete设置成True之后，服务员才能获取本次订单所记录的所有菜品信息，这些信息动态地存储在订单的"集合"项中。

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [系统事件], [Print(OrderNo)],
    [交叉引用], [订单处理],
    [前置条件], [服务员完成处理订单],
    [后置条件], [订单属性被修改：isPrinted],
  ),
  caption: [操作契约 Print(OrderNo)],
)

说明：订单的另一项属性isPrinted必须设置成True，然后才能将一式三份的订单打印出来；该属性也可以用来表示订单已经打印。

#figure(
  table(
    columns: (1fr, 2fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [项目], [说明],
    [系统事件], [EndOrder()],
    [交叉引用], [订单处理],
    [前置条件], [服务员完成处理订单],
    [后置条件], [(1) 订单属性被修改：isReadyToServe;\ (2) 订单与餐馆建立"关联"],
  ),
  caption: [操作契约 EndOrder()],
)

说明：服务员完成一次点菜服务，订单被转交吧台及后厨进行服务，所以isReadyToServe属性必须设置成True，用来表示订单处于服务状态，但还未结账。

== 需求分析规格说明书参考模板

具体内容参见附录一。

== 习题

1. 用例之间的关系有哪几种？它们各自的应用场合是什么？

2. 试述用例在面向对象方法中的地位。

3. 试创建下列问题域的领域模型。

#par(first-line-indent: 0em)[（1）测试工具领域知识：可以添加并运行测试用例，为了方便对测试用例的管理，也可以先添加测试集合，然后将一到多个测试用例或者测试集合添加。选中一个测试的测试用例或者测试集合，可以运行测试，并记录下测试结果。]

#par(first-line-indent: 0em)[（2）大学领域知识：学校由学院组成，每个学院有一位院长，以及多位教师和学生。每位教师可以讲多门课，一门课也可以由多位教师同时讲。学生可以选课，并有每学期的成绩记录。]

#par(first-line-indent: 0em)[（3）飞机票预订系统：预订某一航班的机票，包括具体的时间和座位；预订后，顾客必须在一定的时间内购票，否则预订无效；旅行社和航空售票处均可进行预订服务。]

4. 试给出图书管理系统中"借书"用例的系统事件。针对其中的每一个系统事件，给出操作契约。

5. 试结合校医院的组织结构，给出看病的整个流程，包括进行血常规和某项B超检查，进而总结如果需构建医院管理系统，应该有哪几个主要的角色和用例？
