#set heading(numbering: "1.1")
#show heading: it => {
  let level = it.level
  if level == 1 {
    v(20pt)
    align(center)[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 18pt, weight: "bold"); #it]
    v(15pt)
  } else if level == 2 {
    v(12pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 14pt, weight: "bold"); #it]
    v(6pt)
  } else {
    v(8pt)
    block[#set text(font: ("Noto Sans CJK SC", "Source Han Sans SC", "SimHei"), size: 12pt, weight: "bold"); #it]
    v(4pt)
  }
}

= 第 11 章 软件维护

软件维护是软件生命周期的最后一个阶段,处于软件系统投入生产运行之后,也是最长期的软件生命周期阶段。其主要任务是保证软件在相当长的时期内能够正常运行。相对于编码、测试等开发阶段而言,软件开发费用只占软件生命周期总费用的一小部分,而软件维护费用则占了大头,大约占到软件生命周期总费用的 75% 左右。因此,提高软件的可维护性、开发有效的软件维护手段,降低软件维护费用,满足用户的新需求,以延长软件生命周期是软件工程中一项极为重要的工作。

== 软件维护概述

软件在顺利交付并投入使用之后,必须保证在相当长的时期内能够正常运行。虽然不可能做到使软件维护费用保持在零水平上,但大多数软件维护的花费也远不是一个小数。目前,国外许多软件产品的开发费用的 70% 以上用于了软件维护和软件升级。而且今后百分比还有继续增长的趋势。将来的软件维护工作很可能会成为软件开发组织的负担,使软件开发组织没有余力开发新软件。

软件工程方法学的一个主要目标是提高软件的可维护性,减少软件维护的工作量,降低软件系统的总成本。

=== 软件维护的定义和分类

所谓软件维护就是在软件已经交付使用之后,为了改正错误或满足用户的新需求而修改软件的过程。即在软件运行维护阶段对软件产品所进行的一些改动。要进行软件维护的原因多种多样,归结起来有四种类型:

*1. 改正性维护*

在软件交付使用后,必然会有一定数量的错误隐藏在程序里,这些错误在某个特定的使用环境下就会暴露出来。为了识别和纠正这些错误,改正软件性能上的缺陷和改正错误进行的诊断和改正错误的过程称为改正性维护。

*2. 适应性维护*

随着计算机技术的飞速发展,外部环境(新的软硬件配置)或数据环境(数据库、数据格式、数据输入/输出方式、数据存储介质)可能发生变化,为了使软件适应这种变化而去修改软件的过程称为适应性维护。

*3. 完善性维护*

在软件的使用过程中,用户常常会对软件提出新的功能与性能要求。为了满足这些要求,需要修改或再开发软件,以扩充软件功能、增强软件性能、改进加工效率、提高软件的可维护性。这种情况下进行的维护活动称为完善性维护。

=== 影响软件维护工作量的因素

在软件维护中,维护资源直接影响维护成本,因此应该考虑哪些因素影响软件维护成本,应该采取什么维护策略才能有效地控制维护的成本和影响维护的工作量,影响软件维护工作量的因素主要包括:

#par(first-line-indent: 0em)[（1）系统大小。系统越大,所实现的功能越复杂,理解修改就越困难,改造难度越大。]

#par(first-line-indent: 0em)[（2）程序设计语言。语言的功能越强,生成程序所需的代码量就越少;语言的功能越弱,实现同样的功能所需语句就越多,程序越复杂,维护成本越大。]

#par(first-line-indent: 0em)[（3）系统的年龄。老系统不断被修改,结构变得越来越混乱;由于维护人员经常更换,程序变得越来越难理解。随着时间推移,许多系统没有文档或文档太少,且长期不更新,系统维护成本会大大增加。]

#par(first-line-indent: 0em)[（4）文档的完备程度。许多系统并未按照软件工程的要求进行开发,因此无文档,或文档太少,或在长期的维护过程中文档与程序变得不一致,这些都大大增加软件维护的难度。]

如果软件在开发时没有考虑到将来的修改,则为软件的维护带来许多问题。

#figure(
  image("img/fig_11_1.png", width: 85%),
  caption: [三类"维护"与"总维护"比例 及 "维护"在软件生存期所占比例],
) <fig-11-1>

在上述几个因素中,软件的可维护性可能是最重要的因素。

=== 软件维护中的典型问题

软件维护中出现的大多数问题,都根源于软件需求分析和设计过程中的缺陷,如果在这两个阶段不进行严格的管理和规范的管理,必然会造成软件在维护阶段产生各种问题,以下列出软件维护中经常遇到的典型问题:

#par(first-line-indent: 0em)[（1）在需求阶段人们往往忽略将来的维护问题,所以在收集需求或数据集时容易造成在测试中不出现的、由于在开发环境中的数据不足、或无法完全模拟用户的操作行为,因此,用户遇到的问题很难重现,这就给定位问题带来了很大的困难。]

#par(first-line-indent: 0em)[（2）理解别人写的程序通常非常困难,如果代码逻辑与所附注释不一致或根本没有文档,则阅读和理解程序将是一项非常繁重的工作。]

#par(first-line-indent: 0em)[（3）维护人员经常接手的是没有合适文档的、或者文档数量不足的系统,程序本身也存在许多问题或者需要维护的部分不明确,需要花费大量时间进行梳理,容易误解且与程序代码完全一致的文档在实践中是真正有价值的。]

#par(first-line-indent: 0em)[（4）不能指望原开发人员会熟悉系统。由于维护阶段持续的时间很长,原来的开发人员可能会有流动性,所以在软件维护阶段很长时间内原开发人员很少有人能参与工作。]

#par(first-line-indent: 0em)[（5）许多程序在设计时并未考虑到将来的修改,这就导致软件结构较差,当对某一部分做修改时,很容易对系统的其他部分产生不良影响,使系统变得不稳定。]

#par(first-line-indent: 0em)[（6）软件维护不是一项吸引人的工作。由于上述原因经常导致维护工作出现困难,从而使软件维护人员产生畏难情绪。]

== 软件维护活动

软件系统在上线时,维护工作较多,往往须设置专门的维护团队,完成系统的改造、测试等工作;在系统稳定运行了一段时间后,维护工作相对减少,此时可以不用设置专门的维护团队,但是必须保证随时提供能够维护系统的人员。维护工作往往是在系统发生问题,或用户提出新的需求时进行的,没有严格的时间规律。维护工作涉及的人员多、周期长、流程复杂,所以对维护工作进行周密的管理显得非常重要。为了有效地进行软件维护,应建立完善的维护工作规程,包括建立维护的机构、确定维护申请报告的流程及评价过程、为维护申请规定标准的处理步骤、建立维护活动的等级制度及评审标准等。

=== 维护机构

维护机构指对软件维护工作进行组织、协调、验收、跟踪的部门或人员。除了较大的软件开发公司外,通常在软件维护工作方面不保持正式的维护机构。但是,在软件开发部门,确立一个非正式的维护机构则是非常必要的。图 11-3 给出了典型的维护机构的组织方案。

#figure(
  image("img/fig_11_3.png", width: 80%),
  caption: [软件维护机构],
) <fig-11-3>

在维护机构中,各成员的主要职责如下:

#par(first-line-indent: 0em)[（1）系统用户是系统的使用人员,当用户在使用系统的过程中发现问题或有新的需求,可以将维护申请提交给系统维护管理员。]

#par(first-line-indent: 0em)[（2）维护管理员是维护工作的管理者和组织者,负责维护申请的受理、评价、分配、验收等工作。维护管理员通常是用户与维护机构的接口。]

#par(first-line-indent: 0em)[（3）系统分析人员负责对发生的问题进行分析,判断是否须对系统进行修改。]

#par(first-line-indent: 0em)[（4）修改负责人负责对系统进行修改,以修复系统中存在的缺陷。]

#par(first-line-indent: 0em)[（5）测试人员对修改后的系统进行充分的测试,以保证系统问题真正得以解决,并不影响其他功能。]

#par(first-line-indent: 0em)[（6）工程人员负责将修改后的系统重新部署,以满足用户的使用。]

#par(first-line-indent: 0em)[（7）配置管理人员负责对修改范围严格把关,控制修改范围,对软件配置进行审计。]

#par(first-line-indent: 0em)[（8）维护管理员、系统分析员、修改负责人、配置管理员均代表维护工作的某个职责范围,有的职责需要多个人来承担,例如不同的系统有不同的修改负责人;有的人可以承担多个职责,例如某个人既是维护管理员,也是配置管理员,以及某个系统监督人员可能须进行多个系统的维护评价工作等。]

在开始维护工作之前,明确各种维护工作的责任,可以大大减少维护过程中的混乱现象。

=== 维护申请单

维护申请（Maintenance Request, MR）是发生软件维护工作需求时由用户填写的申请文档,是用户与维护机构进行交互的接口,也是界定维护工作范围和工作量的凭证。软件维护组织通常须提供维护申请报告模板,规定维护申请的格式。维护申请一般包含的内容如表 11-1 所示。

#figure(
  table(
    columns: (auto, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [\\*字段\\*], [\\*内容\\*],
    [申请日期], [维护管理员],
    [起始日期], [优先级：低/中/高],
    [问题/需求描述], [注：如果是改正维护，须明确说明出现输入数据错误、程序错误提示或错误现象，以及其他与问题相关的佐证资料。如果是适应性维护或完善性维护，须额外提出一份修改建议书，并附带所有需要的修改],
  ),
  caption: [维护申请模板],
) <tab-11-1>

维护申请报告是由软件组织外部提交的文档,是计划维护工作的基础。如果须对系统进行修改,软件组织内部相应地给出软件修改报告（Software Change Report, SCR）,说明软件中所须修改的性质、申请修改的优先级,为满足该维护申请报告所需的工作量,预计修改后的状况等内容。修改报告一般包含的内容如表 11-2 所示。

#figure(
  table(
    columns: (auto, 1fr),
    fill: (x, y) => if y == 0 { luma(220) } else if calc.odd(y) { luma(248) } else { none },
    stroke: 0.6pt + luma(160),
    inset: (x: 8pt, y: 5pt),
    [\\*字段\\*], [\\*内容\\*],
    [申请日期], [修改负责人],
    [维护类型：改正/适应/完善/预防], [优先级：低/中/高],
    [拟满足日期], [工作量],
    [修改要求], [注：通过分析给出系统如何修改才能满足用户要求的修改方案],
  ),
  caption: [软件修改报告模板],
) <tab-11-2>

=== 软件维护工作流程

软件维护工作涉及人员多,流程复杂,在 1993 年 IEEE 计算机学会软件工程标准分委员会颁布的 IEEE 1219《软件维护标准》中定义了软件维护工作流程,具体步骤如下所述。

#par(first-line-indent: 0em)[（1）维护申请：一般由用户、程序员或管理人员提出,是软件维护的开始阶段。]

#par(first-line-indent: 0em)[（2）分类与鉴别：根据软件维护申请（MR）,由维护机构来确认其维护类别,给 MR 一个编号,并输入数据库保存。这是整个维护阶段收集数据与审查的开始阶段。]

#par(first-line-indent: 0em)[（3）分析：先进行维护的可行性分析,在此基础上进行详细分析。可行性分析主要确定软件更改的影响、可行的解决方案及所需的费用。详细分析则需要提出更完整的更改需求说明,鉴别须更改的要素,提出测试方案和策略,制定实施计划。]

#par(first-line-indent: 0em)[（4）设计：汇总全部信息,开始更改,如开发过程的工程文档、分析阶段的结果、源代码、资料信息等。本阶段应更改设计的基线,更改测试计划,修订详细分析结果,核实维护需求。]

#par(first-line-indent: 0em)[（5）实现：本阶段的工作是制订程序更改计划并进行软件更改,包括编码、单元测试、集成、风险分析、测试准备审查、更新文档。风险分析在本阶段结束时进行。所有工作应该置于软件配置管理系统的控制之下。]

#par(first-line-indent: 0em)[（6）系统测试：系统测试主要测试程序之间的接口,以确保修改的软件满足用户的需求,回归测试确保不引入新的错误。]

#par(first-line-indent: 0em)[（7）验收测试：这是最终的综合测试,应由客户、用户和第三方共同进行。这个阶段报告测试结果,进行功能配置审核,建立软件新版本,准备软件文档的最终版本。]

#par(first-line-indent: 0em)[（8）交付：此阶段将新的系统交给用户安装并运行。供应商应审核实物配置,通知所有用户,备份文档版本,完成安装与培训工作。]

== 维护期的软件开发

进行软件维护时,必然对源程序进行修改,通常对源程序的修改须经历以下三个步骤,分析和理解程序、修改程序、重新验证程序。下面分别介绍。

=== 分析和理解程序

全面、准确、迅速地理解程序是决定维护工作成败和质量好坏的关键。在这方面,软件的可理解性和文档的质量非常重要。在进行软件维护之前,必须:

#par(first-line-indent: 0em)[（1）理解程序的功能和目标。]

#par(first-line-indent: 0em)[（2）掌握程序的结构信息,即从程序中细分出若干结构成分。如程序系统结构、控制结构等。]

#par(first-line-indent: 0em)[（3）了解数据流信息,即所涉及的数据来源,在哪里使用等。]

#par(first-line-indent: 0em)[（4）了解控制流信息,即执行每条路径的结果。]

#par(first-line-indent: 0em)[（5）理解程序的操作要求。]

=== 修改程序

程序修改时,必须事先计划,然后周密有效地实施修改工作。

*1. 设计程序的修改计划*

程序的修改计划须考虑人员和资源的安排。小的修改工作不需要详细的计划,但是对于需耗时数月的修改工作,必须制订修改的计划,修改计划的内容主要包括以下四项。

#par(first-line-indent: 0em)[（1）规格说明信息：数据修改、处理修改、作业控制语言修改、系统之间接口的修改等。]

#par(first-line-indent: 0em)[（2）维护资源：新程序版本、测试数据、所需的软件系统、计算机时间等。]

#par(first-line-indent: 0em)[（3）人员：程序员、用户相关人员、技术支持人员、厂家联系人、数据录入人员等。]

#par(first-line-indent: 0em)[（4）办公资源：纸面、计算机媒体等。]

针对以上每一项,可采用自顶向下的方法,在理解程序的基础上,进行以下操作:

#par(first-line-indent: 0em)[（1）研究程序的各个模块、模块的接口、数据库,从全局的观点提出修改计划。]

#par(first-line-indent: 0em)[（2）依次把待修改,以及那些受修改影响的模块和数据结构分离出来。为此,须识别受修改影响的数据;识别使用这些数据的程序模块;对于上面程序模块,按属于产生数据、修改数据,还是删除数据进行分类;识别这些数据元素的外部控制信息;识别编辑和检查这些数据元素的地方;隔离待修改的部分。]

#par(first-line-indent: 0em)[（3）详细分析待修改以及那些受变更影响的模块和数据结构的内部细节,设计修改计划,标明新逻辑及待改动的现有逻辑。]

#par(first-line-indent: 0em)[（4）向用户提供回避措施。用户的某些业务因软件中发生问题而中断,为不让系统长时间停止运行,须把问题局部化,在可能的范围内继续开展业务。如果问题原因未明确,先就问题现象提供回避的操作方法;如果原因已明确,则可以通过临时修改或改变运行控制等手段回避系统运行时产生问题。]

*2. 修改代码,适应变化*

在制订修改计划之后,严格按照计划的内容修改代码。在修改过程中,尽量按照以下要求进行:

#par(first-line-indent: 0em)[（1）正确有效地编写修改代码。]

#par(first-line-indent: 0em)[（2）谨慎地修改程序,尽量保持程序的风格及格式;在程序清单上注明改动的语句。]

#par(first-line-indent: 0em)[（3）不删除程序语句,除非完全肯定它是无用的。]

#par(first-line-indent: 0em)[（4）不试图共用程序中已有的临时变量或工作区;为避免冲突或混淆用途,应自行设置变量。]

#par(first-line-indent: 0em)[（5）插入错误检测语句。]

#par(first-line-indent: 0em)[（6）在修改过程中做好修改的详细记录,消除变更中任何副作用。]

修改后程序有可能带来一些副作用。所谓副作用是指因修改软件而造成的错误或其他不希望发生的情况,修改程序带来三种副作用:

*1. 修改代码的副作用*

在使用程序设计语言修改源代码时,都可能引入错误。例如,删除或修改一个子程序、标号、标识符,改变程序代码的时序关系,改变占用存储的大小,改变逻辑运算符,修改文件的"打开"或"关闭",改进程序的执行效率,以及把设计上的变化翻译成代码的变化、边界条件逻辑测试的变化时,都容易引入错误。

*2. 修改数据的副作用*

在修改数据结构时,有可能造成软件设计与数据结构不匹配,因而导致软件出错。数据副作用是修改软件信息结构导致的结果。例如,在重新定义局部或全局的常量,重新定义记录或文件的格式,增大或减小一个数组或高层数据结构的大小、修改全局或公共数据、重新初始化控制标志或指针、重新排列输入/输出或子程序的参数时,容易导致设计与数据不兼容的错误。数据副作用可以通过详细的设计文档加以控制。在此文档中描述一种交叉引用,把数据元素、记录、文件和其他结构联系起来。

*3. 文档的副作用*

对数据流、软件结构、模块逻辑或任何其他有关特性进行修改时,必须对相关技术文档进行相应修改,否则会导致文档与程序功能不匹配、默认条件改变、新错误信息不正确等错误,使得软件文档不能反映软件的当前状态。对于用户来说,软件事实上就是文档。如果对可执行软件的修改不反映在文档里,就会产生文档的副作用。例如,对交互输入的顺序或格式进行修改,如果没有正确地记入文档中,可能引起重大的问题。过时的文档内容、索引和文本可能造成冲突,引起用户的不满。因此,必须在软件交付之前对整个软件配置进行评审,以减少文档的副作用。

=== 重新验证程序

将修改后的程序提交用户之前,须用以下的方法进行充分的确认和测试,以保证修改后的软件正确性。

*1. 静态确认*

修改软件可能引起新的错误。程序修改完成后,须对修改进行静态确认,为了提高确认的有效性,验证修改后的程序至少需要两个人参加,检查内容包括:

#par(first-line-indent: 0em)[（1）软件修改是否涉及规格说明？修改结果是否符合规格说明？有没有曲解规格说明？]

#par(first-line-indent: 0em)[（2）程序修改是否足以修正软件中的问题？源程序代码有无逻辑错误？修改时有无修补失误？]

#par(first-line-indent: 0em)[（3）修改部分对其他部分有无不良影响？]

对软件进行修改,常引发别的问题,因此有必要检查修改的影响范围。

*2. 计算机确认*

在充分进行以上确认的基础上,用计算机对修改程序进行确认测试。

#par(first-line-indent: 0em)[（1）确认测试顺序：先对修改部分进行测试,然后隔离修改部分,测试程序未修改的部分,最后再把它们集成起来进行测试。这种测试方式称为回归测试。]

#par(first-line-indent: 0em)[（2）准备标准的测试用例。]

#par(first-line-indent: 0em)[（3）充分利用软件工具重新验证。]

#par(first-line-indent: 0em)[（4）在重新确认过程中,邀请用户进行测试,以确认问题是否解决。]

*3. 维护后的验收*

在交付新软件之前,维护主管部门要检验以下内容:

#par(first-line-indent: 0em)[（1）全部文档是否完备,并已更新。]

#par(first-line-indent: 0em)[（2）所有测试用例和测试结果已经正确记载。]

#par(first-line-indent: 0em)[（3）记录软件配置所有副本的工作已经完成。]

#par(first-line-indent: 0em)[（4）维护工序和责任已经确定。]

== 习题

+ 什么是软件维护？为什么需要软件维护？

+ 软件维护分为哪几类？简述每类的维护过程。

+ 列举影响软件维护工作量的因素。

+ 下面有关软件维护的叙述有些是不准确的,试将它们列举出来。供选择的答案:

  + 为维护一个软件,必须先理解这个软件。

  + 阅读别人写的程序并不困难。

  + 文档不齐全,也可以维护一个软件。

  + 谁编写软件,谁维护这个软件。

  + 设计软件时应考虑到将来修改软件的情况。

  + 维护软件是一件很吸引人的工作。

  + 维护软件就是改正软件中的错误。

  + 软件维护是一项复杂的工作。

+ 简述软件维护的工作流程,阐述各维护机构在维护过程中的主要任务。

+ 软件维护过程中对程序的修改会带来哪些副作用？
